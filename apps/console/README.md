# apps/console

Next.js clinician workspace + coordinator workspace + institution portal (same app,
role-separated). Build spec v3 §11, §12, §13.

Not yet built. Built at **M8** ("Clinician + coordinator + institution consoles; masked
calling; referrals; public dashboard") — the final Phase 1 milestone, per
`docs/tarragon-build-spec-v3.md` §20. Do not add functional code here out of order.
