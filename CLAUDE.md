# Tarragon Health — Claude Code Master Instructions (v3)

> Read every session. Full business detail: `docs/FEATURE_SPEC.md`. Full brand/voice/UI: `docs/BRAND_GUIDE.md`. Marketing site: `docs/MARKETING_SITE_SPEC.md`. Competitive-intelligence feature roadmap: `docs/FULL_SPECIFICATION_V4.md`. Clinician attribution & trust model: `docs/CLINICAL_TRUST_MODEL_SPEC.md` — authoritative for anything touching clinician/doctor attribution or escalation branding. This file is the operating contract — keep it under 200 lines, update "Current Sprint" every sprint.

## The Business
Nigeria's digital-first chronic disease, preventive health, and family care coordination OS — the trusted coordination layer between patients, families, clinicians, labs, pharmacies, HMOs, and employers. App/web-first, clinician-led, escalation-driven, AI-automated, partner-network based, with WhatsApp/SMS as a follow-up and notification layer only (see Non-Negotiable Business Rules). **No owned clinics.** Five categories, all architecturally represented from Sprint 1 — they are commercially linked, each feeds the others:

1. **Chronic Disease Management** *(core wedge)* — hypertension, diabetes; expansion: asthma, CKD, heart failure
2. **Preventative Medicine** — cancer/metabolic/infectious/reproductive screening. **Abnormal result → Category 1 upgrade is the highest-priority business event in the platform — never lose it, never let it fail silently.**
3. **Care Coordination** — lab network, pharmacy network, specialist referrals, hospital handoffs
4. **B2B & Institutional** — corporate wellness, HMO capitation, NHIA/government programmes
5. **Platform Infrastructure** *(backbone, not a product line)* — WhatsApp/SMS notification engine (reminders, alerts, confirmations — never signup or a feature's only interface), clinician-led delivery, AI clinical decisioning, longitudinal patient record, partner API layer, analytics

Prevention and chronic management **share the same patient record** — design every table and dashboard for dual-state.

## Architecture: Two-Layer (Stack A — Final, Do Not Relitigate)

### Primary Platform — TypeScript
- Web: Next.js 16, TypeScript, Tailwind, shadcn/ui (`apps/web`) — this Next.js has breaking changes vs. training data; read `node_modules/next/dist/docs/` before writing framework code
- **Marketing site:** public pages live in `apps/web/src/app/(marketing)/` as a route group inside the same Next.js app — not a separate package yet. `middleware.ts` routes by hostname: root domain → marketing, `app.` subdomain → platform. Full spec, page copy, and design direction in `docs/MARKETING_SITE_SPEC.md` — read it before building any marketing page. Split into `apps/marketing` only when marketing needs its own CMS/team/deploy velocity — not yet. Marketing pages must not import platform/auth modules; Contact/Join is the only page that writes to Supabase (`leads` table).
- Mobile: React Native Expo (`apps/mobile`)
- DB/Auth/Storage/Realtime: Supabase Postgres, **eu-west-1** region (Supabase has no Africa region; closest available to Nigeria — NDPR residency gap accepted for now), pgvector
- Cache/queues: Upstash Redis
- AI workflows: LangGraph.js + Claude API
- Comms: WhatsApp Cloud API + Termii SMS (fallback) — **follow-up/notification channel only** (reminders, alerts, confirmations); never required for signup or for any feature to function — see Non-Negotiable Business Rules
- Payments: Paystack (NGN), Stripe (GBP/USD diaspora)
- Hosting: Vercel (web + Edge Functions), Railway (persistent compute/background jobs), Cloudflare (DNS/edge)

### ML Microservice — Python (`services/ml/`)
- FastAPI 0.115+, Python 3.12, package manager **uv only** (never bare pip)
- scikit-learn, pandas, numpy, scipy; Pydantic v2 everywhere; pytest + httpx
- **Stateless. No database access. No file writes.** Patient data arrives in the request body — never pulled by the service.

### Service Communication Rules
- TypeScript → ML service over HTTP only (`ML_SERVICE_URL`), auth header `X-Service-Key` (`ML_SERVICE_KEY`)
- 5-second timeout, graceful fallback — **the platform must keep working if ML is down**
- `packages/shared/ml-client.ts`: typed client, never throws, returns `null` on error

## Non-Negotiable Business Rules
- All NGN amounts stored in **kobo** (smallest unit). Diaspora billing: GBP primary, USD secondary, via Stripe.
- **Superseded 2026-07-11 — WhatsApp is not a required interface for signup or core platform actions.** Signup, onboarding, and every core patient/clinician transaction (vitals/meds/screening/booking logging, dose tracking, etc.) happen via app or web only — no bot-driven data entry over WhatsApp, ever, and no feature may be built to depend on a WhatsApp send succeeding. WhatsApp/SMS (Termii fallback) still carries reminders/alerts/confirmations, **and patients may message their doctor on WhatsApp for support, with the doctor replying on WhatsApp too** — that two-way channel is human-routed (a clinician inbox), never parsed by automation into a platform action.
- Phone numbers always E.164 (`+234XXXXXXXXX`). Timezone always `Africa/Lagos`.
- Every table has `organisation_id` — always filter by it. **RLS enforced at the Postgres level for every multi-tenant table — never bypass, never filter in application code instead.**
- Clinician:patient ratio target — **1:120**. Four-level clinical escalation: routine → clinician review → urgent escalation → emergency/urgent care advice.
- Abnormal screening result handling (Cat 2→1 upgrade): Supabase trigger → Edge Function → clinician WhatsApp alert **immediate, not scheduled** → clinician has a 4-hour contact SLA → surfaces as Priority 1 (red) on clinician dashboard.

## Device & Wearable Integration (Planned — Phase 3, not yet built)
Full spec: `docs/FULL_SPECIFICATION_V4.md` §5/§9 (`wearable_connections`, `wearable_readings` tables; `app/routers/wearables.py`). Contract to follow when this is built:
- **Ingestion boundary, not owned hardware** — TarragonHealth never talks to device firmware directly. Two ingestion paths only: (1) consumer platform sync (Apple Health, Google Fit/Health Connect, Oura, WHOOP, Garmin, Fitbit) via their cloud APIs/webhooks; (2) clinical Bluetooth devices (BP cuffs, glucometers, smart scales) paired via the Expo mobile app's native BLE, which uploads readings to the platform — the ML service itself never touches a device.
- **App/web manual entry is never removed.** Device sync is an additive faster path into the same vitals tables patients already log into manually via app/web — same downstream escalation logic, same `patient_risk_scores`, same abnormal-result pipeline. No dual source-of-truth. WhatsApp/SMS may remind a patient to log a reading; it is not itself an entry interface.
- Every reading gets `organisation_id` + RLS like any other table; store raw values + unit + `source` (device vs. manual) on `wearable_readings` for audit.
- Diaspora/premium tier only for the consumer wearable ecosystem (Apple Health/Oura/WHOOP/Garmin/Fitbit) — Bluetooth clinical devices (BP cuff/glucometer/scale) are core-tier, sold as device bundles per `docs/FEATURE_SPEC.md`.
- Per `docs/FULL_SPECIFICATION_V4.md` §11: this is roadmap, not current work — do not start building it unless the user explicitly asks or "Current Sprint" below is updated to point at it.

## TypeScript Code Rules
- Strict mode always. No `any`. Ever. pnpm only.
- All DB queries via Supabase client with RLS active. All API routes validate input with Zod. React Query for all data fetching.
- kebab-case files, PascalCase components. `NEXT_PUBLIC_` prefix only for client-safe env vars. Jest tests for every service function.

## Python Code Rules
- Python 3.12 + uv only. Pydantic v2 schemas for every endpoint — no untyped dicts. Type hints on all params/returns.
- `async def` for all endpoints. Models loaded once at startup (lifespan), never per request. pytest for every model function.
- Never import from `apps/` or `packages/` — the ML service is fully standalone.

## Key Partners & Market References (use real names in seed data / demos)
- HMOs: Reliance, Avon, Ronsberger, Wellahealth. Labs: Synlab Nigeria, Cerba Lancet, Healthtracka, Afriglobal Medicare.
- Comparable platforms referenced for positioning: Helium Health, Wellahealth.

## Brand (see `docs/BRAND_GUIDE.md` for full system)
- Master tagline: **"Care that stays with you."** Wordmark: **TarragonHealth** (camel-case). Mark: **Guard Leaf** (shield + sprout crown + checkmark vein).
- Tarragon Green `#0E7C52` (brand/primary actions), Clinical Navy `#12324B` (B2B/clinical documents). Clinical dashboard status colours (green/amber/red/blue/grey) are a **separate system** from brand colour — never confuse the two.
- Voice: a clinician who knows your name, not a hospital PA system. No fear-based urgency, no "WARNING:", no clinical jargon in patient-facing copy.

## Current Sprint (UPDATE THIS EVERY SPRINT)
Current Sprint: Sprint 4 — Python ML Microservice — **ON HOLD (2026-07-09)** per user decision to prioritize other platform work; do not resume ML work unless explicitly asked.
Sprint Goal (paused): Build out `services/ml` (Sprint 1 gave it a bare FastAPI scaffold) into the SCORE2 CVD/HbA1c-trajectory/BP-control/lab-interpretation/cohort-analytics service, wire it into the TypeScript platform via `packages/shared/ml-client.ts`, and deploy it — per `docs/FEATURE_SPEC.md` §4 Sprint 4 detail (weeks 7–9).
State at pause: Week 9 done — `packages/shared/ml-client.ts` has typed helpers for all 6 endpoints (`cvdRisk`, `hba1cTrajectory`, `bpControl`, `interpretLabs`, `analyseCohort`, `batchPredict`), each never-throws/null-on-failure per the existing contract. Wired into real call sites: BP-control assessment after every blood-pressure vitals log (`apps/web/.../patient/actions.ts`); a new clinician lab/screening-result form (`.../clinician/patients/[patientId]/screening-result-form.tsx`) that calls `/interpret/labs`, opportunistically computes CVD risk (when a lipid panel + existing BP/smoking data are available) and HbA1c trajectory (via the new `lab_analyte_readings` history table), and writes `patient_risk_scores`; the corporate dashboard now renders real `/analytics/cohort` output scoped to the admin's own `organisation_id` (no PII sent). Sentry wired behind an optional `SENTRY_DSN` env var (no-op if unset); Dockerfile/health check confirmed deploy-ready as-is. **Confirmed live 2026-07-12:** `GET https://tarragonweb-production.up.railway.app/health` returns `{"status":"ok","service":"tarragon-ml","version":"0.1.0","environment":"production"}` and `/docs` (FastAPI Swagger) returns 200 — the Railway deploy already happened (via a merged PR, ~2026-07-09), so that step is done. **End-to-end wiring also confirmed 2026-07-12:** queried `patient_risk_scores` directly — 2 real rows with `score_type: bp_control`, `model_version: bp_control_v1` (2026-07-07/08). Per `patient/actions.ts`'s `assessBpControlBestEffort`, that row only gets written when `mlClient.bpControl()` returns non-null, which only happens when `ML_SERVICE_URL`/`ML_SERVICE_KEY` are configured and the live call succeeds — so the full path (Vercel prod → Railway ML service → back into Supabase) has demonstrably worked at least twice. Caveat: both rows predate the "Fix Railway build" PR (~2026-07-09) by a day or two, and there's no MCP tool access to read Vercel's env var values directly to confirm today's `ML_SERVICE_URL` still points at the current Railway URL — if in doubt, log a fresh BP vitals reading and check for a new `bp_control` row dated after 2026-07-09. Still need to create a Sentry project (needs the user's cloud credentials/DSN, not available in this environment).
Active Service: TypeScript — **marketing site + platform convergence, merged to `main-dev` 2026-07-11 (PR #15).** Landed: full marketing site (homepage, 4 priority programmes, pricing, contact/leads, `/medication` + `/labs`); the previously-missing **AbnormalResultHandler** Edge Function (`supabase/functions/abnormal-result-handler`), deployed and verified live against `koiplnmbgnqnbywhpjlf`; an abnormal-screening E2E test (`apps/web/e2e/`, opt-in via `pnpm test:e2e`); the WhatsApp policy change (app/web is the sole interface for signup/core actions; WhatsApp/SMS is notifications + human doctor↔patient support chat only — see Non-Negotiable Business Rules, `docs/ARCHITECTURE.md` §1.3/§8, `docs/FEATURE_SPEC.md`); `docs/FULL_SPECIFICATION_V4.md` roadmap doc. **Staging deploy (2026-07-11):** Vercel auto-deployed `main-dev`'s merge commit (`677735d`) to its Preview environment — build succeeded per Vercel's own check, but the URL sits behind Vercel's deployment-protection/SSO gate so it hasn't been independently browser-verified; ask the user to confirm or disable protection for this environment. CI on the PR/merge: TypeScript (typecheck/lint/test/build) passed; Python ML failed on a **pre-existing, unrelated** `mypy` error in `services/ml/tests/test_score2.py` — confirmed already failing on main-dev's last 4 runs before this merge, not something this PR touched or introduced. **Resolved (2026-07-12):** (a) local migration filenames now match the remote project's applied migration history exactly (30 files renamed to their real applied timestamps; committed `df955dd`); the previously-missing `20260711000000_leads.sql` was also applied to remote (was never run — the marketing Contact page's lead capture was silently failing — now live as version `20260712024423`). (b) The Railway `enchanting-playfulness` service was investigated: despite the alarming `@tarragon/web` label, its Root Directory/Dockerfile Path/Watch Paths were always correctly scoped to `services/ml` — it was never building the web app, just mislabeled. Renamed to `@ml-service` for clarity. Its deploy history shows a merged PR ("Kolalonge-creator/phase8/ml-risk-models — Fix Railway build") from ~2026-07-09, currently Active/Online — confirmed via `curl` against `/health` and `/docs` that **the ML microservice is genuinely live on Railway already** (see Sprint 4 state-at-pause note above), so that part of the paused sprint's "remaining work" is actually done. **Resolved (2026-07-12, cont.):** (c) the `private.handle_new_user()` trigger bugs (phone missing leading `+`, role/organisation_id dropped on delayed `app_metadata` update) were already fixed by `20260711222638_fix_handle_new_user_metadata_timing_and_phone.sql` — confirmed via `pg_get_functiondef()` that the deployed `private.handle_new_user()` and `private.sync_user_profile_from_metadata()` on remote match the migration byte-for-byte, both triggers (`on_auth_user_created`, `on_auth_user_metadata_updated`) exist and are enabled on `auth.users`, and the migration file is now committed (`df955dd`) — the "uncommitted drift" note was stale. **Resolved (2026-07-12, cont.):** (d) the `/corporate`/`/hmo` route collision — moved the platform dashboards to `/dashboard/corporate` and `/dashboard/hmo` (`ROLE_HOME_PATH` in `apps/web/src/lib/auth/roles.ts`), verified in-browser that the new paths still redirect unauthenticated users to login and the bare `/corporate`/`/hmo` now 404 cleanly instead of colliding. Marketing is free to build real pages at those bare paths whenever that's prioritized (add to `MARKETING_ROUTES_BUILT` in `apps/web/src/lib/marketing/routes.ts` once built). **Resolved (2026-07-12, cont.):** (e) the pre-existing Python ML mypy failure — `services/ml/tests/test_score2.py` used a shared `dict(...)` + `**kwargs` spread across several tests; mypy widens such dict literals to `dict[str, object]`, which can never satisfy `score2_risk`'s precisely-typed keyword-only params, so the spread always failed `arg-type` checks even though the code ran fine. Fixed by inlining each call's full argument list instead of spreading — `uv run mypy .` now passes clean (40 files), pytest still green. (f) `/about` page built per `docs/MARKETING_SITE_SPEC.md` §3.4 — hero, continuity-thesis pillars, CTA band, wired into `MARKETING_ROUTES_BUILT` and the footer nav, verified in-browser (desktop + mobile, no console errors), `typecheck`/`lint` clean. **The founder name/photo/bio are bracketed placeholders** (`[Founder Name]`, etc.) — no real founder bio exists anywhere in the repo, so none was invented; don't treat this page as ready to announce publicly until a human supplies the real bio. Also committed separately: an already-finished-but-uncommitted WhatsApp support-inbox webhook (`supabase/functions/whatsapp-webhook` — human-routed clinician inbox only, no bot/intent parsing, per the WhatsApp business rules above) and a Termii sender-ID rename (`TarragonHlth` → `Tarragon`, matching the ID pending carrier approval). **Next:** `/corporate` and `/hmo` marketing pages (route collision already resolved in (d), just unbuilt) are the only remaining items in `docs/MARKETING_SITE_SPEC.md` §7's Definition of Done, then a Lighthouse pass and merge. See `docs/MARKETING_SITE_SPEC.md` §7, `docs/ARCHITECTURE.md` §7/§17 item 5. **Clinical trust model foundation started (2026-07-12, uncommitted):** first two build-sequencing items from `docs/CLINICAL_TRUST_MODEL_SPEC.md` §7 — `clinical_staff` table (migration `20260712191500_clinical_staff.sql`, org-scoped, patient-readable/staff-writable RLS) and `escalations.reviewed_by`/`reviewed_at` (migration `20260712191600_escalations_reviewed_by.sql`) — applied to `koiplnmbgnqnbywhpjlf`, types regenerated. Built the shared null-gated `ReviewedByDoctor` component (`apps/web/src/components/reviewed-by-doctor.tsx`) per §2's engineering rule; `useResolveEscalation` now sets `reviewed_by`/`reviewed_at` once, at resolve time, to the resolving doctor (never retroactively — §5); wired into the doctor escalation detail page. Verified end-to-end with a throwaway service-role script (not committed): gate holds (renders nothing) while `reviewed_by` is null, resolves to the real `clinical_staff` name+credential after resolve, and falls back to a generic line — never an invented name — when `reviewed_by` is set but has no backing `clinical_staff` row. **Care team assignment also built (2026-07-12, uncommitted):** `care_team_assignment` table (migration `20260712201000_care_team_assignment.sql`, one row per patient, upserted on reassignment) + RLS; a clinician-side assignment form (`.../clinician/patients/[patientId]/care-team-form.tsx`, backed by `apps/web/src/lib/queries/clinical-staff.ts`) that picks a clinician from `clinical_staff` and auto-resolves the org's active Clinical Director (never caller-chosen, per spec §1); a patient-facing `YourCareTeam` card (`apps/web/src/components/your-care-team.tsx`) on the patient dashboard, gated the same way as `ReviewedByDoctor` — renders nothing until a real assignment + backing `clinical_staff` records exist. Verified end-to-end with another throwaway service-role script (not committed). **protocol_versions also built (2026-07-12, uncommitted):** append-only ledger table (migration `20260712210000_protocol_versions.sql`, `approved_by` → `clinical_staff` not `profiles`, no update/delete grant at all for `authenticated` — not just an RLS gap) + a new admin settings page (`/admin/settings/protocols`, linked from `/admin`) where only the org's active Clinical Director can sign a new version of a named protocol (`protocol_id` slug + free-text content); any other caller's signing attempt is rejected app-side. Verified with a third throwaway service-role script: non-director blocked, version numbers auto-increment per `protocol_id`, `approved_by` correctly points at the `clinical_staff` row. **MDCN/NMCN credential verification also built (2026-07-12, committed `9ec82e6` + follow-up, uncommitted):** migration `20260712220000_clinical_staff_verification.sql` adds `clinical_staff.verified_by` (→ `profiles`) plus two DB CHECK constraints (not just RLS/app checks) — `clinical_staff_active_requires_verification` (can't set `active` without `license_verified_at` set) and `clinical_staff_no_self_verification` (`verified_by` can never equal the record's own `profile_id`). New `/admin/settings/clinical-staff` page (linked from `/admin`) is also the first UI to actually create `clinical_staff` records at all — previously the care-team and protocol-signing forms could only select from rows that had to be inserted by hand. Verified with a fourth throwaway service-role script: activation before verification is rejected by the DB, self-verification is rejected by the DB, admin verification + subsequent activation succeeds. **Patient-facing escalation view also built (2026-07-12, uncommitted):** `PatientEscalations` (`apps/web/src/components/patient-escalations.tsx`) lists the caller's own escalations (patient-friendly status copy, not the staff worklist labels) with `ReviewedByDoctor` composed into each row — no schema change needed, existing `escalations` RLS already let a patient read their own rows. Wired onto the patient dashboard below `YourCareTeam`. Verified with a fifth throwaway service-role script. **Onboarding wizard + WhatsApp signature wiring also built (2026-07-12, uncommitted):** `profiles.onboarding_completed_at` (migration `20260712230000_onboarding_completed_at.sql`, backfilled for the 4 pre-existing patient profiles so no one gets an unexpected redirect) gates a new `/onboarding` screen (honest care-model consent copy per spec §5 + `YourCareTeam` preview) that `/patient` redirects to until completed — **verified live in-browser** end to end (real signup-style login → onboarding → dashboard → reload doesn't bounce back). `support_messages.sender_id` (migration `20260712231500_support_messages_sender.sql`) + a new **deployed** Edge Function `send-support-reply` (verify_jwt=true, checks caller is org staff, signs every reply with the real clinical_staff name, sends via WhatsApp Cloud API free-text within the 24h window, fails closed and still records the message if WHATSAPP_TOKEN isn't set) + a clinician support-inbox UI (`/clinician/support-inbox`, thread list + reply). **Verified live in-browser**: real inbound message → clinician replies → message shows with correct real signature and `sender_id`, inbound thread flips to `replied`, and the send failure is honestly surfaced to the clinician ("WhatsApp send didn't go through") rather than hidden — confirms the fail-closed contract holds even with a real deployed function. That's every item from spec §7 except the Health Passport (doesn't exist as a feature at all yet — `/patient` still just lists it as "coming") and ops-only items (annual re-verification scheduling, indemnity insurance tracking). **Patient subscriptions, Paystack payments, add-ons, and subscription-based entitlements built (2026-07-12, uncommitted) — Sprint 6, NGN/Paystack only, Stripe/GBP diaspora is an explicit follow-up:** `add_ons`/`subscription_add_ons` catalogue+attachment tables (migration `20260712201454`, each attached add-on gets its own independent Paystack Plan+Subscription since intervals can differ from the base plan), `payment_transactions` webhook-idempotency/audit log (migration `20260712201507`, no authenticated write grant — only the webhook's service-role key writes), `subscription_plans`/`add_ons.price_locked` enforced by a DB trigger (migrations `20260712201431`/`20260712203438`) that blocks price/currency/interval edits once a plan has a real subscriber — the admin UI offers "clone as new plan" instead. Generalized `public.has_feature_access(feature)` RPC (migration `20260712201523`) replaces the old inline plan-features query inside `has_ai_coach_access()` (behavior-preserving refactor, regression-verified). Seed data (`supabase/seed/seed.sql`) rewritten to match the live pricing page's actual tiers (`free`/`essential`/`complete`/`family` + yearly variants) — the old codes (`free_tracker`/`basic_monitoring`/etc.) were stale and never matched `pricing.ts`; same fix applied to remote (`koiplnmbgnqnbywhpjlf`, confirmed zero real subscribers referenced the stale rows before deleting them). Paystack integration: server-only client (`apps/web/src/lib/paystack/`, hosted-checkout redirect flow, no card data or `NEXT_PUBLIC_` key involved) + deployed `paystack-webhook` Edge Function (mirrors `whatsapp-webhook`'s signature-verified/idempotent/never-throws shape) at `https://koiplnmbgnqnbywhpjlf.supabase.co/functions/v1/paystack-webhook`. Entitlement gating (`RequiresEntitlement`/`UpgradePrompt` components, null-gated per the established pattern) applied to the 5 capabilities `pricing.ts`'s Free-tier footnote excludes (clinician review, doctor check-in, lab/pharmacy booking, medication refill coordination, family dashboard) — the last two required building new UI that didn't exist yet (`/patient/family`, `care-team-contact.tsx`). Full funnel wired: onboarding gained a plan-selection step (`/onboarding` → Paystack redirect → `/onboarding/checkout-callback`, webhook-driven activation), self-serve `/patient/subscription` (upgrade/downgrade/add-on attach-detach/cancel), and admin `/admin/settings/subscriptions` (create/clone/activate plans+add-ons, auto-synced to Paystack). **Verified live in-browser** end-to-end with synthetic throwaway accounts (never real user data): Free-tier signup → onboarding plan selection → dashboard renders the correct upgrade prompts on all 5 gated sections and none of the gated content leaks through → `/patient/subscription` correctly filters add-ons by `restricted_to_plan_code` → attempting checkout without Paystack keys degrades gracefully ("Card payments aren't set up yet") rather than crashing → admin create-plan flow works end-to-end for a free-tier test plan. Also verified via throwaway service-role SQL: the add-on plan-restriction trigger, price-lock trigger, family-plan member-cap trigger, `has_feature_access` resolution, and RLS isolation (a patient can't read `payment_transactions` or another patient's `subscriptions`) all behave correctly. **Resolved (2026-07-13):** the user filled real Paystack test-mode keys and completed a real test-mode checkout — `payment_transactions` shows a genuine `charge.success` processed successfully, empirically confirming `PAYSTACK_WEBHOOK_SECRET` is correctly set as an Edge Function secret (the webhook fails closed and rejects everything when it's unset, so a processed event is proof positive). **Sign-out re-verified (2026-07-13):** the earlier "session doesn't invalidate" report was the browser-profile-specific corrupted cookie the prior session suspected, not a code bug — confirmed clean in a fresh browser: `signOut()` clears the session, `/patient` correctly bounces to `/login` afterward, no server errors. **Live click-through completed (2026-07-13)**, and it surfaced three real bugs, all now fixed: (1) `detachAddOn`/`changePlan`'s free-plan branch resolved server-side but the client never called `refetch()` on the `useAttachedAddOns`/`useCurrentSubscription` React Query hooks, so a detached add-on or a plan switch showed stale "Pending payment"/old-plan UI until a manual reload — fixed in `subscription-manager.tsx` by capturing `refetch` from both hooks and calling them after `handleDetach` and via a `useEffect` on `changeState`. (2) Bigger issue: `subscriptions`' UPDATE RLS policy only grants org staff, never the subscriber (asymmetric vs. `subscription_add_ons`, which does allow the subscriber) — so `cancelSubscription`'s and `changePlan`'s local-only cancel path (used whenever a subscription lacks a Paystack `provider_email_token`) silently no-op'd under the patient's own session while still returning a "Plan cancelled." success message, leaving the row `active` and the patient still billed. Fixed in `patient/subscription/actions.ts` by using `createServiceRoleClient()` for those specific writes (ownership already verified via `requireOwnedSubscription`'s RLS-scoped SELECT beforehand, per that file's own defense-in-depth comment). (3) Root cause of why `provider_email_token` was null in the first place: confirmed via real webhook payload timestamps that Paystack's `subscription.create` event (carrying the real `subscription_code`/`email_token` needed for `disableSubscription()`) can be processed a couple ms *before* `charge.success` flips the row from `trialing` to `active` — so the enrichment's candidate query (`.eq("status", "active")`) permanently missed it. Fixed in `supabase/functions/paystack-webhook/index.ts` (both the subscription and add-on correlation queries) to match `status IN ('trialing', 'active')` instead, and **redeployed** (now version 4, live). All three fixes verified end-to-end live in-browser against the real `patient.paystack.test@tarragonhealth.com` account: add-on attach → real Paystack transaction created (verified via Paystack's own `/transaction/verify` API) → remove → UI updates in place; plan switch (paid→free, free→paid) → UI updates in place, old row genuinely `cancelled` in DB (not just locally-displayed); `pnpm typecheck`/`lint` clean. **Still open:** full external Paystack hosted-checkout completion (entering a real test card) wasn't driven through the browser tool (cross-origin navigation to checkout.paystack.com gets aborted by the sandboxed preview browser) — checkout initialization itself is confirmed working (real Paystack transaction refs created, verified via their API), but no one has watched a card charge succeed and the `charge.success`→`active` transition happen live end-to-end in a real browser this session. Stripe (GBP/USD diaspora) — still not started. Test accounts (`admin+test@…`, `patient.paystack.test@…`) — still not cleaned up, left in place since they're still useful for further browser testing; `patient.paystack.test@…`'s password was reset this session (to enable this verification pass) — whoever tests next will need the new credential or another reset. **Resolved (2026-07-13): `/corporate` and `/hmo` marketing pages built** — the last two unbuilt items in `docs/MARKETING_SITE_SPEC.md` §7's Definition of Done (route collision was already resolved 2026-07-12). Both follow the spec's §3.5/§3.6 content (message, campaign line, HMO pull-quote, illustrative example-report stats explicitly labelled "not real client data") via a new shared `B2bPageTemplate` (`apps/web/src/app/(marketing)/_components/b2b-page-template.tsx`) and content file (`_content/b2b.ts`); each page's CTA routes to `/contact?source=corporate`/`hmo`. Wired into `MARKETING_ROUTES_BUILT`, the footer's new "Business" group, and the homepage's employer/HMO audience-tab CTAs (previously linked straight to Contact, now link to the dedicated pages first, per spec §3 point 7). Verified in-browser (desktop + mobile screenshots, no console errors); `pnpm typecheck`/`lint`/`build` all clean. **Next:** a Lighthouse pass and merge is all that's left per §7's DoD. Separately noted but out of scope for this change: the homepage FAQ copy (`_content/services.ts`, `HOMEPAGE_FAQS`) still says "you can log readings through WhatsApp or the app/web dashboard," which contradicts the WhatsApp non-negotiable business rule above (WhatsApp is notifications/support chat only, never a data-entry interface) — worth a follow-up fix.

## Definition of Done
- TypeScript: compiles, ESLint passes, tests pass, migrations committed
- Python: mypy passes, pytest passes, all Pydantic schemas typed
- Both: feature branch (never commit to main), `.env.example` updated for any new vars, works fully via app/web — WhatsApp/SMS notifications are additive, never required

## What Claude Must Never Do
- Never commit directly to `main`
- Never hardcode credentials
- Never bypass Supabase RLS, "just for this query"
- Never give the ML service direct database access
- Never skip Zod validation (TS) or Pydantic schemas (Python)
- Never design a patient-facing feature that requires a WhatsApp send to succeed, or that only works via WhatsApp — app/web is the interface for every core action; WhatsApp/SMS is notifications plus human doctor↔patient support chat, not a transactional interface
- Never build a WhatsApp-initiated signup, onboarding, or account-creation flow, and never build automation (bots/intent parsing) that turns an inbound WhatsApp message into a platform action — signup and core actions are app/web only; inbound WhatsApp only ever routes to a human clinician inbox
- Never deprioritise or silently swallow an abnormal screening result event
- Never invent a standalone sub-brand name for an internal product (see `docs/BRAND_GUIDE.md` §7)
- Never render a UI element claiming a doctor reviewed a specific case without a corresponding `reviewed_by`/`reviewed_at` record — the "Reviewed by Dr. X" pattern must be a single shared component that is null-gated, never a hardcoded string (see `docs/CLINICAL_TRUST_MODEL_SPEC.md` §2, §9)
- Never make a doctor the default face of the day-to-day patient relationship — that's the clinician's role; doctor attribution is earned per-case through real escalation review, never applied as a uniform branding layer (`docs/CLINICAL_TRUST_MODEL_SPEC.md` §1, §9)

## Where to Look
- System architecture, topology, RLS model, event pipelines, infra → `docs/ARCHITECTURE.md`
- Business model, pricing, full DB schema, 7-sprint plan, clinical protocols, launch gates → `docs/FEATURE_SPEC.md`
- Brand voice, tagline system, colour/type tokens, dashboard copy patterns → `docs/BRAND_GUIDE.md`
- Public marketing site — sitemap, page copy, design tokens, hostname routing, DoD → `docs/MARKETING_SITE_SPEC.md`
- Logo assets → `/brand/Tarragon_Health_Logo_Mark.png`, `/brand/Tarragon_Health_Logo_Lockup.png` (marketing deploy copies → `apps/web/public/brand/`)
- Competitive-intelligence feature roadmap (Health Score, wearables, symptom tracking, fee-at-risk contracts, phased Now/Phase 2/Phase 3) → `docs/FULL_SPECIFICATION_V4.md` — additive feature layer only; per its own §11 guardrail it informs the roadmap and never overrides "Current Sprint" above
- Clinician/doctor role architecture, per-touchpoint attribution rules, escalation→doctor review flow, `clinical_staff`/`care_team_assignment`/`protocol_versions` schema, MDCN/NMCN compliance → `docs/CLINICAL_TRUST_MODEL_SPEC.md` — authoritative on conflicts touching clinician attribution or escalation branding; its Stage/§ cross-references map to the original `docs/source/` planning docs, not `FEATURE_SPEC.md`'s Sprint numbers — see the reconciliation note at the top of the file
