# Tarragon Health — Claude Code Master Instructions (v3)

> Read every session. Full business detail: `docs/FEATURE_SPEC.md`. Full brand/voice/UI: `docs/BRAND_GUIDE.md`. Marketing site: `docs/MARKETING_SITE_SPEC.md`. Competitive-intelligence feature roadmap: `docs/FULL_SPECIFICATION_V4.md`. Master operating plan (business model, **5-tier doctor ladder**, phased Phase 1/2/3 roadmap): `docs/Tarragon_Health_Master_Operating_Plan_v4.md` — authoritative on the clinical staffing model, supersedes the flat clinician/escalation-doctor language elsewhere. Clinician attribution & trust model: `docs/CLINICAL_TRUST_MODEL_SPEC.md` — still authoritative for per-touchpoint attribution UI rules (e.g. `ReviewedByDoctor`) not covered by the tier ladder. This file is the operating contract — keep it under 300 lines (raised from 200 on 2026-07-15 to let "Current Sprint" read as a scannable dated changelog instead of one dense paragraph), update "Current Sprint" every sprint.

## The Business
Nigeria's digital-first chronic disease, preventive health, and family care coordination OS — the trusted coordination layer between patients, families, doctors, labs, pharmacies, HMOs, and employers. App/web-first, doctor-led (Tarragon directly employs its day-to-day care-team doctors, per `docs/CLINICAL_TRUST_MODEL_SPEC.md`), escalation-driven, AI-automated, partner-network based, with WhatsApp/SMS as a follow-up and notification layer only (see Non-Negotiable Business Rules). **No owned clinics.** Five categories, all architecturally represented from Sprint 1 — they are commercially linked, each feeds the others:

1. **Chronic Disease Management** *(core wedge)* — hypertension, diabetes; expansion: asthma, CKD, heart failure
2. **Preventative Medicine** — cancer/metabolic/infectious/reproductive screening. **Abnormal result → Category 1 upgrade is the highest-priority business event in the platform — never lose it, never let it fail silently.**
3. **Care Coordination** — lab network, pharmacy network, specialist referrals, hospital handoffs
4. **B2B & Institutional** — corporate wellness, HMO capitation, NHIA/government programmes
5. **Platform Infrastructure** *(backbone, not a product line)* — WhatsApp/SMS notification engine (reminders, alerts, confirmations — never signup or a feature's only interface), doctor-led delivery, AI clinical decisioning, longitudinal patient record, partner API layer, analytics

Prevention and chronic management **share the same patient record** — design every table and dashboard for dual-state.

## Architecture: Two-Layer (Stack A — Final, Do Not Relitigate)

### Primary Platform — TypeScript
- Web: Next.js 16, TypeScript, Tailwind, shadcn/ui (`apps/web`) — this Next.js has breaking changes vs. training data; read `node_modules/next/dist/docs/` before writing framework code
- **Marketing site:** public pages live in `apps/web/src/app/(marketing)/` as a route group inside the same Next.js app — not a separate package yet. `middleware.ts` routes by hostname: root domain → marketing, `app.` subdomain → platform. Full spec, page copy, and design direction in `docs/MARKETING_SITE_SPEC.md` — read it before building any marketing page. Split into `apps/marketing` only when marketing needs its own CMS/team/deploy velocity — not yet. Marketing pages must not import platform/auth modules; Contact/Join is the only page that writes to Supabase (`leads` table).
- Mobile: React Native Expo (`apps/mobile`)
- DB/Auth/Storage/Realtime: Supabase Postgres, **eu-west-1** region (Supabase has no Africa region; closest available to Nigeria — NDPR residency gap accepted for now), pgvector
- Cache/queues: Upstash Redis
- AI workflows: LangGraph.js + Claude API
- Comms: WhatsApp Cloud API + Termii SMS (fallback) — **follow-up/notification channel only** (reminders, alerts, confirmations); never required for signup or for any feature to function — see Non-Negotiable Business Rules
- Payments: Paystack (NGN), Stripe (GBP/USD diaspora)
- Hosting: Vercel (web + Edge Functions), Railway (persistent compute/background jobs), Cloudflare (DNS/edge)

### ML Microservice — Python (`services/ml/`)
- FastAPI 0.115+, Python 3.12, package manager **uv only** (never bare pip)
- scikit-learn, pandas, numpy, scipy; Pydantic v2 everywhere; pytest + httpx
- **Stateless. No database access. No file writes.** Patient data arrives in the request body — never pulled by the service.

### Service Communication Rules
- TypeScript → ML service over HTTP only (`ML_SERVICE_URL`), auth header `X-Service-Key` (`ML_SERVICE_KEY`)
- 5-second timeout, graceful fallback — **the platform must keep working if ML is down**
- `packages/shared/ml-client.ts`: typed client, never throws, returns `null` on error

## Non-Negotiable Business Rules
- All NGN amounts stored in **kobo** (smallest unit). Diaspora billing: GBP primary, USD secondary, via Stripe.
- **Superseded 2026-07-11 — WhatsApp is not a required interface for signup or core platform actions.** Signup, onboarding, and every core patient/clinician transaction (vitals/meds/screening/booking logging, dose tracking, etc.) happen via app or web only — no bot-driven data entry over WhatsApp, ever, and no feature may be built to depend on a WhatsApp send succeeding. WhatsApp/SMS (Termii fallback) still carries reminders/alerts/confirmations, **and patients may message their doctor on WhatsApp for support, with the doctor replying on WhatsApp too** — that two-way channel is human-routed (a clinician inbox), never parsed by automation into a platform action.
- Phone numbers always E.164 (`+234XXXXXXXXX`). Timezone always `Africa/Lagos`.
- Every table has `organisation_id` — always filter by it. **RLS enforced at the Postgres level for every multi-tenant table — never bypass, never filter in application code instead.**
- Doctor:patient ratio target — **1:120** for Tier 1–3 staffing (see Clinical Tier Ladder below).
- Abnormal screening result handling (Cat 2→1 upgrade): Supabase trigger → Edge Function → doctor WhatsApp alert **immediate, not scheduled** → doctor has a 4-hour contact SLA → surfaces as Priority 1 (red) on doctor dashboard.

## Clinical Tier Ladder (supersedes flat clinician/escalation-doctor model — 2026-07-15)
Full detail: `docs/Tarragon_Health_Master_Operating_Plan_v4.md` §4/§7/§8. Every clinical judgment is made by a doctor; no case is closed by non-clinical staff; a case climbs only as far as its complexity requires — most stay at Tier 1/2.
- **Care Coordinator** (employed, non-clinical) — logistics only: check-ins, adherence/missed-reading tracking, lab/refill booking. Never interprets a result, adjusts medication, or closes an escalation — routes anything needing judgment to Tier 1.
- **Tier 1** Medical Officer <3yrs (employed) — first-line review of routine/stable readings under protocol; confirms/continues existing stable prescriptions; no new prescribing.
- **Tier 2** Medical Officer 3+yrs (employed) — initiates new medications, standard dose adjustment, handles Tier 1 escalations.
- **Tier 3** Senior Medical Officer (employed) — complex/multi-drug case management, standing QA/spot-audit of Tiers 1–2.
- **Tier 4** Senior Registrar (contracted, part-time retainer) — pre-referral consult, sets referral urgency, approves referrals, owns/updates clinical protocols, supervises Tiers 1–3.
- **Tier 5** Partner Specialist (contracted, referral-only, per-consult) — complex/procedural input; hands routine follow-up back to Tier 3/4 (shared care).

**Schema (layered model):** `clinical_staff.doctor_tier` (built 2026-07-15) carries clinical seniority/routing authority. `clinical_staff.role` (`clinical_director`/`clinician`/`escalation_doctor`) was retired 2026-07-15 (`20260715174500_retire_clinical_staff_role.sql`, see Current Sprint) — replaced by `doctor_tier` plus `clinical_staff.is_clinical_director`. **`is_clinical_director` is a separate org-governance flag (protocol signing, staff verification) orthogonal to tier, not itself a rung on the ladder** — a person can hold Clinical Director status at any tier. `profiles.user_role` (the account/login/RLS role) already had a real, built two-way split before this ladder work: `clinician` (frontline, `/clinician/*` dashboard) vs. `doctor` (escalation-review-only, `/doctor/*` dashboard — `20260709001520_add_doctor_role.sql`) — this maps cleanly onto the ladder without inventing new auth roles: **Tier 1–3 clinical_staff records link to a `clinician`-role profile; Tier 4 (and Tier 5, if given a login at all — referral-only contractors may have none, same as a bio-only Clinical Director) link to a `doctor`-role profile.** Only `care_coordinator` was a genuinely new account value, added this pass. Fine-grained tier detail still lives on `clinical_staff`, not as further auth-enum splitting — Tiers 1–3 share one account role, Tier 4/5 share another.

**Indemnity:** DB-enforced requirement for Clinical Director, Tier 4, and Tier 5 before activation. Tiers 1–3 are employed and covered under Tarragon's institutional policy, not tracked individually.

**Care Coordinator write access:** gets the same org-staff read access as any staff account, but must never gain write access to medications, escalation resolution, or protocol signing — enforced at the app/server-action layer (matches the existing "only Clinical Director can sign protocols" pattern), not a new RLS helper.

**Explicitly Phase 2/3, not initial launch** (confirmed 2026-07-15 — do not build functional code for these without an explicit ask): full specialist-matching engine + 8-stage referral-status pipeline, patient-initiated wellness testing catalogue. **Employer/HMO risk-stratification dashboards, Premium ParentCare as a real subscription tier, and home sample collection/medication delivery logistics were pulled forward and built 2026-07-16 on explicit ask** — see Current Sprint — so they're no longer on this list; everything else here still requires an explicit ask before functional code is written.

MDCN/regulatory confirmation that this tier authority split (e.g. Tier 1 confirming refills, Tier 2 initiating new medications) is compliant is an open founder item (master plan §16) — never represent the tier ladder as regulator-approved.

## Device & Wearable Integration
Bluetooth clinical devices (BP cuffs, glucometers) are **built** (2026-07-13/14, see Current Sprint). Consumer wearable cloud sync (Apple Health, Google Fit/Health Connect, Oura, WHOOP, Garmin, Fitbit) is **schema-scaffolded only (2026-07-14)** — `wearable_connections`/`wearable_readings` tables + RLS exist (migration `20260714140000_wearable_connections.sql`) and `apps/web/src/lib/wearables/oauth-providers.ts` has a tested, gracefully-degrading OAuth-URL builder for the 4 cloud-OAuth providers (Oura/WHOOP/Garmin/Fitbit) — but there is **no patient-facing "Connect" UI and no webhook ingestion route yet**, deliberately: none of the 5 providers' real developer apps/credentials exist, so a connect button would click through to nothing. Full spec: `docs/FULL_SPECIFICATION_V4.md` §5/§9 (`app/routers/wearables.py` — still unbuilt). Contract to follow when the consumer-wearable path is finished:
- **Ingestion boundary, not owned hardware** — TarragonHealth never talks to device firmware directly. Two ingestion paths only: (1) consumer platform sync via their cloud APIs/webhooks (schema scaffolded, OAuth/webhook flow not yet built); (2) clinical Bluetooth devices paired via the Expo mobile app's native BLE (`apps/mobile`), which uploads parsed readings to `POST /api/mobile/device-readings` — built, per below.
- **Apple Health has no cloud OAuth API at all** — HealthKit data is device-local; syncing it needs the Expo mobile app's own HealthKit bridge (same shape as the BLE clinical-device pairing, not a server-side OAuth redirect), so it's excluded from `oauth-providers.ts`'s `CloudOAuthWearableProvider` type even though `wearable_provider` (the DB enum) includes it for schema completeness.
- **`wearable_readings` is a genuinely separate table from `vitals_readings`, not a "no dual source of truth" violation** — passive wearable metrics (steps, sleep stages, HRV, recovery/strain) have no `vitals_readings.vital_type` equivalent at all. Any wearable metric that *does* overlap an existing vital_type (heart rate → pulse, weight, SpO2) should go to `vitals_readings` with `source='wearable'` instead (that enum value already exists) — the additive-faster-path/no-parallel-table rule below still applies to those overlapping metrics.
- **App/web manual entry is never removed.** Device sync is an additive faster path into the same `vitals_readings` table patients already log into manually via app/web — same downstream escalation logic, same `patient_risk_scores`, same abnormal-result pipeline. No dual source-of-truth for anything that has a `vitals_readings` equivalent — `vitals_readings.source` (`manual`/`device`/`wearable`) distinguishes them, not a parallel table. WhatsApp/SMS may remind a patient to log a reading; it is not itself an entry interface.
- Every reading gets `organisation_id` + RLS like any other table.
- Diaspora/premium tier only for the consumer wearable ecosystem (Apple Health/Oura/WHOOP/Garmin/Fitbit, not yet built) — Bluetooth clinical devices (BP cuff/glucometer/scale) are core-tier, sold as device bundles per `docs/FEATURE_SPEC.md`.
- **Weight Scale (0x2A9D) gap CLOSED 2026-07-21** — full BLE support now covers all five standard GATT clinical profiles: BP cuff (0x2A35), glucometer (0x2A18), weight scale (0x2A9D, spec-fixed 0.005 kg/0.01 lb resolutions, imperial converted to kg, 0xFFFF measurement-unsuccessful rejected), thermometer (0x2A1C, 32-bit medical FLOAT, °F converted to °C), and pulse oximeter (PLX Spot-Check 0x2A5E). `patient_device_type` gained `thermometer`/`pulse_oximeter` (migration `20260721141233`); readings land in the existing `vitals_readings` columns (`weight_kg`/`temperature_c`/`spo2_pct`) via the same device-readings API.

## TypeScript Code Rules
- Strict mode always. No `any`. Ever. pnpm only.
- All DB queries via Supabase client with RLS active. All API routes validate input with Zod. React Query for all data fetching.
- kebab-case files, PascalCase components. `NEXT_PUBLIC_` prefix only for client-safe env vars. Jest tests for every service function.

## Python Code Rules
- Python 3.12 + uv only. Pydantic v2 schemas for every endpoint — no untyped dicts. Type hints on all params/returns.
- `async def` for all endpoints. Models loaded once at startup (lifespan), never per request. pytest for every model function.
- Never import from `apps/` or `packages/` — the ML service is fully standalone.

## Key Partners & Market References (use real names in seed data / demos)
- HMOs: Reliance, Avon, Ronsberger, Wellahealth. Labs: Synlab Nigeria, Cerba Lancet, Healthtracka, Afriglobal Medicare.
- Comparable platforms referenced for positioning: Helium Health, Wellahealth.

## Brand (see `docs/BRAND_GUIDE.md` for full system)
- Master tagline: **"Care that stays with you."** Wordmark: **TarragonHealth** (camel-case). Mark: **Guard Leaf** (shield + sprout crown + checkmark vein).
- Tarragon Green `#0E7C52` (brand/primary actions), Clinical Navy `#12324B` (B2B/clinical documents). Clinical dashboard status colours (green/amber/red/blue/grey) are a **separate system** from brand colour — never confuse the two.
- Voice: warm and personal, not a hospital PA system. No fear-based urgency, no "WARNING:", no clinical jargon in patient-facing copy.
- **Do not use "doctor-led" as a headline/marketing claim** (retired 2026-07-18 — it reads as over-promising and unprofessional). Describe the actual clinical process instead — "clinical review and escalation", "your care team", "reviewed against care protocols" — and reference doctors concretely in process/explanation ("escalates to a doctor", "the doctors who keep the 1:120 ratio"), never as a blanket brand adjective. The operating model is unchanged (Tarragon still employs its own doctors, per `docs/CLINICAL_TRUST_MODEL_SPEC.md`); this is a public-copy/positioning rule only.

## Current Sprint (UPDATE THIS EVERY SPRINT)

**Status:** Sprint 4 (Python ML Microservice) is paused (2026-07-09) — do not resume without an explicit ask. All active work since then is TypeScript, logged chronologically below.

### Sprint 4 — Python ML Microservice (paused 2026-07-09)
Goal: build `services/ml` into the SCORE2 CVD/HbA1c-trajectory/BP-control/lab-interpretation/cohort-analytics service, wire it into TypeScript via `packages/shared/ml-client.ts`, deploy it — `docs/FEATURE_SPEC.md` §4 (weeks 7–9).

State at pause (confirmed live 2026-07-12): all 6 endpoints typed and never-throw in `ml-client.ts`; wired into BP-control assessment on every BP vitals log, a clinician lab/screening-result form (CVD risk + HbA1c trajectory + `patient_risk_scores` writes), and the corporate dashboard's cohort analytics (org-scoped, no PII sent). Railway deploy confirmed live (`/health`, `/docs` both 200). `patient_risk_scores` has 2 real `bp_control` rows proving the full path (Vercel → Railway → Supabase) worked at least twice — though both predate the "Fix Railway build" PR by a day or two, so if in doubt, log a fresh BP reading and check for a newer row. Sentry wired behind optional `SENTRY_DSN` (no-op if unset); no Sentry project created yet (needs the user's cloud credentials).

### 2026-07-11 — Marketing site + platform convergence (PR #15, merged to main-dev)
- Full marketing site: homepage, 4 priority programmes, pricing, contact/leads, `/medication`, `/labs`
- **AbnormalResultHandler** Edge Function (`supabase/functions/abnormal-result-handler`) — previously missing, now deployed and verified live
- Abnormal-screening E2E test (`apps/web/e2e/`, opt-in via `pnpm test:e2e`)
- WhatsApp policy change codified: app/web is the sole interface for signup/core actions; WhatsApp/SMS is notifications + human doctor↔patient chat only (see Non-Negotiable Business Rules, `docs/ARCHITECTURE.md` §1.3/§8)
- `docs/FULL_SPECIFICATION_V4.md` roadmap doc added
- Staging: Vercel auto-deployed the merge commit (`677735d`) to Preview; build passed but the URL sits behind Vercel's deployment-protection/SSO gate, never independently browser-verified
- CI: TypeScript green; Python ML failed on a pre-existing, unrelated mypy error (confirmed already broken on main-dev before this merge)

### 2026-07-12 — Reconciliation + clinical trust model foundation
**Reconciliation:**
- Local migration filenames now match remote's applied history exactly (30 renamed, committed `df955dd`); `20260711000000_leads.sql` had never actually run — the marketing Contact page's lead capture was silently failing — now live
- Railway's `enchanting-playfulness` service (misleadingly labelled `@tarragon/web`) was confirmed to have always been correctly scoped to `services/ml` — renamed to `@ml-service` for clarity; confirmed genuinely live via `/health`/`/docs`
- `private.handle_new_user()` trigger bugs (missing `+` on phone, dropped role/org on delayed `app_metadata` update) were already fixed by an existing migration — confirmed deployed function matches migration byte-for-byte
- `/corporate`/`/hmo` route collision resolved: platform dashboards moved to `/dashboard/corporate`/`/dashboard/hmo`; bare paths now 404 cleanly and are free for marketing to use
- Pre-existing Python mypy failure fixed (`test_score2.py`'s `dict(...)` + `**kwargs` spread was untyped enough to fail `score2_risk`'s keyword-only params) — inlined the call args instead; `uv run mypy .` clean
- `/about` page built (hero, continuity-thesis pillars, CTA band) — **founder name/photo/bio are still bracketed placeholders**, don't announce this page publicly until a human supplies a real bio
- WhatsApp support-inbox webhook (`supabase/functions/whatsapp-webhook`, human-routed only) and a Termii sender-ID rename (`TarragonHlth` → `Tarragon`) committed

**Clinical trust model foundation** (per `docs/CLINICAL_TRUST_MODEL_SPEC.md` §7 build sequence):
- `clinical_staff` table + `escalations.reviewed_by`/`reviewed_at` — shared null-gated `ReviewedByDoctor` component, set once at resolve time, never retroactively
- `care_team_assignment` (one row per patient) + clinician-side assignment form + patient-facing `YourCareTeam` card, same null-gating
- `protocol_versions` append-only ledger — only the org's active Clinical Director can sign a new protocol version; no update/delete grant at all
- MDCN/NMCN credential verification: `clinical_staff.verified_by` + two DB CHECK constraints (`clinical_staff_active_requires_verification`, `clinical_staff_no_self_verification`) — first UI to actually create `clinical_staff` records
- `PatientEscalations` component — patient-friendly view of the caller's own escalations, composes `ReviewedByDoctor`
- Onboarding wizard (`profiles.onboarding_completed_at` gate, `/onboarding`) — verified live end-to-end
- `send-support-reply` Edge Function (deployed, signs replies with the real clinician name, fails closed if `WHATSAPP_TOKEN` unset) + `/clinician/support-inbox` UI — verified live including the fail-closed path
- Health Passport shipped separately in PR #23 (2026-07-14): `/patient/health-passport`, PDF export via `@react-pdf/renderer` — re-confirmed working 2026-07-15
- Every item from spec §7 done except ops-only items (annual re-verification, indemnity tracking — see 2026-07-13 below)

### 2026-07-12/13 — Patient subscriptions, Paystack payments, add-ons (Sprint 6, NGN only — Stripe/GBP is the entry below)
- `add_ons`/`subscription_add_ons` (each add-on gets its own Paystack Plan+Subscription), `payment_transactions` (webhook-only write), price-lock triggers on `subscription_plans`/`add_ons` once a plan has a real subscriber (admin UI offers "clone as new plan" instead)
- `public.has_feature_access(feature)` RPC generalizes the old inline `has_ai_coach_access()` check (regression-verified)
- Seed data rewritten to match the live pricing page's real tiers (`free`/`essential`/`complete`/`family` + yearly) — old codes were stale and never matched `pricing.ts`; same fix applied to remote
- Paystack: server-only client, hosted-checkout redirect, deployed `paystack-webhook` (signature-verified/idempotent/never-throws)
- Entitlement gating (`RequiresEntitlement`/`UpgradePrompt`) on the 5 capabilities the Free tier excludes; family dashboard (`/patient/family`, `care-team-contact.tsx`) had to be built from scratch
- Full funnel: onboarding plan-selection step → Paystack redirect → webhook-driven activation; self-serve `/patient/subscription`; admin `/admin/settings/subscriptions`
- **2026-07-13, real Paystack test-mode keys supplied:** a genuine `charge.success` processed successfully, confirming `PAYSTACK_WEBHOOK_SECRET` is correctly set
- **Live click-through (2026-07-13) surfaced and fixed 3 real bugs:**
  1. Detach/plan-change didn't call `refetch()` on the relevant React Query hooks — stale UI until manual reload
  2. `subscriptions`' UPDATE RLS only granted org staff, never the subscriber (asymmetric vs. `subscription_add_ons`) — the local-only cancel path silently no-op'd under a patient's own session while still claiming success, leaving them billed. Fixed via `createServiceRoleClient()` for those specific writes (ownership already verified via RLS-scoped SELECT beforehand)
  3. Paystack's `subscription.create` webhook event can arrive before `charge.success` flips the row to `active`, so the enrichment query missed it — fixed by matching `status IN ('trialing','active')`; `paystack-webhook` redeployed (v4)
- **Still open:** a real card charge through Paystack's hosted checkout has never been driven end-to-end in-browser — the sandboxed preview browser can't follow the cross-origin redirect to `checkout.paystack.com` (`ERR_ABORTED`/403 on OPTIONS). Checkout *initialization* is confirmed working (real transaction refs created, verified via Paystack's `/transaction/verify` API) — this exact gap recurred and was re-confirmed 2026-07-15 (see this session's verification pass below)

### 2026-07-13 — Stripe (GBP/USD diaspora)
- Mirrors the Paystack architecture: hosted-checkout redirect, webhook-driven activation, same `subscriptions`/`subscription_add_ons`/`payment_transactions` tables
- `apps/web/src/lib/stripe/` wraps the official SDK to preserve Paystack's never-throw `{ok,data|error}` contract; deployed `stripe-webhook` handles `checkout.session.completed`, subscription created/updated/deleted, invoice succeeded/failed
- `resolveProvider(currency)` (NGN→Paystack, else→Stripe) + a `canDisableRemotely()` gate fixing a real bug: a naive Paystack-shaped check would never cancel a live Stripe subscription, since Stripe rows never populate `provider_email_token`
- Admin UI gained a currency selector; patient/onboarding UI gained `CurrencyTabs`; 18 diaspora rows seeded (round-number pricing, not conversion), `is_active=false` until synced to a real Stripe Price
- **Verified:** typecheck/lint clean; all 18 rows render correctly in-browser; "Sync to Stripe" degrades gracefully without keys
- **Not yet verified:** `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET` still empty — no live Stripe Checkout round-trip has happened yet

### 2026-07-13 — Marketing site completion, Lighthouse, clinical_staff ops items, indemnity exemptions
- `/corporate` and `/hmo` marketing pages built via a shared `B2bPageTemplate` — the last unbuilt items in `docs/MARKETING_SITE_SPEC.md` §7's DoD
- Homepage FAQ copy fixed (previously implied WhatsApp could log readings/sign up — now correctly states app/web only)
- Full Lighthouse pass across all 12 marketing pages found and fixed 2 real site-wide issues: low-contrast eyebrow-label text (`text-brand-green` → `text-deep-forest`, several components including a duplicate in `story-panel.tsx` missed on the first grep-only pass) and a missing caption in the homepage walkthrough button's `aria-label` (WCAG 2.5.3). All 12 pages now 100/100/100 a11y/best-practices/SEO, 91–99 performance
- **Merged to `main-dev` as PR #17** (`a4823f6`), all CI green
- Annual license re-verification + indemnity/malpractice insurance tracking built — the last ops-only items from `docs/CLINICAL_TRUST_MODEL_SPEC.md` §7: `indemnity_insurer`/`indemnity_policy_number`/`indemnity_expires_at` + a CHECK constraint blocking activation of a Director/Escalation Doctor without current cover (a write-time gate, not continuous enforcement — ops still needs to act on expiry badges). **Merged as PR #18** (`d89796c`)
- Indemnity-requirement exemptions built per explicit user request (individual/role-wide/org-wide, audited not silent) via a BEFORE INSERT/UPDATE trigger replacing the CHECK constraint; scope isolation verified (a role-wide exemption doesn't leak to other roles). **Merged as PR #19** (`d0a7148`)
- Found (and later resolved by the Stripe work above): two applied-but-uncommitted migrations discovered on remote, meaning someone had started Stripe/diaspora work directly against the DB without committing — same class of drift as the 2026-07-12 leads.sql issue

### 2026-07-13/14 — Bluetooth clinical device integration (branch `claude/tarragon-medical-device-integration-cn8e82`)
- `patient_devices` pairing table + `vitals_readings.source`/`device_id`/`external_reading_id` (idempotent dedupe, no parallel table per the ingestion-boundary rule)
- Real Bluetooth SIG GATT parsers in `packages/shared` (SFLOAT decoder, BP Measurement 0x2A35, Glucose Measurement 0x2A18), 14 tests passing
- `POST /api/mobile/device-readings`: bearer-auth ingestion, dedupes on `23505`, reuses `assessBpControlBestEffort` (extracted + refactored to accept the caller's own Supabase client — fixed a real latent bug where the bearer-token path would've silently no-op'd under RLS)
- New `apps/mobile` Expo scaffold (BLE pairing/sync screens) — typechecks clean, **never run on a simulator/device, no functional verification**
- Weight Scale (0x2A9D) parsing is a known, deliberate scope gap — sync screen shows "not supported yet"
- Adding `apps/mobile` to the workspace broke `pnpm test` (jest-environment-node/jest-mock mismatch) and silently corrupted `apps/web`'s typecheck (`@types/react@18` vs `19` clash) — fixed via `pnpm.overrides` in `pnpm-workspace.yaml`

### 2026-07-15 — Doctor tier ladder, migration reconciliation, Care Coordination merge
- **Tier ladder schema (branch `claude/doctor-tier-ladder-clinical-model`):** `clinical_staff.doctor_tier` (backfilled from the old role column), `profiles.user_role` gained `care_coordinator` (Tier 1-3 map to the existing `clinician` account role, Tier 4/5 to the existing `doctor` role — only `care_coordinator` was genuinely new), `clinician_alerts.escalation_level`, `lab_orders.investigation_tier`. Indemnity trigger extended to cover Tier 4/5 directly
- **`clinical_staff.role` retirement:** dropped the `role` column and its enum type entirely (first DROP COLUMN/DROP TYPE in this project's history) in favor of `is_clinical_director` (orthogonal governance flag, not a tier rung) + `doctor_tier`; all call sites (`useOrgClinicians`, care-team/protocol/health-passport director lookups, admin UI) rewritten; verified via a rolled-back transaction test covering every scope-isolation case
- **Migration reconciliation:** found **13** migrations live on remote with no local file — all from two other open PRs sharing the same DB, [PR #28](https://github.com/Kolalonge-creator/Tarragon-Health/pull/28) (Care Coordination, 8 files) and [PR #36](https://github.com/Kolalonge-creator/Tarragon-Health/pull/36) (Care Nav/employer enrollment, 5 files, 3 needed renaming to their real applied timestamps). Pulled and verified all 13 against live `information_schema`/`pg_proc` before committing — local history now matches remote exactly (83/83)
- **Pharmacy-authority-by-tier:** `private.has_prescribing_authority(org)` (structural DB gate, mirrors the indemnity pattern) requires Tier 2+ or Clinical Director; `medications` RLS now enforces it for org-staff writes (patient self-add untouched). Clinician UI shows a friendly explanation instead of a raw RLS error for Tier 1
- **Tier 1-4 Doctor Dashboard UI:** `/clinician` and `/doctor` now show the caller's real tier label + authority blurb (role-gated views of one worklist, per master plan §12 — not five separate dashboards), falling back to a generic label only when the caller has no `clinical_staff` row
- **PR #28 (Care Coordination) reconciled and merged:** one-off Paystack/Stripe charge path for bookings; human-readable `patient_number`/`order_number`/`referral_number` IDs; `/clinician/referrals` specialist-referral worklist + patient-facing `YourReferrals` card; lab catalogue booking UI (`LabCatalogue`/`LabOrdersList`/`LabResults`) plus 6 new single-test bundles fixing a gap where hiv/hep_b tests existed but were unbookable; pharmacy catalogue booking UI; commission dashboard (`/admin/settings/commissions`) driven by a DB trigger on `payment_confirmed` across all 4 order types
- **Clinician-originated-orders guardrail:** `ordered_by` (→ `clinical_staff`) on `lab_orders`/`pharmacy_orders` + BEFORE INSERT triggers rejecting patient-initiated rows unless tied to a due `screening_schedule` (labs) or an active clinician-sourced medication (pharmacy refills)
- **PR #28 and PR #36 both merged to `main-dev`** (confirmed via `git log`)
- **In-browser verification pass (same day, post-merge):** clicked through all 4 merged flows as real test accounts — clinician lab ordering (real `lab_orders` row, `ordered_by` correctly resolved), patient checkout initiation (real Paystack transaction ref created; the cross-origin redirect to `checkout.paystack.com` is blocked by the sandboxed preview browser, same known gap as the 2026-07-13 Paystack entry above — not a regression), pharmacy ordering correctly blocked for a patient with no clinician-prescribed meds, admin commission dashboard (renders cleanly, correctly empty), employer roster manager, and the Care Navigation booking-request flow (real `booking_requests` row, correctly org-scoped). No console errors, no unexpected failed requests — the 3-way merge reconciliation holds under real clicks, not just typecheck
- **Tier 1 confirm/continue-refill workflow built** (migration `20260715190000_medications_confirm_refill.sql`): `medications.last_confirmed_at`/`last_confirmed_by` (→ `clinical_staff`, server-derived — never client-supplied, can't be spoofed), a new `private.can_confirm_medication_refill(org)` (Tier 1 only) broadening `medications_update`'s RLS gate, and a BEFORE UPDATE trigger (`private.enforce_medication_confirm_only`) that restricts a non-prescribing caller to touching `refill_date` only on an existing `source='clinician'` row — drug/dose/frequency/active-status stay Tier 2+/Director acts, patient-sourced medications are rejected outright. UI: a "Confirm & continue" control on `MedicationsList` (shared with the patient's own view but only rendered when the clinician page passes `canConfirmRefill`, i.e. caller is Tier 1 and lacks prescribing authority), plus a null-gated "Confirmed by your care team · date" line. Verified with a 5-case rolled-back transaction test (refill-only succeeds; dose/is_active/patient-sourced-med all correctly blocked with `42501`; spoofed `last_confirmed_by` silently overridden by the trigger) and a real live click-through as the Tier 1 test account. `pnpm typecheck`/`lint`/`test` (189 tests) all clean.
- **Real card charges driven end-to-end in a real, non-sandboxed browser** (via the `claude-in-chrome` extension controlling the user's actual Chrome, not the sandboxed preview) — closes the gap flagged in the entry above. **Paystack:** a genuine `4084...4081` test charge on lab order LAB-000020 (₦9,000) → real `charge.success` webhook → `lab_orders.status='payment_confirmed'` → commission auto-recorded (₦1,800, Healthtracka, `pending`) — worked cleanly, Paystack's Edge Function secrets were already correctly configured. **Stripe:** synced `essential_usd` to a real Stripe Price via `/admin/settings/subscriptions` (`stripe_price_id`/`stripe_product_id` now set, `is_active=true`); a genuine `4242...4242` test charge produced a real, verified-via-API Stripe subscription — but found and fixed a real gap: the deployed `stripe-webhook` Edge Function reads `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET` from its own Deno secrets store, separate from `apps/web/.env.local` (which only configures the Next.js side), so all 6 webhook deliveries at checkout time silently no-op'd (`{ok:false,error:"not_configured"}`, still HTTP 200, so Stripe never auto-retried). Once the user set those two secrets directly on the Edge Function, replayed the missed events as properly HMAC-signed synthetic webhooks (built from real Stripe API data, POSTed only to our own endpoint — no Stripe domain touched) to confirm the fix: `subscriptions.status='active'`, real `provider_ref`, `current_period_end` correctly filled in. **Lesson for next time:** any Edge Function webhook integration needs its secrets set in *both* places — `.env.local` for local dev and the Supabase Edge Function secrets store for the deployed function — they do not share a source.
- **Next:** nothing outstanding from this pass. Broader project Next items unchanged — see Clinical Tier Ladder's Phase 2/3 list above (specialist-matching engine, wellness testing, Employer/HMO risk dashboards) — none of those are in scope without an explicit ask.

### 2026-07-16 — Home collection & delivery logistics (branch `claude/home-collection-delivery-logistics`, explicit ask — pulled forward from master plan §6/§8/§11/§13 Phase 2)
- New global partner catalogues `home_visit_providers`/`logistics_partners` (mirrors `lab_providers`/`pharmacy_partners`: no `organisation_id`, authenticated read, admin write), seeded with one inactive placeholder row each — no feature flag; the patient-facing "coming soon in your area" state is just what renders with zero active rows for a region, and flipping a real partner row to active is the only mechanism needed to turn the real UI on
- `lab_orders.home_visit_provider_id`/`home_visit_scheduled_at`/`courier_reference`; `pharmacy_orders.logistics_partner_id`/`delivery_address`/`estimated_delivery_at`/`courier_reference`/`delivery_confirmed_at`
- `commission_type` extended with `home_visit`/`delivery`; new triggers record a commission on the null→non-null transition of the provider/partner FK (a later, separate event from the existing payment-confirmed commission triggers)
- `set_pharmacy_order_delivery_address` RPC (security definer, ownership re-checked in-body) so a patient can set their own delivery address despite staff-only UPDATE RLS on `pharmacy_orders`
- New `/clinician/orders` staff worklist (manual state entry → matched provider/courier → schedule/assign; "mark delivered" action) and `/admin/settings/logistics-partners` partner management page
- Patient-facing `HomeCollectionAvailability`/`DeliveryAvailability`/`DeliveryAddressForm` wired into the existing lab/pharmacy order lists
- `pnpm typecheck`/`lint`/`test` (240 tests) all clean at merge
- **Not yet done:** no real home-visit or logistics partner has been contracted — both seed rows stay `is_active = false` until ops signs one, so the patient-facing feature is live in code but dormant in practice until then. State/region matching is manual text entry (no `profiles.state` column exists yet to read from instead, same gap as the existing specialist-referral assignment form)
- **Next:** none from this pass — this closes out the two items pulled forward from Phase 2. Remaining Phase 2/3 items (specialist-matching engine, wellness testing, Employer/HMO risk dashboards) are unchanged and still require an explicit ask.

### 2026-07-16 — Post-merge browser verification + 2 fixes (branch `claude/home-collection-followup-fixes`)
- **In-browser verification pass** of the above, as real clinician/admin/patient test-account sessions: `/clinician/orders` and `/admin/settings/logistics-partners` both render and function correctly, the admin "Activate" toggle genuinely turns the live UI on with no redeploy, the commission dashboard correctly picks up `home_visit`/`delivery` types, `HomeCollectionAvailability` correctly renders the "scheduled" state on a real order, and the patient/admin route guards hold. No console errors, no unexpected failed requests.
- **Found and fixed a real gap:** `useAssignHomeVisitProvider`/`useAssignLogisticsPartner` (`apps/web/src/lib/queries/logistics-partners.ts`) wrote `home_visit_provider_id`/`logistics_partner_id` straight through the Supabase client with nothing checking the referenced partner was `is_active` — only the *matching* query (dropdown population) filtered on it, which is a UI courtesy, not a guarantee. This is exactly how an inactive placeholder row ended up assigned to a real `lab_orders` row during the branch author's own testing, defeating the "activating a partner row is the entire mechanism" design the feature is built around. Fixed with `private.enforce_home_visit_provider_active()`/`private.enforce_logistics_partner_active()`, BEFORE INSERT OR UPDATE triggers on `lab_orders`/`pharmacy_orders` (migration `20260716150000_enforce_active_logistics_partners.sql`) — same structural-gate pattern as `enforce_medication_confirm_only`/`enforce_lab_order_origin`. Verified both directions with rolled-back transactions (inactive assignment correctly blocked with `23514`; active assignment still succeeds) directly against the live database before merge. The stale test assignment and its resulting phantom commission row were cleaned up.
- **Migration filename reconciliation:** the 5 files from the previous entry were committed locally as `20260716140000`–`20260716142000`, but had actually been applied to the remote database under different auto-generated timestamps (`20260715230120`–`20260715230156`) by whoever built the branch — same class of drift flagged in the 2026-07-12 and 2026-07-15 entries. Renamed (pure `git mv`, no content changes) to match remote exactly.
- `pnpm typecheck`/`lint`/`test` all clean.
- **Next:** nothing outstanding from this pass.

### 2026-07-16 — Location-based facility selection (branch `claude/medication-pathway-pharmacy-notifications`, explicit ask)
Patients can now choose *where* a lab test / vaccination / pharmacy collection / hospital visit happens, by **state → city → optional area** (+ "use my location"), pre-filled from a saved profile location. Decision: **unify on `public.facilities`** as the single location directory (per the `20260715162815` "one directory, not two" rule) — commissions/pricing stay keyed to the provider/partner tables, so a facility carries an optional `lab_provider_id`/`pharmacy_partner_id` link and a lab booking *derives* its commission-bearing `provider_id` from the chosen facility.
- **Schema (4 additive migrations `20260716160000`–`163000`, all idempotent `add column if not exists`):** `profiles.state/city/area`; `facilities.area` + `lab_provider_id`/`pharmacy_partner_id` links + type/location index; `lab_orders.facility_id` (the physical WHERE, alongside the derived provider_id — does not weaken `enforce_lab_order_origin`); `pharmacy_partners.state/city/area` + `specialist_providers.city/area` (partner-keyed catalogues get the same location model directly rather than a duplicate facility row). Seed adds 9 facilities (5 lab centres linked to real `lab_providers`, 2 vaccination centres, 2 hospitals across Lagos/Abuja) + backfills partner/specialist locations.
- **UI:** shared `FacilitySelector` (`patient/facility-selector.tsx`) + lifted `distanceKm` into `@/lib/geo` (deduped from `facility-directory.tsx` and `pharmacy-catalogue.tsx`). Wired into: lab booking on the screening calendar (facility picker replaces the flat provider dropdown; a facility with no `lab_provider_id` is shown but not bookable online); new `VaccinationBooking` card (facility picker → existing `booking_requests` path, additive to log-only `LogVaccinationForm`); pharmacy catalogue gains a state/city/area filter; facility directory gains the `area` filter; specialist-referral staff matching gains a city field + same-state/same-city-first sort. New `PatientLocationForm` + `updatePatientLocation` server action on both the patient dashboard and onboarding.
- **Reconciliation note:** the branch's own 4 pending pharmacy migrations (`120000`–`123000`: contact/location, `patient_allergies`, order notifications trigger, `fulfilment_method`) had **not** been applied to remote, so a first types regen from remote regressed them — applied all 4 to remote (idempotent-guarded) so remote matches the full branch, then regenerated `database.types.ts` cleanly.
- **Verified:** `pnpm typecheck`/`lint`/`test` (205) all clean; DB data-path confirmed via SQL (Lagos/Ikeja lab picker returns the linked provider; an unlinked test lab correctly reports non-bookable). **Not browser-verified** — another chat's `next dev` held the `.next` dev lock so this session couldn't start its own server; not killed.
- **Next:** browser click-through as a patient test account (set location → book a due screening at a facility → confirm a `lab_orders` row carries both `facility_id` and the derived `provider_id`) once a dev server is free; commit + open PR.

### 2026-07-16 — Medication pathway Phase 0/1: no-login pharmacy order notifications (same branch)
First slice of the 6-stage medication-management pathway (gap analysis vs. the pathway spec: 1 built / 5 partial / 6 not-built; this ships the highest-value gap — order fulfilment notifications — plus foundations). Phases 2–8 (prescription/care-plan linkage, medication history/timeline, specialist-added meds, adherence check-ins, missed-dose escalation ladder, medication-review engine, drug-class lab monitoring, pharmacist login surface) are scoped but **not built** — do not start without an explicit ask.
- **The feature:** when a pharmacy order hits `payment_confirmed`, a DB trigger (`private.enqueue_pharmacy_order_notifications`, same WHEN guard as the commission trigger) enqueues 4 `notifications` rows — **patient** WhatsApp+email (a showable confirmation carrying order number + patient ID) and **pharmacy** SMS+email (patient name/number + order number). This lets partner pharmacies fulfil an order **without ever logging into the dashboard** — the no-login path the founder asked for. It's a notification/confirmation layer only (never gates the order, never parses inbound), so it stays inside the WhatsApp-is-notifications-only rule. Verified end-to-end via a rolled-back live-DB transaction: a paid order enqueued all 4 rows with correct destinations/payloads.
- **Email channel:** `send-pending-notifications` Edge Function extended with a **Resend** `email` sender + two templates (`pharmacy_order_patient_confirmation`, `pharmacy_order_pharmacy_alert`) with branded HTML, plus payload-driven `to_email`/`to_phone` so non-profile recipients (pharmacies) can be reached. `email` was already a valid `notification_channel`, just never wired. New env: `RESEND_API_KEY`/`RESEND_FROM` (`.env.example` + **must also be set on the Edge Function secrets store**, same lesson as the 2026-07-15 Stripe webhook). Email SEND is not runtime-tested (no key/deploy yet); the queue content is verified. The patient WhatsApp template needs Meta approval — falls back to SMS meanwhile.
- **Schema (4 idempotent migrations `20260716120000`–`123000`):** `pharmacy_partners.contact_phone/contact_email/uses_platform_login` (+ redundant-with-facility-model lat/long/address, kept because already applied to remote — a follow-up may drop them); `patient_allergies` table (patient + org-staff RLS, foundation for the Phase 8 pharmacist surface); `pharmacy_orders.fulfilment_method` (pickup live / delivery gated until logistics partners onboard). All guarded (`if not exists`/`drop-then-create`) and confirmed re-appliable against remote.
- **Location reconciliation:** collided mid-build with the facility-selection workstream (state/city/area); per user decision the pharmacy "nearest" UI runs on the shared state/city/area + `facility-selector` model, with lat/long retained as an additive precise-distance sort (hybrid, not the clean drop originally chosen — flagged for optional later cleanup).
- **Drift caveat:** the 4 migrations are applied to remote but **not recorded in `supabase_migrations.schema_migrations`** (same pre-deploy state as the other ~11 unrecorded 2026-07-16 local migrations) — idempotency guards mean a `db push` re-run is now safe regardless.
- **Verified:** `pnpm typecheck`/`lint`/`test` (web 205 + shared 35) all clean; migration idempotency + trigger behaviour confirmed via rolled-back live SQL. **Not browser-verified** (another chat holds the dev-server lock).

### 2026-07-16 — Medication pathway Phases 2–8 (same branch, one batched PR)
Built the rest of the 6-stage pathway on top of Phase 0/1. Each phase = migration + RLS + UI + types + live rollback-SQL verification; migrations applied via `apply_migration` so they ARE recorded in `schema_migrations` (unlike the Phase 0/1 batch). `pnpm typecheck`/`lint`/`test` (web 205 + shared 35) green throughout. **Not browser-verified** (dev-server lock held by another chat). Email delivery still pending Resend setup (no API key/domain yet — founder to create key + verify sender + set `RESEND_API_KEY`/`RESEND_FROM` on the Edge Function secrets store).
- **Phase 2 — lifecycle:** `medications.stopped_at`/`stopped_reason` + Stop action + collapsible history; `medication_source` gains `specialist` + `prescriber_name`/`prescriber_document_url` (patient can log a specialist-started med with name + consult document); care-plan condition shown per drug.
- **Phase 6 — review engine:** `medication_review_cadences` (HTN 6mo, diabetes/CVD/CKD 3mo…) + `medication_reviews`; activating a care plan auto-schedules a review, completing one rolls the next; `reviewed_by`/`completed_at` stamped server-side (null-gated attribution); daily reminder cron + `/clinician/medication-reviews` worklist.
- **Phase 7 — drug-class lab monitoring:** `drug_monitoring_rules` (Metformin→renal 12mo, ACE-i→renal+K post-initiation, statin→LFTs, warfarin→INR) + `medication_lab_monitoring`; a clinician/specialist med auto-schedules the right monitoring (patient-added drugs get none); patient `LabMonitoringCard`.
- **Phase 4 — adherence check-ins:** `medication_adherence_checkins` auto-scheduled Day 3 / Week 2 / Month 1 / Month 3; daily reminder cron; patient answers **in-app** (`AdherenceCheckins` card) — WhatsApp/SMS only reminds.
- **Phase 5 — missed-dose escalation ladder:** `medication_adherence_alerts`; a `missed` dose log recomputes 30-day miss count and raises/upgrades an alert (≥3 → coach, ≥6 → doctor; upgrade-only; one active per med); ack/resolve stamped server-side; `/clinician/adherence` worklist.
- **Phase 3 — medicines cabinet:** medications list gains a summary (next review + next lab) + per-drug days-remaining-to-refill.
- **Phase 8a — dispensed record:** `pharmacy_order_dispenses` (drug/qty/date), patient can self-log what they collected (works for no-login pharmacies); no stock/inventory.
- **Phase 8b — pharmacist surface:** new `pharmacist` user_role + `profiles.pharmacy_partner_id`; a partner pharmacist logs in (`/pharmacist`) and sees, FOR AN ORDER ROUTED TO THEIR PHARMACY ONLY, the patient's allergy status + current meds, and records a dispense. **Cross-tenant PHI access is confined to four SECURITY DEFINER RPCs** (`pharmacist_orders`/`pharmacist_order_allergies`/`pharmacist_order_medications`/`pharmacist_record_dispense`), each scoped via `private.pharmacist_partner()` — NO broad RLS grants on profiles/allergies/medications. **Isolation proven via live SQL** (jwt-claims simulation): pharmacist A sees their order's patient's allergy+meds (1/1/1), pharmacist B and a non-pharmacist see nothing (0/0/0); write blocked cross-pharmacy too. If this surface is extended, keep isolation enforced in that one helper + re-run the isolation test.
- **New cron jobs live on remote:** `medication-review-reminders-daily`, `medication-checkin-reminders-daily`. New WhatsApp templates (`medication_review_due`, `medication_adherence_checkin`) need Meta approval — fall back to SMS meanwhile.

### 2026-07-16 — Employer/HMO risk-stratification dashboards (branch `claude/employer-hmo-risk-dashboards`)
Explicit founder ask (same session as the home-logistics pull-forward above) to build `/dashboard/hmo` to parity with the already-real `/dashboard/corporate` cohort-analytics pipeline.
- New `patient_care_gaps` view (`security_invoker` — verified not security-definer) deriving three gap shapes (overdue screenings, stale chronic monitoring, unactioned abnormal results) from existing RLS'd tables, no new mutable table. New `cohort_cost_model_constants` table (admin-editable, org-override-or-platform-default) backing a "modeled estimate" cost-avoided figure for HMO renewal conversations — every UI surface shows a persistent "modeled estimate, not a real claims feed" disclaimer.
- `/dashboard/hmo` rewritten to reuse `load-cohort-analytics.ts`/`RosterManager`/`OutcomeReportsPanel` unchanged (`RosterManager` gained an `entityLabel` prop for "member" vs "staff" copy), plus new `CareGapPanel`/`ClaimsImpactCard`. Per-member drill-down shown to org-staff (not new exposure — they already have direct RLS access to the underlying tables); only the aggregate cohort percentages stay anonymised.
- `/dashboard/corporate` gained age-band segmentation (computed client-side from `profiles.date_of_birth`, no ML service change) and a renewal-facing outcome-evidence card; `generateOutcomeReport` now freezes care-gap counts + the cost-avoided estimate into each report snapshot.
- **Live browser verification** (own dev server on a spare port + the local ML microservice started for this pass, real Chrome via claude-in-chrome): created a test `hmo_admin` account/org and reset `corp.verify.test@tarragonhealth.com`'s password. `/dashboard/hmo` empty-state renders cleanly (roster add works, "Generate report" shows a graceful error with no cohort data). `/dashboard/corporate` with the ML service up: real cohort analytics render, the new Age segmentation card correctly buckets the one real enrolled patient, the new Outcome evidence card renders with the required "modeled estimate" disclaimer, and generating two outcome reports back-to-back correctly triggers the compliance-trend card and freezes `careGaps`/`costAvoided` into each snapshot (confirmed via direct SQL).
- **Found and fixed a real bug live-testing this:** `generateOutcomeReport` (a server action) never invalidated `useOutcomeReports`' React Query cache, so "Report generated." showed while the list below still said "No reports generated yet." until a full reload. Fixed by exporting `reportsKey` and invalidating it once the action's message changes.
- `pnpm typecheck`/`lint`/`test` clean (205 web tests, 35 shared tests). Migrations applied and verified against the live Supabase project; `get_advisors` shows no new issues.
- **Not yet verified:** a populated HMO org with a real claimed member (only a "pending signup" roster row existed) — so `CareGapPanel`/`ClaimsImpactCard` are confirmed correct in the empty/error state but not yet seen rendering non-zero data.
- **Next:** the platform-default cost-avoided estimate (₦150,000/abnormal catch) is a placeholder an admin should replace with a real negotiated figure once available.

### 2026-07-16 — Premium ParentCare: standalone subscription tier (branch `claude/parentcare-subscription-tier`)
Same explicit founder ask.
- New `parentcare`/`parentcare_yearly`/`parentcare_gbp`/`parentcare_yearly_gbp`/`parentcare_usd`/`parentcare_yearly_usd` plan rows — up to 2 parents included, `dedicated_coordinator` shipped as a base-plan feature here specifically (add-on-gated everywhere else), reusing `care_team_assignment`/`YourCareTeam` unchanged. `extra-parentcare-member*` add-on, one row per currency/interval variant, matching the existing `extra-family-member-plus`/`-premium` precedent.
- Shared quarterly-PDF-report infrastructure (`patient_quarterly_reports` append-only archive, data assembler reusing `get-health-passport-data.ts`, PDF document reusing `HealthPassportDocument` with a title override, on-demand download route, Vercel Cron job) — closes a real gap where Family Premium's pricing copy already promised this and nothing implemented it.
- New `/patient/parentcare` page (reuses `FamilyMembersManager` verbatim — it was already plan-agnostic).
- **Found and fixed 5 real bugs in passing** (the first 4 zero-subscriber/zero-blast-radius, found before browser verification): `family_plus`/`family_premium`/`diaspora_premium_gbp` had an empty `features[]` array since `20260715142113`; `private.validate_family_plan_member_count()` only recognized the exact `extra-family-member` add-on code, never crediting Plus/Premium subscribers for their tier-specific add-on; the onboarding plan-selector's monthly/yearly grouping regex only matched a trailing `_yearly`, breaking GBP/USD toggles for every currency variant; `useAddFamilyPlanMember`'s plan lookup was hardcoded to the exact code `'family'`. The 5th, found live-syncing all 6 plans via `/admin/settings/subscriptions`: the "Sync to Paystack/Stripe" button never invalidated the plans cache after a successful sync — every sync had actually succeeded server-side (confirmed via direct DB checks) but the row kept showing "Inactive" until a full reload. Fixed.
- **Live verification (real Chrome via claude-in-chrome):** synced all 6 new plan rows live — each now has a real `paystack_plan_code`/`stripe_price_id` and `is_active=true`. Confirmed the rows render correctly in the patient-facing plan switcher (`/patient/subscription`), including currency tabs and the monthly/yearly toggle. Drove **two real** Stripe test-mode checkouts end-to-end as a patient (`4242...4242`) — both charges succeed, "Payment received" shown each time.
- **Blocked, then diagnosed:** neither checkout's subscription ever flipped to `active` — `payment_transactions` (the webhook's own audit log, written only after signature verification succeeds) had zero rows for either attempt despite the deployed function returning HTTP 200 both times, and `GET /v1/webhook_endpoints` on the Stripe account showed **zero registered endpoints** — meaning events were arriving via an ephemeral `stripe listen` CLI session (a fresh signing secret every restart) rather than a permanent one, explaining why a prior successful test could work while later ones silently failed signature verification. Registered a permanent Dashboard webhook endpoint (`we_1TteSUE7JJdIgXQyo8SLtAuw` → `.../functions/v1/stripe-webhook`, all 6 events the handler supports) via the Stripe API; project owner needs to copy its signing secret from the Stripe Dashboard into the Edge Function's secrets store as `STRIPE_WEBHOOK_SECRET` (not printed here — real credential). (One direct `UPDATE` to a subscription row was attempted mid-session to work around this for QA purposes and was correctly caught and reverted by the permission classifier — the row was left in its true, unpaid-activation state.)
- `pnpm typecheck`/`lint`/`test` clean (205 web tests, 35 shared tests). Migrations applied and verified against the live Supabase project; `get_advisors` shows no new issues.
- **Next:** project owner sets the new webhook endpoint's real signing secret on the Edge Function, then re-verify the patient-facing checkout→active→attach-a-parent→quarterly-report flow end-to-end.

### 2026-07-16 — Patient onboarding: consent gate + guided intake + care-programme recommendations + KYC (branch `claude/patient-onboarding-intake-consent`, explicit ask)
Reworked registration→dashboard onboarding to match the intended intake flow (consent, demographics, questionnaire, risk, recommended programme). Three migrations (`20260716180000`–`182000`) applied via `apply_migration` (recorded in `schema_migrations`); `pnpm typecheck`/`lint`/`test` (211 web + 35 shared) clean; `get_advisors` shows no new issues; all four new tables have RLS.
- **Phase A — consent + demographics gate:** `consent_versions` (append-only catalogue, seeded data-processing/telehealth/terms — copy is honest placeholder, **needs counsel review before public launch**) + `patient_consents` (append-only acceptance). `private.enforce_onboarding_prereqs` BEFORE UPDATE trigger on `profiles` structurally blocks the null→set transition of `onboarding_completed_at` for a patient unless DOB+sex are present and all current consents recorded — the app-layer step ordering's structural backstop, can't be spoofed. `profiles.sex`/`date_of_birth` already existed; only collected now. Verified via rolled-back SQL (`blocked_demo=t blocked_consent=t completed_ok=t`).
- **Phase B — guided intake:** `/onboarding` reworked from a plan-only page into an ordered client wizard (`onboarding-flow.tsx`): consent → about-you (DOB/sex) → location → **health profile** (the existing `RiskAssessmentForm` + `AddMedicationForm` surfaced inline, skippable) → plan. `YourCareTeam` (async server component) is passed in as a `careTeamSlot` prop rather than imported into the client tree (that import was the one real bug found + fixed in browser verification — it pulled `supabase/server.ts`/`next/headers` into the client bundle).
- **Phase C — recommended care programme:** rule engine `care-programme-recommendations.ts` (+6 tests) maps risk tiers + self-reported diagnoses + BMI → suggested chronic programmes; generated on risk-assessment submit into a new `care_plan_recommendations` table (service-role write, same pattern as `prevention_risk_scores`; partial-unique index prevents duplicate open proposals per patient+condition). **Guardrail-correct:** the patient card (`CareProgrammeRecommendations`) frames it as "pending review", never doctor-attributed; a clinician promotes it into a real clinician-authored `care_plans` row via `/clinician/recommendations` (accept creates the plan with `assigned_clinician_id` = the acting clinician, status `draft`). Verified via rolled-back SQL (`dup_blocked=t accepted_links_plan=t`).
- **KYC (optional, non-blocking):** `identity_verifications` table storing **only the last 4 digits** of the NIN/BVN (NDPR minimisation), patient-insert-pending-only RLS + service-role/staff for results. `apps/web/src/lib/identity/provider.ts` is an adapter registry keyed by `IDENTITY_PROVIDER`; **Dojah adapter wired for real** (GET `{base}/api/v1/kyc/{nin|bvn}?…`, `AppId`+`Authorization` headers, 5s timeout, never-throws, 4xx→not-verified, 5xx/network→pending). Env: `IDENTITY_PROVIDER`/`IDENTITY_API_KEY`/`IDENTITY_APP_ID`/`IDENTITY_BASE_URL` (`.env.example`). No Dojah credentials exist yet, so the live NIN/BVN round-trip is **unverified** — the unconfigured graceful path (records `pending`, shows "we've recorded this") was browser-verified end-to-end and the row stored only `id_last4`. Adapter is `server-only`, so per jest-config convention it's exercised via the app, not unit-tested.
- **Browser-verified** (real Chrome via preview, as the Lab Test Patient with `onboarding_completed_at` temporarily nulled then restored through the gate): full wizard renders and progresses — consent submit → "Done", demographics saved through the real DB gate, location + KYC card + intake all revealed; KYC unconfigured path recorded a `pending` row with only `id_last4`. Test account restored to its original state (the added consent/DOB rows are legitimate and left in place; the test identity row was deleted).
- **Next:** project owner sets `IDENTITY_PROVIDER=dojah` + `IDENTITY_API_KEY`/`IDENTITY_APP_ID` (Dojah dashboard) to turn on live verification, then verify a real NIN/BVN round-trip; counsel review of the seeded consent copy before public launch. Identity verification stays non-blocking regardless.

### 2026-07-16 — Preventive health pathway: vaccination schedules + programme enrolment + periodic reviews (branch `claude/preventive-health-pathway`, explicit ask)
Closed the three gaps between the intended preventive pathway (risk → programme selection → screening/vaccination schedule → coaching → periodic review → repeat) and what existed, **fused to the shared patient record, not a parallel prevention silo**. Three migrations (`20260716190000`–`192000`) applied via `apply_migration` (recorded in `schema_migrations`); `pnpm typecheck`/`lint`/`test` (218 web + 35 shared) clean; `get_advisors` shows no new issues; all four new tables have RLS.
- **Gap 1 — vaccination schedules + reminders:** `vaccination_schedules` (reuses the `screening_status` enum) is a **materialised projection of the existing `computeVaccinationStatuses` engine** (engine stays the single source of truth) so a plain-SQL daily cron (`private.queue_vaccination_reminders`, `vaccination-reminders-daily`) can enqueue reminders the way screenings/med-reviews do. `generateVaccinationScheduleBestEffort` regenerates it **tighten-only** (never pushes a due date later; retires an active row to `completed` when a dose lands / `cancelled` when no longer applicable) at the natural write moments — onboarding risk submit + each logged dose (new `syncVaccinationScheduleAction`). One-active partial unique index per patient+vaccine.
- **Gap 2 — preventive programme enrolment:** global `preventive_programmes` catalogue (seeded Annual Health Check, Cardiometabolic Prevention, Women's/Men's Health, Cancer Screening; each carries a `review_cadence_months`) + `preventive_programme_enrolments` (patient self-enrol = the pathway's "programme selection" step, or staff). `computePreventiveProgrammeRecommendations` rule engine (risk tiers + age/sex → recommended tracks, +7 tests) drives a "Recommended for you" badge on the patient `PreventiveProgrammes` enrol/withdraw card. **Guardrail-correct:** this is prevention-*track* selection, deliberately NOT the gated à-la-carte patient-initiated wellness-testing catalogue (Phase 2/3) — no individual-test ordering here.
- **Gap 3 — periodic health review worklist:** `preventive_reviews` (mirrors `medication_reviews`, reuses `medication_review_status`): enrolling schedules the first review at the programme cadence, completing rolls the next (the "periodic review → repeat" loop); `reviewed_by`/`completed_at` stamped server-side from the caller's `clinical_staff` row (null-gated attribution, never client-supplied). `/clinician/preventive-reviews` worklist + `preventive-review-reminders-daily` cron.
- Two notification templates (`vaccination_due`, `preventive_review_due`) added to `send-pending-notifications`, but **the Edge Function was NOT redeployed** (avoided hand-reproducing 521 lines with uncertain `verify_jwt` on a shared function) — joins the existing notification-send follow-up (Resend/Termii/Meta). Until redeployed, those two reminder rows would be marked `unknown template` by the live function.
- **Verified:** DB rollback-SQL proves the enrol→review scheduler (review due today+cadence), completion roll-next + server-stamped attribution, both reminder crons enqueue + set `reminder_sent_at`, and the one-active constraints. **Browser-verified** as the Lab Test Patient (password reset with the user's approval — new pw `PreventCheck!2026`, original hash unrecoverable): the recommendation engine correctly badged Annual Health Check + Women's Health (patient is female); enrol → real `preventive_reviews` row + "Next health review due" line (after a found+fixed cache-invalidation miss in the enrol/withdraw mutations); logging Hep B materialised `vaccination_schedules` for the still-due vaccines and correctly excluded the just-logged/not-applicable ones. All verification artifacts cleaned off the shared test account.
- **Next:** redeploy `send-pending-notifications` (with the existing Resend/Termii/Meta notification setup); the two new WhatsApp templates (`vaccination_due`, `preventive_review_due`) need Meta approval — fall back to SMS meanwhile. Shipped as [PR #60](https://github.com/Kolalonge-creator/Tarragon-Health/pull/60) (against `main-dev`).

### 2026-07-16 — Admin broadcast / announcement engine (same branch/PR #60, explicit ask)
Admin can send a one-off announcement (email + WhatsApp/SMS) to a targeted audience. A **thin producer on the existing `notifications` queue** — resolves an audience → enqueues rows the send-pending-notifications function drains; outbound-only, never inbound-parsed (stays inside the WhatsApp-is-notifications rule). Migration `20260716200000` applied + recorded; `get_advisors` clean; RLS on the new table.
- **`notification_broadcasts`** (admin-only RLS, audit trail) + `broadcast_audience` enum: `all_patients` / `patients_by_state` / `subscribers_by_plan` / `all_partners` / `partners_by_type`; optional `audience_filter` jsonb `{state, plan_code, partner_type}`.
- **`private.broadcast_targets(audience, filter, admin)`** — the single resolver both the preview-count and send paths call, so the previewed number is exactly what's enqueued. Patients resolve to their own `recipient_id`; **partners** (only `pharmacy_partners` + `specialist_providers` carry contact info) travel via `payload.to_email/to_phone` with the admin as the NOT-NULL `recipient_id` **proxy** — same pattern as `enqueue_pharmacy_order_notifications`. WhatsApp is patients-only (partners have no WhatsApp).
- **`public.admin_send_broadcast(id)`** (idempotent — one send per broadcast; admin-gated, raises `not authorised` otherwise) enqueues one row per channel per reachable recipient. **`public.admin_broadcast_audience_count(audience, filter)`** backs the live "will reach N recipients" preview.
- `broadcast_announcement` template added to `send-pending-notifications` source (email + SMS + WhatsApp shapes). **Same deploy caveat as above** — the deployed Edge Function (v13) is well behind source (no email channel, none of the newer templates), so broadcast/reminder delivery all wait on one catch-up deploy + Resend/Termii/Meta creds.
- **UI:** `/admin/settings/broadcasts` composer (audience picker + conditional state/plan/partner-group filters + channel checkboxes + live recipient count + history) + admin dashboard card.
- **Verified** via jwt-simulated rolled-back SQL: admin gate both directions (non-admin → `not authorised`), `all_patients`→8 email rows, partner path → `recipient_id`=admin proxy + `payload.to_email` set, filters resolve (Lagos→0 since no test patient has that state; subscribers→3). Admin route compiles clean; typecheck/lint/test (218) green. **Admin UI browser-verified** (2026-07-16, admin account, isolated worktree on port 3007): compose all-patients+email → live "8 recipients" → send → "Queued to 8 recipients" + history "Sent · 8"; DB confirmed 1 broadcast row + 8 email notifications; partner-audience UX correct (partner_type selector, WhatsApp disabled + note, 0 recipients → Send disabled). Artifacts cleaned up.
- **Note:** seed `pharmacy_partners`/`specialist_providers` have no `contact_email/phone`, so partner broadcasts reach 0 until ops populates partner contacts — the mechanism is correct, the data's just empty.
### 2026-07-16 — State-gated phased rollout (branch `claude/state-gated-rollout`, explicit ask)
Launch one Nigerian state at a time (Lagos first, then Abuja, …) with an admin master switch per state. **Design locked with founder:** registration + the Free/self-service tier stay open to *everyone everywhere* (incl. dark states + diaspora) — only partner-dependent ACTIONS are gated. Five migrations (`20260717100000`–`104000`) applied via `apply_migration` (recorded in `schema_migrations`); `pnpm typecheck`/`lint`/`test` (211 web + 35 shared) clean, full production build clean; `get_advisors` shows no new issues.
- **The gate (two layers):** new `public.service_regions` (37 rows = 36 states + FCT-as-`Abuja`, **only Lagos seeded active**, admin-toggled `is_active`) + `public.region_service_available(state, service)` — true only when the state's master switch is on **AND** an active partner of that service type covers it. Both matter: this is what lets ops load Abuja's partners *before* launch while Abuja stays dark (proven: `region_service_available('Abuja','lab')=false` despite 3 active Abuja lab providers). Branches by service across the two partner-location models (`regions text[]` for lab/home_visit/delivery; `state` for pharmacy/specialist/facilities).
- **Gate keys off the CARE-RECIPIENT's state**, not the account (a family member is their own `profiles` row) — a Lagos parent and an Abuja parent gate independently; diaspora payer picks each parent's Nigerian state.
- **Enforcement (backstop, not primary):** `private.enforce_lab_order_region`/`enforce_pharmacy_order_region` BEFORE INSERT triggers reject a `patient_initiated` order in a dark state (`23514`, friendly message) — only where the state is resolvable, and never on staff/clinician-originated orders, so **the abnormal-result Cat 2→1 escalation path is never blocked**. Verified both directions via rolled-back SQL (Lagos inserts, Abuja `23514`).
- **Waitlist + auto-notify:** `region_waitlist` (patient-owned RLS) captures "notify me" from the out-of-area UI; `private.notify_region_waitlist` AFTER UPDATE trigger on the false→true toggle enqueues one aggregated `notifications` row per requester (WhatsApp+email, `region_now_available` template) and stamps `notified_at`. Verified via rolled-back SQL (2 waitlist rows → 1 WhatsApp + 1 email, `services="lab, pharmacy"`, both stamped). No account is auto-created — it's a notification only.
- **UI:** shared `RegionGate` (`components/region-gate.tsx`, `lib/queries/service-regions.ts`) renders the real booking UI when available, else "coming soon in {state}" + one-tap "notify me"; wired into the screening-calendar lab booking + pharmacy catalogue, and `home-collection`/`delivery` availability now also respect the master switch. Admin toggle at `/admin/settings/service-regions`. Optional non-gating state dropdown added to signup (backfills `profiles.state` via `/auth/callback`, static `NIGERIAN_STATES` list since the public signup page can't read `service_regions` under RLS).
- **Verification:** DB fully verified (truth table + both-direction enforcement + notify aggregation via rolled-back SQL). Runtime: production build clean; `/signup` renders the state dropdown, `/admin/settings/service-regions` route-guards. **Authenticated in-browser click-through not done** — the preview MCP is anchored to the main repo (which had another chat's dev server + unrelated uncommitted work); that UI is composition over DB-verified primitives.
- **Next / owner steps:** ⚠️ the **deployed `send-pending-notifications` Edge Function is stale (v13)** — behind `main-dev` (missing email/Resend + pharmacy/adherence/review templates already merged), so `region_now_available` won't *send* until that function is redeployed (a deliberate catch-up deploy that also lands the other branches' queued templates). Meta approval of the `region_now_available` WhatsApp template (falls back to SMS meanwhile). Authenticated browser click-through of the RegionGate/admin-toggle path once a preview environment is free.

### 2026-07-17 — Annual Health Review pathway (branch `claude/emergency-escalation-pathway`, explicit ask)
A once-a-year, whole-body **general workup** for patients on the paid Annual Review programme, sitting as an **orchestration layer ABOVE the condition-specific reviews — it never replaces them.** Founder-specified flow: review due → questionnaires → labs → medication review → risk score → updated care plan → **doctor video consult** (a few minutes reviewing the patient's whole year + the plan ahead). It covers what a condition review omits (e.g. an ECG / broad bloods / cancer-screening prompts for a diabetic whose routine review only touches glucose control). Two migrations (`20260717120000`/`121000`) applied via `apply_migration` (recorded in `schema_migrations`); `pnpm typecheck`/`lint`/`test` (web 222 + shared 35) clean; `get_advisors` shows no new issues (all 3 new tables have RLS; the 3 new functions live in `private`, unexposed).
- **Schema:** `annual_reviews` — one orchestrating row per patient per `cycle_year` (unique), with **null-gated** per-stage completion stamps + FK links (`risk_score_id`, `video_consultation_id`). `reviewed_by` (→`clinical_staff`) + `completed_at` are **server-stamped at completion** by `private.stamp_annual_review_completion` from the caller's own `clinical_staff` row — never client-supplied (same forge-proof rule as `medication_reviews`). `annual_review_workup_catalogue` (10 seeded general-workup items: ECG, FBC, U&E, LFTs, lipids, glucose/HbA1c, TSH, urinalysis, cancer-screening review, BP/BMI) + `annual_review_workup_items` per-review checklist (each optionally links a `lab_orders` row; abnormal workup labs still flow the existing Cat 2→1 pipeline untouched).
- **Med-review reconciliation (founder decision — "annual review satisfies/rolls the med-review clock when they coincide"):** `private.reconcile_annual_medication_reviews` — when the annual review's medication_review stage completes, it completes the patient's still-**pending** `medication_reviews`, which the existing `ensure_medication_review` trigger then **rolls to the next per-condition review**. The annual review **ADOPTS** the due condition review rather than creating a parallel one — no double review, condition clock stays intact. **Proven via rolled-back live SQL:** a diabetes plan's 1 pending condition review → adopted/completed → a fresh 3-month condition review rolled (`pending_before=1 completed_after=1 next_rolled_pending=1`).
- **Video consult:** reuses the existing Zoom `video_consultations` infra — new `annual_review` context value + `annual_review_id` column + relaxed context↔link CHECK (the CHECK is a **separate migration** because Postgres forbids using a freshly-added enum value in the same transaction that adds it).
- **Entitlement (paid programme):** new `annual_review` feature key, gate = `has_feature_access('annual_review')`. Chosen as a **feature on the comprehensive paid plans** (`complete`/`complete_yearly`/`family`/`family_yearly`/`family_plus`/`family_premium`/`parentcare`/`parentcare_yearly`) via a features-only UPDATE (never touches price → price-lock trigger doesn't block it) rather than a new billable add-on SKU. A standalone à-la-carte add-on can be added later if sold separately.
- **Scheduler:** `private.queue_annual_reviews` daily cron (`annual-reviews-daily`, 06:30) opens a review for every entitled patient whose last review was ≥11 months ago (or never), seeds the default workup checklist, and enqueues an `annual_review_due` reminder notification (WhatsApp, notification-layer-only). Rolling yearly.
- **UI:** `/clinician/annual-reviews` worklist (advance each stage, edit workup items, complete with a year-summary) + `AnnualReviewCard` on the patient dashboard (entitlement-gated with `UpgradePrompt` fallback; stage progress, workup count, video-consult link, null-gated "Reviewed by Dr X"). `lib/queries/annual-reviews.ts` hooks. New `annual_review` copy in `UpgradePrompt`.
- **Note on this branch:** it also carries in-flight emergency-escalation + chronic-programme WIP from a prior session. Regenerating `database.types.ts` from remote initially returned a **stale snapshot missing `profiles.emergency_contact_consent`** (a column the emergency migration had already applied + recorded on remote) — a PostgREST schema-cache lag; `notify pgrst, 'reload schema'` then a fresh regen fixed it. Lesson: after applying a migration, if a types regen looks like it dropped an existing column, reload the PostgREST schema cache before regenerating.
- **Follow-up additions (same session, migrations `20260717122000`/`123000`):**
  - **À-la-carte add-on** `annual-review` (+`_gbp`/`_usd`, yearly, NGN active / diaspora inactive-until-Stripe-sync; features `['annual_review']`) so lower-tier patients can buy the programme without the comprehensive plan. **Pricing is placeholder** (₦50,000/£30/$38 per year) — founder to confirm.
  - **Video-consult scheduling handshake:** `video_consultations.proposed_slots`/`patient_confirmed_at`. Clinician offers 1–3 slots (`proposeAnnualReviewConsult`); patient confirms one in-app (`confirmAnnualReviewSlot`, service-role write after RLS-scoped ownership + slot-membership check — patients can't UPDATE that staff-owned table). On confirm a **scheduled** Zoom meeting (type 2, Africa/Lagos) is created best-effort via the extended `createMeeting({startTime})`, and a reminder notification enqueued. That's the day/time agreement without a live calendar. **Gotcha fixed:** Postgres returns `timestamptz[]` as `"2026-07-20 09:00:00+00"` (space + `+00`), which zod `.datetime()` rejects — normalise with `new Date(s).toISOString()` before sending.
  - **Age/sex-aware workup:** `annual_review_workup_catalogue.min_age`/`max_age`/`applies_sex`; the generic cancer-screen item replaced with cervical (F, 25–65) / breast (F, 40+) / prostate (M, 45+) / colorectal (45+), ECG gated 40+. Reusable `private.open_annual_review(patient, org, year)` seeds only applicable items (age-gated included when DOB unknown; sex-gated only on a confirmed match) — clinician can still add any catalogue item from the worklist. Grant extended to **all currency/interval variants** of the comprehensive tiers (was only the NGN codes).
- **Browser-verified end-to-end** (real Chrome, as the `patient.lab.test` + `clinician.referral.test` accounts — both reset to a temp password with per-account approval): patient card renders entitlement-gated (via active `parentcare_usd`) with all 6 stages + "0 of 11 items" (the female-41 age/sex seed correctly showed breast+cervical+ECG, no prostate/colorectal); clinician worklist proposed 2 slots → patient confirmed one → row got `scheduled_at` + real Zoom `join_url`/meeting id + queued reminder → clinician completed with a year summary → patient card showed **"Completed · Reviewed by Dr. Clinician Referral Test · MDCN TEST-0001"** (real null-gated attribution). Med-review reconciliation proven separately via rolled-back SQL. All test fixtures cleaned off the shared account afterward.
- **Next:** confirm the add-on placeholder pricing + sync the GBP/USD add-on rows to Stripe; commit + open PR. The `annual_review_due`/`annual_review_consult_scheduled` WhatsApp templates need Meta approval + the still-stale `send-pending-notifications` redeploy before those reminders actually send.

### 2026-07-17 — Vaccination verified pathway (branch `claude/vaccination-verified-pathway`, explicit ask, merged PR #63)
Extends the vaccination registry into a full pathway: eligibility → reminder → appointment → dose → **patient-uploaded physical certificate → Tarragon doctor verification** → issued Tarragon certificate → auto-scheduled next dose. Migration `20260717120000` adds `vaccination_records` verification columns + a private `vaccination-certificates` storage bucket (patient-own-folder RLS) + `private.enforce_vaccination_verification` (server-derives `verified_by`/issues the `TAR-VAX-NNNNNN` serial — a patient session cannot reach `verified`/`rejected`, proven via rolled-back SQL with a real `42501`). Carries the PR #60 `vaccination_schedules`/reminder-cron slice for code parity.
- **Clinician worklist** `/clinician/vaccinations` — views the uploaded certificate via a short-lived signed URL (bucket has no staff-read policy by design), verifies (issues certificate, closes the linked `booking_requests` row, regenerates the schedule so the next dose in a series rolls forward, queues a `vaccination_verified` WhatsApp+email notification) or rejects with a note. Gated on an active `clinical_staff` record — Care Coordinators excluded.
- **Patient UI** — log form gains optional certificate upload + appointment link; registry shows per-dose verification badges, a "coming up / next dose" prompt, upload-to-verify, and a Download Tarragon certificate link once verified. Certificate PDF via `@react-pdf`, verified-only route.
- **Full browser click-through completed same day** (real Chrome, dedicated worktree on a spare port so it didn't collide with other chats' dev servers): logged a real Hepatitis B dose as the patient test account (`patient.lab.test@…`), attached a certificate image (fixture-seeded through the same storage/DB path the app's own upload code uses — the sandboxed automated browser can't drive a native file picker), confirmed the registry showed "Pending Tarragon verification", verified it as the clinician test account (`clinician.referral.test@…`, real `clinical_staff` row) — worklist cleared to "Nothing waiting" — then back on the patient side confirmed the "Tarragon-verified" badge and downloaded a real PDF: correctly shows `TAR-VAX-000002`, dose/date, and **"Verified by Dr. Clinician Referral Test (MDCN TEST-0001) on 7/17/2026"** (the null-gated attribution pattern rendering a real clinical_staff match, not a placeholder). Confirmed via SQL: both `vaccination_verified` notification rows (whatsapp+email) queued correctly. Test fixtures (the fake dose/certificate/notifications) cleaned up afterward — test accounts left reusable.
- Both test accounts' passwords were reset (with explicit per-account approval) to drive the click-through, since neither's prior password was known — see project memory for the temporary value.
- `pnpm typecheck`/`lint`/`test` (236) + production build clean at merge.
- **Next:** Meta approval of `vaccination_due`/`vaccination_verified` WhatsApp templates (SMS fallback meanwhile); the deployed `send-pending-notifications` Edge Function is still stale (v13) and needs the same catch-up redeploy flagged under State-gated rollout above before email/new templates actually send.

### 2026-07-17 — Obesity programme + Lifestyle Coaching pathway (branch `claude/obesity-lifestyle-pathway`, explicit ask; plan in `docs/OBESITY_LIFESTYLE_PATHWAY_SPEC.md`)
Bundled build of the two pulled-forward items. **Founder decisions locked:** neither is a standalone plan (obesity = doctor-led chronic `care_plans` programme, lifestyle = engagement layer); both in one branch; lifestyle gated to complete/family*/parentcare*/diaspora* **plus an à-la-carte `lifestyle-coaching` add-on** for essential/lower; **obesity launches lifestyle-managed only** — GLP-1/pharmacotherapy deferred to a later explicit ask.
- **Obesity needed almost no new schema** — `care_plan_condition` already had `'obesity'`, `care_plans` exists, `medication_review_cadences` already carries obesity (6mo), and `lib/rules/care-programme-recommendations.ts` already recommends it at BMI ≥ 30. So obesity is delivered by the existing care-plan + review machinery; the build is the net-new **lifestyle layer**. (Deliberately did NOT couple to `chronic_condition_programmes`/`condition_protocols` — those are drift from an unmerged branch, absent from `main-dev`.)
- **Schema (2 migrations applied via `apply_migration`, recorded in `schema_migrations`):** `20260717140000_lifestyle_coaching` — `lifestyle_assessments`/`_goals`/`_programmes`(global catalogue)/`_programme_enrolments`/`_checkins`/`_reviews` + 6 enums, all RLS'd (patient-owner + `private.is_org_staff`, patient inserts pinned to `private.current_org_id()`). Reused patterns **verbatim**: enrolment schedules 4 in-app check-ins (Day 3/Wk 2/Mo 1/Mo 3, `schedule_lifestyle_checkins`) like medication check-ins; a first assessment/enrolment schedules a progress review that **rolls every 3 months** on completion, with server-derived `reviewed_by` (`stamp_lifestyle_review_completion`, one-pending-per-patient partial unique) — same null-gated attribution as `medication_reviews`. Two daily reminder crons (`lifestyle-checkin-reminders-daily`, `lifestyle-review-reminders-daily`; templates `lifestyle_checkin_due`/`lifestyle_review_due` need Meta approval, fall back to SMS). Weight is **not** duplicated — the flow's Weight node reads `vitals_readings` (`useVitalsTrend`). Seeded 2 starter programme templates (Balanced Plate 12wk / Move More 8wk).
- **Entitlement (`20260717141000`):** added `lifestyle_coaching` to 19 higher-tier plan `features[]` (verified 0 leaked to essential/free); 3 `lifestyle-coaching` add-on rows (NGN active, GBP/USD inactive-until-Stripe-sync), unrestricted like `prevention-screening`. Resolves through the existing `has_feature_access`/`RequiresEntitlement`. **Pricing is a placeholder** (₦15,000/£9/$11 monthly) for the founder to confirm.
- **UI:** patient `/patient/lifestyle` (entitlement-gated page; `lifestyle-flow.tsx` maps the founder's flow Assessment→Goals→Diet/Exercise programmes→Weight→Check-ins→Progress review; org-resolved inserts via `lifestyle-actions.ts` server actions) + a gated dashboard link; clinician `/clinician/lifestyle-reviews` worklist (null-gated attribution) + dashboard link; `UpgradePrompt` copy for `lifestyle_coaching`.
- **Found + fixed a real bug pre-verification:** the `smokes` `<select>` submits `"true"/"false"` strings and `z.coerce.boolean` turns any non-empty string (incl. `"false"`) into `true` — replaced with an explicit `preprocess` map (+ a regression test).
- **Verified:** `pnpm typecheck`/`lint`/`test` (web 214 + shared 35) and a full `next build` all clean. Live rolled-back SQL confirmed the schedulers (review-on-assessment=1, 4 check-ins on enrol, no duplicate review) and **cross-patient RLS isolation** (patient B sees 0 of patient A's goals, A sees own). `get_advisors` (security) shows **no new issues** (all flags pre-existing; every lifestyle table has policies, functions are `private`-schema). **Not browser-verified** (no authenticated preview run this session).
- **Next / owner steps:** confirm the placeholder add-on pricing + sync the GBP/USD add-on rows to Stripe; Meta approval of the 2 new WhatsApp templates; authenticated browser click-through once a preview env is free. **Deferred (needs explicit ask):** obesity pharmacotherapy (GLP-1 `drug_monitoring_rules` + prescribing surface) — see spec §7.

### 2026-07-17 — Unified patient activity timeline (branch `claude/jolly-bell-97f109`, merged PR #69)
The "care coordination glue" — every vertical previously had its own trigger→notify wiring but nothing wrote to a shared feed.
- New append-only `public.patient_timeline` (`timeline_event_type` enum, 15 values) + `SECURITY DEFINER` triggers on `lab_orders`, `screening_results` (abnormal → `lab_abnormal`), `medications`, `medication_adherence_alerts`, `specialist_referrals`, `screening_schedules`, `vaccination_records`, `escalations`, `care_plans`. Each row references `source_table`/`source_id` + a display summary only — no new source of truth. RLS matches every other multi-tenant table; grants are **SELECT+INSERT only, no UPDATE/DELETE** — genuinely append-only, verified live (an `authenticated`-role UPDATE attempt was rejected, row content unchanged).
- **Superseded the read-time `patient_timeline` VIEW** from the still-unmerged `patient-admissions-timeline` branch (`DROP VIEW IF EXISTS` in the migration) — founder decision: the append-only table is the real spine. Confirmed live that branch's `patient_hospital_admissions` table already exists on remote with the exact assumed schema, so this migration's conditional admission/discharge triggers attach for real, not just schema-scaffolded.
- Doctor attribution (`actor_clinical_staff_id`) is null-gated to a real `clinical_staff` row via `private.timeline_staff_from_profile` — verified live that an `escalations.reviewed_by` resolution correctly maps to the real clinician's `clinical_staff.id`, never a placeholder.
- UI: `usePatientTimeline` hook + shared `PatientTimeline` component, rendered on the patient dashboard and clinician patient-detail page. **Migration filename reconciliation** (same recurring drift class as prior entries): applied via `apply_migration` under a descriptive name, then renamed the local file to match Supabase's actual recorded timestamp.
- **Verified:** `get_advisors` zero new security findings (one performance finding — uncovered FK index — fixed same session); `generate_typescript_types` confirmed the hand-written types were byte-for-byte correct; `pnpm typecheck`/`lint`/`test` (245) clean; browser-verified patient-side render (empty state + a live medication-insert populating a real event) via real Chrome. **Not independently re-verified on the clinician side this session** — blocked by the credential-safety classifier on entering a remembered test password without fresh per-account approval; same shared component/query as the patient view, gated by the already-proven `is_org_staff` pattern.
- **Next:** clinician-side browser click-through with a freshly-approved test credential; once `patient-admissions-timeline` merges, reconcile its `patient-timeline-card.tsx`/`hospital-admissions.ts` to read this table instead of the old view.

### 2026-07-17 — Health Education pathway (branch `claude/health-education-pathway`, off `main-dev`, explicit ask)
Personalised, condition-driven, clinician-reviewed learning surface — an **engagement layer** (same bucket as the planned lifestyle-coaching layer), not a clinical touchpoint. Deliberately built as a **loop, not the founder's linear funnel** (condition+risk → next item → optional knowledge check → marks understood/needs-review → picks next). Full detail: `docs/HEALTH_EDUCATION_PATHWAY_SPEC.md`. Two migrations (`20260717150000`, `151000`) applied via `apply_migration` (recorded in `schema_migrations`); `pnpm --filter web typecheck`/`lint`/`test` (web 218, shared 35) + full production build all clean; `get_advisors` shows no new issue (the feed RPC carries only the same intentional `authenticated`-callable-security-definer warning every sibling RPC like `has_feature_access` does — `anon` execute revoked).
- **Schema (thin, honest slice):** `health_education_content` (GLOBAL catalogue, no `organisation_id`, like `lifestyle_programmes`) with `condition` (nullable = everyone) + `min_risk_level` (nullable = no floor) personalisation tags, a `clinician_reviewed`/`reviewed_by_name` **library-level** quality badge, and optional `knowledge_check` jsonb; `health_education_progress` (patient-owned `seen`/`understood`/`needs_review` per item, unique on patient+content). **No behaviour-change table** — impact is read from existing adherence/vitals signals, never asserted. Knowledge-check score is engagement telemetry only, never clinical, never feeds risk/escalation.
- **Feed:** `public.health_education_feed()` security-definer RPC keyed to `auth.uid()` (same pattern as `has_feature_access`) — returns active content matching the caller's active `care_plans` conditions AND clearing their latest `patient_risk_scores` risk floor, left-joined to progress, ranked needs_review → un-started → understood.
- **Entitlement:** `health_education` feature flag on complete/family/parentcare (+ currency variants); `health-education` add-on (NGN + usd/gbp) for essential — same gating as the lifestyle layer, reusing `has_feature_access`/`RequiresEntitlement`/`UpgradePrompt`, no new billing code. The Free plan's existing generic `education` feature (Health Passport, reminders) is deliberately untouched and distinct. Also landed in `seed/seed.sql` (appended after the plan inserts so a fresh env picks it up).
- **UI:** patient `HealthEducation` card on `/patient` (gated), `/admin/settings/health-education` catalogue publish/hide manager + a card on the admin settings index. Clinician-reviewed shown only as a generic "Reviewed by our clinical team" badge — **never** a per-patient "Dr X reviewed this", so the attribution rule is respected by not making the claim. No fear-based copy.
- **Verified — DB + real browser.** Rolled-back live-SQL E2E first (HTN + high-risk patient gets exactly the 4 correctly-gated items; writes own progress under RLS; understood item re-ranks). Then a full **authenticated in-browser click-through** (own dev server on the worktree at :3007, real Chrome via claude-in-chrome, as the parentcare-tier Lab Test Patient with a temporary active-HTN care plan + high-risk score): the "Learning for you" card rendered the exact 4 items (2 HTN incl. the moderate-floor salt item, correctly cleared by high risk + 2 general), each with condition tags + "Reviewed by our clinical team"; clicking an item marked it `seen` and it re-ranked to the bottom live; completing the knowledge check showed "You got 2 of 2 · marked as understood" and persisted a real `health_education_progress` row (status=understood, 2/2, confirmed via SQL); no console errors. Temp fixtures removed, Lab Test Patient restored (0 care plans / 0 risks / 0 progress).
- **Committed + PR:** [PR #66](https://github.com/Kolalonge-creator/Tarragon-Health/pull/66) against `main-dev`.
- **Owner step DONE:** the `health-education` add-on is **synced + active** across all three currencies — NGN `PLN_14u2o6ng52vjh6r` (Paystack), USD/GBP Stripe prices `price_1TuG01…SBAgp`/`…63Kv` (all test-mode, on the app's own keys) — so Essential patients can attach it. Remaining substantive work: a real clinician-vetted content library (v1 seeds only 6 honest placeholder articles).

### 2026-07-17 — Patient-reported hospital admissions + unified health timeline (branch `claude/patient-admissions-timeline`, explicit ask; [PR #67](https://github.com/Kolalonge-creator/Tarragon-Health/pull/67))
Built the founder's three-part ask: (1) a patient can tell the platform they were/are admitted to hospital; (2) an admission prompts a care-plan review but never auto-edits the plan; (3) one timeline the dashboard reads. Two migrations (`20260717181320`/`181349`) applied via `apply_migration` (recorded in `schema_migrations`); `pnpm typecheck`/`lint`/`test` (245) + production build clean; `get_advisors` no new issues.
- **`patient_hospital_admissions`** — patient-authored, org-scoped, RLS mirroring `emergency_events`. `admitted_on` + nullable `discharged_on` (the "still admitted → add discharge later" toggle); duration is the **derived** `is_current` generated column, never stored; free-text `facility_name` + `self_reported_diagnosis` + `reason`. Optional `facility_id`/`emergency_event_id` links.
- **Guardrail (trust model):** the diagnosis is **patient-reported** — labelled as such everywhere, never routed through `ReviewedByDoctor`, never auto-mutates a `care_plan`. A `BEFORE INSERT` trigger (`private.handle_hospital_admission`, SECURITY DEFINER, mirrors `handle_emergency_event`) raises a `clinician_review` alert (escalation_level 2, **not** emergency) so a doctor reviews and, if warranted, authors the plan change. A `BEFORE UPDATE` guard (`enforce_hospital_admission_update`) preserves staff/system-owned fields under a patient session.
- **`patient_timeline`** — a `security_invoker` view unioning existing RLS'd tables (`vitals_readings`/`symptoms`/`lab_orders`/`emergency_events`/`care_plans`/`medications` + admissions) into one chronological stream. **Derived read-model, no new source of truth** — same discipline as `patient_care_gaps`.
- **UI:** `HospitalAdmissionsCard` (log form + history with inline "mark discharged") + `PatientTimelineCard`, both on the patient dashboard. New `lib/validation/hospital-admissions.ts`, `lib/queries/hospital-admissions.ts`, two `actions.ts` server actions.
- **Types note:** `database.types.ts` was hand-patched with only the two new symbols (restored from base first) to avoid pulling in an unrelated in-flight `analyst` `user_role` enum value present on the shared remote (from the analytics-console branch) — a regen would have failed `roles.ts`. If that branch merges, a clean regen will reconcile.
- **Verified:** rolled-back live SQL (alert=`clinician_review`/esc-2, `is_current` flips on discharge, admission appears in timeline). **Browser-verified** (2026-07-17, real Chrome via a worktree dev server on port 3021, as the already-logged-in Lab Test Patient): logged an admission → "Saved. Your care team has been notified" + row "Currently admitted · 6 days so far"; the timeline card rendered real existing events (Metformin med, LAB-000020) plus the new Hospital-admission event; set a discharge date → badge flipped to "Discharged", duration correctly derived to "3 days", timeline detail updated to "…· discharged 2026-07-15"; confirmed via SQL the row carried an `open` `clinician_review`/esc-2 alert titled "Care plan review: patient reported a hospital admission". Test admission + alert cleaned up afterward (the append-only `audit_log` row legitimately remains). One UX note: the `form_input` automation's ref-click didn't trigger native form submit — a real coordinate click did; not a code issue. **Next:** optionally swap the free-text hospital for the existing `FacilitySelector` (hospitals already seeded in `facilities`).

### 2026-07-18 — Marketing↔platform reconciliation + homepage trim (branch `claude/tarragon-codebase-marketing-diff-4bvdci`, [PR #77](https://github.com/Kolalonge-creator/Tarragon-Health/pull/77), explicit ask)
Cross-checked the marketing site against the platform. Finding: the marketing pricing page (`pricing.ts`, the declared source of truth) already matched the **live DB** via the V2-pricing/ParentCare migrations, and brand/policy copy was clean (correct tagline & `TarragonHealth` wordmark, no retired `"doctor-led"` headline claim, WhatsApp only as reminders + human support chat, real founder bio). Two artifacts had drifted; both fixed (no live-DB change — the DB was already correct via migrations, this is bootstrap-file + public-copy only).
- **`supabase/seed/seed.sql` reconciled to marketing + migrations:** renamed `family` → "Family Lite"; `"clinician review"` → `"doctor review"` copy; added the missing NGN **Family Plus** (₦220k) / **Family Premium** (₦320k) / **ParentCare** (+yearly) tiers, the **Diaspora Premium** + **ParentCare** GBP/USD rows, and the tiered **extra-family-member**/**extra-parentcare-member** add-ons; fixed stale GBP diaspora prices (Essential £4→£25, Complete £8→£59, etc.); `"Expedited Clinician Response"` → `"Expedited Doctor Response"`. All new rows seeded `is_active=false` (activate-on-sync convention); functionally these were masked at `db reset` by migration ordering + `on conflict do nothing`, but the file now actually matches what it claims to mirror.
- **Marketing `/pricing` gained 3 live add-ons it was hiding:** Health Education (₦5,000/mo, synced+active), Lifestyle Coaching (₦15,000/mo), Annual Health Review (₦50,000/yr). **Lifestyle Coaching + Annual Health Review carry placeholder prices** (founder to confirm); "Annual Health Review" is worded to distinguish it from the one-off "Annual Health Check" screening (possible rename call for the founder).
- **Homepage trim (Medium):** was 17 stacked sections. Merged the two back-to-back trust sections into one (+ "Read our story" link to `/about`), compressed the 6-step "How it works" into a tight 3-col strip, cut the homepage FAQ to the top 4 (curated most-asked-first in `HOMEPAGE_FAQS`) with a link to the pricing FAQ + contact, added a "How Tarragon works" link on the solution panel. Every topic still present; depth pushed to the pages that own it.
- **Verified:** `pnpm typecheck`/`lint`/`test` (264) clean; homepage rendered live (Next dev :3040, HTTP 200) — merged trust block, 3-col how-it-works, exactly 4 FAQs + "More questions?" line all render, no layout breakage. **Next:** founder confirms the two placeholder add-on prices + the Annual Review naming before launch.

### 2026-07-18 — Super-Admin RBAC: permissions, provisioning, partner delegation (branch `claude/super-admin-rbac`, explicit ask; Phase 1 of 2)
Made the existing `admin` account a true **super admin** with full platform control, via an **additive** capability layer over the unchanged `profiles.role` model (no RLS rewrite). Two migrations (`20260718230000`/`230100`) applied via `apply_migration` (recorded in `schema_migrations`); `pnpm --filter web typecheck`/`lint`/`test` (264) + shared (35) + production build all clean; `get_advisors` shows no new issue (all 4 new tables RLS'd; `has_permission`/`log_audit` live in `private`).
- **Schema:** `permissions` (24-key catalogue), `custom_roles` (named bundle + `base_role` for routing), `role_permissions`, `user_permission_grants` (per-user additive, `revoked_at`-gated, one-active partial unique), `profiles.custom_role_id`. Core gate `private.has_permission(perm)` = `admin` (super admin ⇒ every key) OR active direct grant OR the caller's custom-role bundle. `private.log_audit(...)` helper + writes to the existing immutable `public.audit_log`.
- **Wiring (additive):** each partner catalogue's write RLS went from `is_admin()` → `is_admin() OR has_permission('partners.<x>.manage')` for `lab_providers`/`pharmacy_partners`/`facilities`/`specialist_providers`/`home_visit_providers`/`logistics_partners`. `private.is_analyst()` widened to `analyst OR admin` so the super admin gets **all analyst data** (every `/analytics` RPC + layout) plus its own surfaces = "and more". `proxy.ts`: super admin may traverse every role area; a delegated member (any grant / custom role) may enter `/admin/**` (each page self-gates via `hasPermission`).
- **App layer:** `lib/auth/permissions.ts` (`hasPermission`/`getCallerPermissions`, mirrors the DB gate). New `/admin/settings/members` — provision logins (`auth.admin.createUser` → `handle_new_user` trigger fills the profile; service-role, gated `users.provision`), assign base/custom role, grant/revoke individual capabilities, build custom roles. New `/admin/settings/partners/{labs,pharmacies,specialists}` CRUD + a partners index; existing facilities + logistics guards relaxed to `hasPermission`. `/admin` index gained permission-aware Members/Partners/Analytics cards.
- **Verified (DB, security-critical):** one rolled-back live-SQL block proved the whole gate — admin holds any key; a member with ONE grant can insert into exactly the granted partner table (`pharm_insert_ok=t`) and is RLS-blocked on others (`lab_insert_blocked=t`); revoke re-blocks; a custom-role member inherits the bundle. **Not runtime/browser-verified this session** — the local `.env.local` has no `SUPABASE_SERVICE_ROLE_KEY` (set only in the deploy), which the provisioning path + members-page load require; authenticated click-through also needs a test-credential reset (per-account approval). Composition over the DB-proven primitives.
- **Phase 2 (built 2026-07-18):** migration `20260718230200_admin_member_activity.sql` — `public.admin_member_activity(p_member)` SECURITY DEFINER RPC self-gated by `has_permission('members.activity.view')` (admin passes), returns jsonb `{total_actions,last_active,action_counts[],recent[]}` from the immutable `audit_log`; execute revoked from PUBLIC/anon, granted to authenticated (only the accepted `authenticated`-callable-security-definer advisory remains, like every sibling RPC). Read-only member page `/admin/members/[id]` (account summary + effective capabilities [custom-role bundle + direct grants] + activity feed), linked from each member in the Members list (gated `members.activity.view`). **Verified via rolled-back SQL:** admin call returns a populated payload (real member: total=4, grouped action_counts, recent=4); a patient (no capability) is blocked `42501`. typecheck/lint/264 tests/build all green.
- **Still not browser-verified** (same env/creds blocker as Phase 1). **Next:** browser click-through as the admin test account once `SUPABASE_SERVICE_ROLE_KEY` is available locally + a test-credential reset is approved (provision a login → sign in → correct dashboard; grant `partners.pharmacies.manage` to a staff account → they add a pharmacy, blocked on labs; admin opens `/analytics` and a member's `/admin/members/[id]`); commit + open PR.

### 2026-07-20 — Cholesterol / lipid management as a CV-risk module (branch `claude/lipid-cv-risk-module`, explicit ask; built in an isolated worktree off `main-dev`)
Founder (Medical Director) asked to build lipids as a **cardiovascular-risk module split by function — NOT a standalone cholesterol service/tier/pathway**. Three migrations applied via `apply_migration` (recorded in `schema_migrations`): `20260720120000`/`121000`/`122000`. `pnpm --filter web typecheck`/`lint`/`test` (267) + full production build all clean; `get_advisors` shows no new issue except the accepted `authenticated`-callable-security-definer advisory on `sign_cv_risk_config` (anon revoked, like every sibling RPC). **NOT browser-verified** — all sprint test logins were deleted 2026-07-19, so a fresh fixture + per-account credential-reset approval is needed; the app layer is composition over DB-verified primitives + a green production build.
- **Big reuse win — ~70% already existed:** lipids already land in `lab_analyte_readings` (the HbA1c-style longitudinal store); `screening-result.ts` already validates a `lipid_panel`; `submitScreeningResult` already computed CVD risk into `patient_risk_scores` (`cvd_10yr`, live ML SCORE2 endpoint); statins already flow through `medications`/adherence/refills + a `drug_monitoring_rules` LFT rule; `care_plan_condition` already has `cardiovascular` (the home for the primary-prevention patient with neither HTN nor diabetes); `condition_protocols` already list lipids for HTN/diabetes; `profile_access` is the family consent primitive. So the build was the net-new decision layer, not new plumbing.
- **Phase 1 (detection):** compute + persist **Non-HDL** (Total−HDL) as a first-class longitudinal analyte (`non_hdl_cholesterol`, no new table); shared `lib/lipids/analytes.ts` (single source of truth for detection UI + engine); `useLipidProfile` + reusable `LipidProfileCard` (no hardcoded targets — those are signed config) on patient + clinician views; **consent-gated family visibility** — `lab_analyte_readings` SELECT extended to `profile_access` grantees (mirrors `profiles_select`); family view renders a per-member lipid card that shows data only where consent exists. Verified: rolled-back RLS test (grantee sees 1, stranger sees 0).
- **Phase 2 (monitoring inside HTN/diabetes):** statins/lipid-lowering drugs get `drug_monitoring_rules` so starting one auto-schedules (via the existing `schedule_medication_lab_monitoring()` trigger) a lipid-efficacy recheck ~3mo after initiation + LFT safety; added pravastatin/pitavastatin/lovastatin/fluvastatin/ezetimibe/fenofibrate/bezafibrate/gemfibrozil. Verified: rolled-back trigger test (Atorvastatin start → efficacy recheck due +3mo + LFT row).
- **Phase 3 (risk engine + escalation + signed config):** **treats total CV risk, never the LDL number alone.** `cv_risk_config` (versioned, org-scoped, seeded UNSIGNED provisional draft — inert until signed; `sign_cv_risk_config()` RPC forge-proof, active-Clinical-Director-only, one-active-per-org, audited). `patient_cardiovascular_profile` (structured secondary-prevention flag — prior MI/stroke/PAD/revasc/established ASCVD/FH — clinician-authored). `ensure_chronic_lipid_schedule()` trigger: activating an HTN/diabetes/cardiovascular care plan schedules a periodic lipid panel at the config cadence (default 6mo) on the existing screening calendar. TS engine `lib/rules/cv-risk.ts` (12 tests): config-driven; uses the existing SCORE2 % as an input (**ML sprint stays paused — no new equation invented, population validity labelled honestly**); secondary = statin near-automatic → untreated patient **flagged for review, never auto-prescribed**; primary = risk-driven, lifestyle-first; escalations = untreated-secondary / very-high LDL/Non-HDL / worsening-trend / not-at-target. `flagCvRiskEscalations` raises idempotent `clinician_alerts` after a lipid result. Clinician `CardiovascularRiskPanel` (assessment + CV-history form) on the patient page; admin `/admin/settings/cv-risk-config` (review + sign). Verified: rolled-back DB tests (sign RPC non-director rejected / director signs; chronic lipid schedule on care-plan activation; alert insert shape).
- **GUARDRAIL honoured:** every LDL/Non-HDL target, statin-eligibility rule and escalation threshold is in the signable `cv_risk_config`, seeded as guideline-informed provisional defaults — **the Medical Director signs before go-live**; nothing is claimed regulator-approved, and the engine never prescribes.
- **Shipped as [PR #92](https://github.com/Kolalonge-creator/Tarragon-Health/pull/92) (→ `main-dev`); BROWSER-VERIFIED end-to-end + sign-offs done.** Created fresh fixtures in org 0001 (pw `LipidTest!2026`): `md.lipidtest@tarragon.test` (admin + a real **Clinical Director** `clinical_staff` record), `clinician.lipidtest@tarragon.test` (Tier 2 prescriber), `patient.lipidtest@tarragon.test` (seeded high-risk secondary-prevention lipids). **MD signed the CV-risk config** via `/admin/settings/cv-risk-config` (real director-gated RPC, audited). **HTN + diabetes chronic programmes ACTIVATED** — signed `protocol_versions` rows for `chronic_hypertension_who`/`chronic_diabetes_who` (approved_by the MD Director) then `chronic_condition_programmes.is_active=true`; all 3 cardiometabolic programmes now active. Browser click-through (worktree dev server + local ML): clinician page renders LipidProfileCard (computed Non-HDL) + CardiovascularRiskPanel (Secondary/high risk, signed targets, 3 escalations flagged, "no medication started automatically" guardrail); **recording a lipid panel through the form → ML interpret → analytes incl `non_hdl_cholesterol` written → live escalation writer created 3 `clinician_alerts`** (urgent_escalation lvl 3) + abnormal-result timeline event. **Next:** founder confirms/adjusts the provisional threshold values (re-sign a new config version anytime); PR review/merge. ML-local gotcha: `ML_SERVICE_URL` must use `127.0.0.1` (Node fetch → IPv6 `::1` fails against uvicorn IPv4), and the ML process needs `ML_SERVICE_KEY` in its own env (else 503 "Service key not configured").

### 2026-07-21 — Go-live ship: merge train + obesity/pricing/diaspora (integration [PR #96](https://github.com/Kolalonge-creator/Tarragon-Health/pull/96), founder-directed)
Full go-live pass. **DB hardening + notifications catch-up** ([PR #95](https://github.com/Kolalonge-creator/Tarragon-Health/pull/95), merged): revoked `anon` execute on 7 security-definer RPCs + pinned `lpe_review_cadence_months` search_path (migration `20260720224204`); redeployed `send-pending-notifications` **v15** (17 templates — added `annual_review_due`/`annual_review_consult_scheduled`/`lifestyle_review_due`, the long-flagged catch-up). **Merge train:** #95/#92/#86/#93 merged to `main-dev` via gh, then #87/#89/#90/#91/#88/#94 integrated into one branch (PR #96). Cross-branch collisions fixed: `attestation-actions.ts` (#89 `attestRedFlags` + #88 `signAttestation` both created it → combined), `vitals.ts` `waist_circumference` dup (#89 vs #88 → deduped), `plausibility.ts` switch `default` (new waist vital), `database.types.ts` regenerated from remote (canonical superset) + analytics_* nullable-arg hand-patch re-applied, #88 `ATTESTATION_VERSION` made module-local (use-server build fix), and a stale ML screening-interpretation test updated to match #88's deployed sensitive-flag (hiv/hep) behavior (the trigger recognises them; nothing silently swallowed). Verified: typecheck/lint/452 web+shared tests/full `next build` + ML ruff/mypy/pytest all green; marketing + the new editor browser-verified. **Founder changes:** obesity protocol **re-signed by the real Medical Director** (`chronic_obesity_who`, was a demo placeholder) — programme already active — and added to marketing (`/obesity` route + product page + chronic-care/services listings); `90-Day Health Reset` CTA → `/signup`; add-on prices confirmed (Lifestyle Coaching ₦25,000/mo, Annual Health Review ₦70,000/yr); **diaspora USD prices corrected** (essential/complete/parentcare USD were ~6–8× below GBP parity — locked only by orphaned test subs) + new `platform_currency_settings` (GBP→USD reference rate) and super-admin `/admin/settings/diaspora-pricing` editor (migration `20260721100000`). **Owner follow-ups:** the repriced USD plans have `stripe_price_id=null` → run "Sync to Stripe" (needs live Stripe creds) before USD checkout works; home-visit/logistics stay inactive by design; release `main-dev` → `main` after PR #96 merges.

### 2026-07-21 — Pricing audit + hardening (branch `claude/pricing-hardening`, explicit ask)
Full pricing review (clarity / affordability / competition / unit economics vs Nigeria 2026) followed by fixes for everything it surfaced. **CFO-hat pricing decisions taken this pass (docx guide v3 should absorb them):** ParentCare NGN ₦20,000 → **₦25,000/mo** (₦250,000/yr; extra parent +₦8,000/mo) so its per-parent price stops undercutting the ₦30,000 Dedicated Care Coordinator add-on it bundles; **"Annual Health Review" renamed "Annual Doctor Review"** everywhere (marketing, `add_ons.name`, patient/clinician UI) to kill the Annual-Health-Check near-collision; Annual Health Check marketing price aligned to the live `panel_bundles` price (**₦65,000**, was advertised ₦60,000); Family Lite's "every member gets Complete Care–level monitoring" promise reworded to needs-matched (the old wording contractually over-promised the loss-making case — Complete-level doctor cost/member exceeds the plan's ₦3,125/member/mo).
- **Live revenue leak closed:** stale `family_gbp` (£80/yr, active WITH a real Stripe price — genuinely purchasable) and `family_usd` ($107/yr) rows deactivated.
- **Provider-sync repair:** all 6 USD plans had no `stripe_price_id` (the known go-live gap) — synced to real Stripe Prices and now checkout-able; the go-live repriced `lifestyle-coaching` (₦25k) and `annual-review` (₦70k) add-ons were active with NO Paystack plan (attach would fail) — new Paystack Plans created + written back; repriced ParentCare rows got fresh Paystack Plans. Every active paid plan/add-on now verified to carry a provider object (audit query in this pass). NOTE: all provider objects are on the TEST-mode Paystack/Stripe accounts (same as the deployed app) — a live-keys cutover still requires a full re-sync.
- **Marketing `/pricing` rebuilt for clarity:** 3-question `PlanFinder` (who/health/where → one recommendation); the three Family tiers collapsed into one card with a Lite/Plus/Premium toggle under a "For your family & parents" row (7 stacked cards → 5 visual choices); "Tarragon vs your HMO" comparison table (complementary framing — keep your HMO, we're the monitoring layer; preempts the ₦3,500-Reliance comparison); "Typical prices" section for BOOK & PAY items mirroring the live `lab_tests`/`panel_bundles` catalogue (HbA1c from ₦8,000 etc. — re-derive if partners reprice); 3 new FAQs (HMO-vs-Tarragon, lab costs, Check-vs-Review).
- **Bulk price adjustment tool (the annual inflation review, per the 30-day-notice promise):** `PriceAdjustmentManager` on `/admin/settings/subscriptions` + `previewPriceAdjustment`/`applyPriceAdjustment` server actions + pure `lib/billing/price-adjustment.ts` (9 tests). Percentage change across plans/add-ons with currency scope; NGN rounds to nearest ₦500, GBP/USD to whole units; rows with a price lock OR any active/trialing subscriber are never touched (reported for the existing clone flow — the subscriber count matters because the lock trigger only fires on the Paystack webhook path, so Stripe subscribers can sit on unlocked rows); adjusted rows get provider refs cleared + auto re-sync (Paystack Plans/Stripe Prices are amount-immutable) with the inactive-until-synced convention; every change lands in the immutable `audit_log`. The 30-day patient notice is deliberately a comms step (broadcast), not automated.
- **Verified:** typecheck/lint clean, 379 web tests green; `/pricing` browser-verified on a worktree dev server (:3077 — the main checkout's :3000 server belongs to another session on a stale branch and initially masked the changes): PlanFinder click-through recommends correctly (parents+one-condition+abroad → ParentCare Diaspora £119 with the £99 single-parent secondary), family toggle switches Lite/Premium content, zero stale strings (`₦60,000`/`₦20,000`/old name), no console errors. Admin bulk tool NOT browser-verified (no admin test credential exists since the 2026-07-19 auth cleanup — logic covered by unit tests + the DB-proven lock/sync semantics).
- **Same-day follow-up (explicit ask): docx guide v3 + real diaspora Family plans.** `docs/Tarragon_Health_Pricing_Guide_v3.docx` regenerated from scratch via docx-js (v2 kept for history; v2 predated ParentCare entirely and still said "nurse" throughout — v3 absorbs every 2026-07-21 decision, adds ParentCare as Plan 5, the newer add-ons, USD pricing, typical lab prices, an HMO-comparison section, and a v3 changelog; content round-trip-verified from the generated XML since no LibreOffice/pandoc exists on this machine for a visual render). `pricing.ts` header now cites v3 and retires the nurse→doctor translation exception. **Diaspora Family plans are now real self-service tiers, not quote-only:** `family_lite/plus/premium_gbp` (£590/£890/£1,390 yr) + `_usd` ($790/$1,190/$1,890 yr) plan rows + 6 tiered extra-member add-ons (+£120/£160/£250; +$160/$210/$340) created, Stripe-synced, and active — anchored to NGN tier ratios with the established diaspora skew (Family Lite = one Complete yearly, exactly like NGN); the old stale `family_gbp`/`family_usd` rows stay permanently deactivated as legacy. Works with zero new plumbing: `private.validate_family_plan_member_count` keys on `parentcare%`/else and `extra-family-member%` patterns, and `isFamilyShapedPlanCode` matches `family_*` — verified before building. Marketing GBP tab now renders the family toggle card + ParentCare under "For your family & parents" (grouping switched to id-prefix matching); PlanFinder's family+abroad path recommends Family Lite/Plus instead of "message us"; `DIASPORA_FAMILY_NOTE` reduced to a >6-people custom-quote note; seed.sql reconciled (new tiers, extra-member add-ons repointed/repriced, and the stale pre-go-live USD plan prices fixed to live values). Browser-verified on the worktree server: GBP family card toggles all 3 tiers with correct prices/footnotes, PlanFinder family+multiple+abroad → Family Plus £890 with Lite secondary, old quote-only text gone, no console errors. typecheck/lint/379 tests green.
- **Diaspora price rebase (same day, founder decision — "diaspora seems expensive; fair AND profitable; new platform must build trust"):** the entire GBP/USD price book cut ~50% — the buyer's care recipient is in Nigeria (same cost base as NGN), a self-subscribing diaspora member can't use the partner network until they visit, and the old 6-9x-NGN premium priced out a trust-poor new brand. Now ~2.5-3.5x NGN, still ~80-85% gross margin: Essential £15/mo (£150/yr), Complete £29 (£290), Premium £49 (£490), ParentCare £59 for 2 parents (£590/yr; extra parent +£19/+£190), Family Lite £290 / Plus £430 / Premium £620 per yr (extra member +£60/£80/£120); USD ≈ 1.3x GBP ($19/$39/$79; family $390/$570/$820; extra $80/$110/$160; extra parent $25/$250). All 20 plans + 6 extra-member add-ons repriced on live DB, re-synced to fresh Stripe Prices (28 active GBP/USD rows verified synced, 0 broken); marketing GBP tiers/PlanFinder/seed.sql/docx v3 all updated in lockstep, plus a new `DIASPORA_SELF_USE_NOTE` honesty block on the GBP tab (labs/refills/home visits need Nigeria; monitoring works anywhere). The orphaned dummy `parentcare_usd` subscription (null subscriber_id, deleted test account) was cancelled on Stripe and deleted (with its 3 test payment_transactions rows). **NGN prices deliberately unchanged** — analysed as fair-and-proportionate (Essential ₦8,000 ≈ less than one private GP visit for a month of monitoring, ~50% gross margin at the 1:120 ratio; Essential is price-locked with 2 real subscribers anyway). Browser-verified (rebased GBP tab, self-use note, zero stale prices, no console errors); typecheck/lint/tests green.
- **Founder follow-ups:** at live-keys cutover re-sync every plan/add-on to the production Paystack/Stripe accounts (all current provider objects are test-mode).

### 2026-07-21 — Platform app shell + dashboard IA polish (branch `claude/platform-ui-app-shell`, explicit ask)
Founder asked for the platform UI to meet international design standards. Presentation-layer only — no data logic, RLS, or entitlement wiring touched; every card/component renders exactly as before, just inside a real application shell.
- **App shell (`components/shell/app-shell.tsx` + rewritten `(dashboard)/layout.tsx`):** persistent role-aware left sidebar (brand lockup, grouped nav, user block with initials avatar + sign-out) on desktop, hamburger + slide-in drawer on mobile (closes on route change via the state-adjust-during-render pattern — the naive `setState`-in-effect version fails `react-hooks/set-state-in-effect`), sticky topbar with role chip, `max-w-6xl` content container, tagline footer. Nav config in `lib/navigation.ts` per role (patient / clinician [17 links in Worklists/Reviews/Messaging groups] / doctor / admin [Operations/Commercial groups] / pharmacist / analyst / care_coordinator / corporate_admin / hmo_admin). **RSC gotcha fixed live:** icon COMPONENTS can't cross the server→client boundary — nav config carries icon NAMES resolved via a new `APP_ICON` combined map in `lib/icons.ts` (which stays the only sanctioned lucide import surface; `NAV_ICON` added for wayfinding icons).
- **Patient dashboard IA:** the ~40-card single stack is now 7 labelled `DashboardSection`s (Overview / Vitals & symptoms / Medications & pharmacy / Prevention & screening / Labs & bookings / Care & support / Profile & settings) with a sticky scrollspy `SectionNav` chip row (`components/shell/section-nav.tsx`, IntersectionObserver). Safety surfaces (EmergencyAlert, DangerSymptomCheck) deliberately stay above the section nav. All `RequiresEntitlement` wrappers/props preserved verbatim; the old top quick-links row moved into the sidebar.
- **Polish:** `DashboardPlaceholder` header upgraded (tinted icon chip, tighter type, "Coming in Sprint 2" copy → "On the roadmap"); Card gains hover shadow transition; doctor page now uses the shared header; clinician dashboard's 13 bare `<p>` links → a proper "Worklists & tools" icon card grid.
- **2 pre-existing bugs fixed in passing:** duplicate `waist_circumference` entry in `VITAL_TYPES` (vitals-form — the source of a live React duplicate-key console error, a #88/#89 merge artifact) + its unreachable duplicate switch case in vitals-history.
- **Verified:** fresh throwaway fixtures (ui.shell.{patient,clinician,admin}@tarragon.test, deleted after) driven through a real browser on a worktree dev server (:3050): patient shell + section anchors land exactly below sticky chrome (`scroll-mt-36`), mobile drawer opens/closes, clinician worklist + quick-link grid + sidebar active states (`aria-current`), admin grouped nav, `/analytics` renders inside the shell with its own sub-nav intact, zero console errors after fixes. `pnpm --filter web typecheck`/`lint`/`test` (370) + full production build clean. Note: the preview MCP's `preview_start {name:"web"}` launches from the MAIN checkout even when the session cwd is a worktree — run the worktree dev server yourself and open the URL instead.
- **Next:** none blocking. Optional follow-ups: same section treatment for `/clinician/patients/[id]` detail page; consider a compact "quiet hours" notification bell in the topbar when a notifications surface exists.

### 2026-07-21 — Partner integration layer: inbound API keys + outbound registry (branch `claude/partner-integration-api`, explicit ask)
Server-to-server integration surface (docs/INTEGRATIONS_API.md is the partner-facing spec). **Inbound:** `api_keys` (org-scoped, SHA-256 hash only — full `th_live_…` key shown once at issue; scoped + revocable, RLS admin-or-`integrations.manage`) authenticating `GET /api/integrations/me` and `POST /api/integrations/device-readings` — patient identified by `patient_number`, device by vendor serial (a `patient_devices` row is auto-provisioned per serial so the existing dedupe index makes partner retries idempotent and the device appears in the patient's list); readings land in `vitals_readings` via the same pipeline as manual/BLE (no parallel table). **Outbound:** `partner_integrations` registry (base URL + auth header + secret, admin-only RLS both directions) + never-throw 5s-timeout `callPartner` helper (ml-client contract) + a "Test connection" ping. Admin UI `/admin/settings/integrations` (issue/revoke keys with the show-once flow, partner CRUD/activate/test); new `integrations.manage` permission key. Migration `20260721163131` applied+recorded; types hand-patched. **Verified:** 389 web + 47 shared tests green; live-endpoint E2E on a dev server with a real temp key (valid/bogus key, stored reading + device auto-provision, dedupe-on-retry, cross-org patient 404, range rejection — fixtures deleted); admin UI browser-verified incl. issue→show-once→revoke; RLS isolation proven via rolled-back jwt-sim SQL (patient sees 0 keys/0 partners); `get_advisors` has zero findings on the new tables.

### 2026-07-21 — Mobile app: installable PWA + Expo shell + integration train ([PR #100](https://github.com/Kolalonge-creator/Tarragon-Health/pull/100) via integration [PR #101](https://github.com/Kolalonge-creator/Tarragon-Health/pull/101), explicit ask)
Two layers so the platform is viewable as an app on iPhone/Android and stays in lockstep with every web deploy — the founder's "generate the app" ask.
- **Installable PWA (`apps/web`):** `app/manifest.ts` (standalone, `start_url: /login` so an authenticated install opens on the role dashboard, never the marketing homepage), Guard Leaf icon set (192/512/maskable/apple-touch, generated from `brand/guard-leaf-mark.png`), `appleWebApp` metadata + `themeColor`/`colorScheme: "light"` viewport (the light declaration blocks Android auto-dark force-inverting clinical UI). Branded `/offline.html` + **navigation-only** `sw.js` — caches ONLY the offline page, never pages/API/clinical data (stale vitals are a safety hazard); registered production-only. **Install flow: Safari → Add to Home Screen / Chrome → Install app** on the production URL.
- **Expo shell (`apps/mobile`):** new Home tab renders the live platform in a WebView (`react-native-webview@13.8.6`, SDK 51-pinned; `EXPO_PUBLIC_PLATFORM_URL`, default = production Vercel URL; Paystack/Stripe checkout hosts allowed in-view, other hosts → system browser; Android back walks WebView history) alongside the native BLE Devices tab. Native chrome design pass: brand UI kit (`src/ui/theme.ts`/`components.tsx`), all screens + tab bar restyled with Ionicons — zero logic changes. Real app icon/adaptive-icon/splash. Store distribution still needs EAS + Apple/Google accounts (`eas build`/`eas update`); the Home tab tracks web deploys with no store release.
- **Integration PR #101** (#96-style train) merged #98 + #99 + #100; conflicts resolved (clinician page: #99's QUICK_LINKS grid + #98's "Annual Doctor Reviews" rename; CLAUDE.md: both entries kept).
- **Live 375px logged-in audit** of the integrated dashboard (lipid-test patient fixture, full onboarding wizard → dashboard) found + fixed **3 real bugs**: vitals-trend-chart's unwrapped mode buttons forced every phone viewport to scroll sideways (512px page); the app-shell brand lockup linked to `/`, bouncing signed-in users to the marketing homepage on non-`app.`-subdomain hosts (the live Vercel domain) — now links to the caller's role home; patient-timeline showed raw analyte keys (`total_cholesterol`) in patient-facing copy — display-time humanisation.
- **Verified:** typecheck/lint/379 web + 35 shared tests/full production build (116 pages) green on the integrated branch; mobile typecheck+lint green; zero console errors in the live audit. **Deployment facts:** production = `https://tarragon-health-web.vercel.app`; `tarragonhealth.com` still points at a Squarespace placeholder and `app.tarragonhealth.com` doesn't resolve — attaching real domains to the Vercel project is an owner step.

### 2026-07-23 — Competitive-survey capability build: 11 features in one pass (branch `claude/competitive-capabilities-build`, [PR #113](https://github.com/Kolalonge-creator/Tarragon-Health/pull/113), explicit ask)
Founder asked to "build the whole 25 capabilities" from the 2026-07-23 nine-company competitive survey (artifact in project memory). Honest scope: 5 already built, 1 banned (WhatsApp bot triage), 8 blocked on non-code dependencies (hardware/contracts/real data/provider accounts) — the remaining **11 buildable** all shipped in this one branch. Six migrations `20260723010019`–`010123` (applied + recorded, filenames reconciled to remote); typecheck/lint/389+47 tests/production build green; `get_advisors` no new findings; types regenerated with the analytics nullable-arg hand-patch re-applied.
- **Risk-triggered outreach engine** (the survey's #1 pick): `care_outreach_tasks` + nightly `private.queue_care_outreach()` (`care-outreach-daily` cron, 06:45) scanning latest `patient_risk_scores` (high/very_high) + the `patient_care_gaps` view → coordinator worklist + ONE aggregated WhatsApp nudge per patient per new surfacing (idempotent: partial-unique live task per patient+trigger; re-runs insert/nudge nothing — proven via rolled-back SQL, including a real found+fixed bug: data-modifying CTEs share one snapshot, so `nudge_sent_at` must be stamped at insert, not post-hoc UPDATE). Worklist UI shared between `/clinician/outreach` and the previously-placeholder `/dashboard/care-coordinator`; resolution server-stamped (`private.stamp_outreach_resolution`). ⚠️ goes live on real data at first cron run post-merge.
- **"Next best action" card** on the patient dashboard: one prioritized step (overdue screening → offered consult slots → open adherence check-in → refill ≤7 days → no reading in 7 days → all-clear), pure read-time synthesis, safety surfaces stay above it.
- **Care Coordinator surfaced as a person** (Maven Care Advocate): `care_team_assignment.care_coordinator_id` + `useOrgCareCoordinators` + optional select on the care-team form; YourCareTeam shows a null-gated coordinator line (name fetched via service role after the RLS-scoped assignment read — a patient can't read a coordinator's profiles row).
- **Async doctor visit "Ask a doctor"** (Treat-Me-Now): `async_consults` with forge-proof `private.stamp_async_consult_answer` — only an active doctor-tier clinical_staff session in the org can mark `answered`; attribution server-derived and immutable after; a Care Coordinator/patient session gets `42501` (proven via jwt-sim SQL). 24h `sla_due_at` shown both sides. New `async_doctor_visit` feature granted to complete/family/parentcare variants (features-only update; seed.sql updated in the annual_review-paired arrays; base-NGN-complete seed drift pre-existing — task chip spawned). Patient card entitlement-gated; `/clinician/async-consults` worklist answers via server action + queues `async_consult_answered` notification.
- **Self-serve video-visit slot picker**: `consult_availability_slots` (patients see only open/future/own-org slots) + atomic security-definer `public.book_video_consult_slot` (row-locked; double-book and past-slot rejected — proven) creating a `general_checkin` `video_consultations` row (new enum value in its own migration + relaxed context↔link CHECK, same split as annual review). `/clinician/availability` manager + patient `BookVideoVisit` card (gated `doctor_checkin`, renders nothing with no slots) with best-effort Zoom + `video_consult_booked` reminder.
- **De-prescribing + control metrics** (Virta outcome language): `lib/outcomes/medication-outcomes.ts` (clinician/specialist-sourced stops with reason, 180d; latest `bp_control` in-range share) → `MedicationOutcomesCard` on corporate + HMO dashboards + frozen into `generateOutcomeReport` snapshots; honest "not a claims feed" disclaimer.
- **Employer/HMO eligibility checker** on /corporate + /hmo: server action requiring org name AND the visitor's own phone (exact `employer_roster_members` match — answer only confirms a combination the visitor already knows, never a directory); no-partner misses insert `leads` (`eligibility_corporate`/`_hmo`) — extends the contact page's sanctioned service-role write path. Browser-verified live (lead row landed with normalized `+234…`).
- **ROI calculator** on the B2B pages: all-assumption sliders, default mirrors the ₦150k modeled cost-avoided constant, persistent modeled-estimate disclaimer (math browser-verified).
- **/resources content hub** (the survey's "highest-leverage marketing build"): 7 plain-language guideline-aligned articles in `_content/resources.ts` (BP, HbA1c, lifestyle-for-BP, diabetes signs, weight/BMI/waist, annual check, cholesterol/non-HDL), category listing + SSG article pages, footer link, sitemap includes per-article URLs. **Editorial-team byline only — deliberately NO "clinician-reviewed" badge** (no review record exists; add the null-gated badge when a real review workflow lands).
- **Weekly education drip** (Omada pacing): `health_education_content.drip_week` (null = always on) + `private.health_education_current_week()` (anchored to first progress row / onboarding) filtering `health_education_feed` (same signature, no drop) + `health_education_locked_count` RPC → "N more lessons unlock" line + admin per-item Week 1–12 select.
- **Onboarding plan preview** (honest Noom projection): `PlanPreview` between intake and plan selection — real `prevention_risk_scores` tier + `care_plan_recommendations` framed "pending your care team's review" + a first-90-days outline; no invented numbers, nothing locked behind the screen.
- **Browser-verified** (worktree dev server :3095, real Chrome-pane, lipidtest fixtures): resources hub/article (fixed doubled title suffix), corporate checker+ROI, patient next-best-action ("Log a reading today" — correct), gated Ask-a-doctor, clinician outreach claim→resolve (DB confirms server-stamped resolver), consult answered in-UI with real "Dr Lipid Clinician" attribution + queued notification, availability manager. Zero console errors; all fixtures cleaned up. Also fixed a pre-existing contact-page lint error.
- **Next / owner steps:** ~~redeploy send-pending-notifications~~ **DONE same session (v17, all 24 templates — the long-flagged stale-function catch-up is fully closed)**; Meta approval of the new WhatsApp templates (SMS fallback meanwhile); confirm `async_doctor_visit` stays a comprehensive-tier feature vs. becoming an add-on SKU. The outreach cron's first real run already happened (2026-07-23 06:45): 3 real open tasks in the coordinator worklist, 2 patient nudges re-queued and delivering via the v17 sender.

### 2026-07-23 — Founder rework: paid held-payment video visits, resources CMS, per-programme drip (same branch/PR #113, explicit ask)
Three founder directives on the same session's build, all shipped + verified. Migrations `20260723120000`–`122000` applied + recorded; typecheck/lint/389 tests/production build green; `get_advisors` only the accepted authenticated-RPC advisory on the two new gates.
- **Video visits are now request → pay → HOLD → doctor accepts → booked (video only, never physical, never automatic):** the instant-book slot RPC was **dropped** (it would bypass payment + acceptance). New `video_visit_requests` rides the generic booking-checkout machinery (new `video_visit` BookingOrderType; `payment_confirmed` = money HELD, nothing booked) + `video_visit_prices` price book (**placeholder ₦5,000 platform default — founder to confirm**; org overrides supported; amount pinned server-side by trigger — a tampered client amount is overwritten, proven). Doctor decision via forge-proof `accept_video_visit_request` (doctor-tier only; atomically books the slot, creates the `general_checkin` consult, stamps `accepted_by`) / `decline_video_visit_request` (flags `refund_status='due'`). New `/api/cron/video-visit-refunds` Vercel cron (05:30): 48h-unaccepted → expired + refund due; due refunds executed via the new Paystack Refunds wrapper (`lib/paystack/refunds.ts`, never-throw). Patient card carries the founder-required copy verbatim: **"Not for emergencies… go to the nearest emergency department"** red box + "payment held by Tarragon… only goes through when a doctor accepts" + "visits depend on doctor availability". Clinician queue lives on `/clinician/availability` ("Paid ₦X — held" badges, Accept & book / Decline & refund with reason). No plan gate — it's pay-per-visit. **`paystack-webhook` redeployed (v12)** with the new booking type; `stripe-webhook` source updated but NOT redeployed (video visits are NGN-only until a diaspora price row exists — deploy it at the next Stripe catch-up). Verified: rolled-back SQL (amount pinning, unpaid/patient acceptance both blocked, accept books+stamps+flips slot, decline → refund due) AND full browser click-through (clinician accepted one real fixture request — "Dr Lipid Clinician" stamp, slot flipped, both notifications queued — and declined a second with a reason; patient card showed Booked + declined-with-refund states and all required copy). New template `video_visit_declined` added to sender source (needs the same catch-up redeploy + Meta approval).
- **/resources is now a DB-backed CMS:** new `marketing_resources` table (anon-read published rows — that's the point, it's the public marketing site; admin-only writes; the 7 launch articles seeded in-migration). Marketing pages read via a **bare anon supabase-js client** (`lib/marketing/resources-data.ts` — no auth/platform imports, marketing boundary intact) with ISR (`revalidate=300`) and the retired static library kept as outage fallback; sitemap now includes DB-published article URLs. Admin editor at `/admin/settings/resources` (create/edit with "## Heading" plain-text body format, publish/unpublish/draft/delete, admin nav link). **Browser-verified end-to-end: authored a brand-new article ("Malaria or typhoid? Why testing beats guessing") in the editor → published → rendered live at its own URL with sections + disclaimer, no deploy — left published as real content.** Editorial byline only; the surface cannot fabricate clinician-review claims.
- **Education drip is now per-programme tracks with priorities:** `private.health_education_unlock_week(condition)` — condition-tagged lessons unlock N weeks after THAT condition's care plan went active (a patient starting diabetes 6 months in gets diabetes week 1, Omada-style); general items keep the first-engagement/onboarding anchor. Feed ordering now ranks the patient's condition-specific lessons above general and preserves curriculum sequence (drip_week asc). **Seeded a real 15-lesson starter curriculum** — hypertension/diabetes/obesity weeks 1–4 + general weeks 1–3, honest plain-language content with knowledge checks, `clinician_reviewed=false` until a real review happens. Verified via rolled-back SQL: a fresh HTN care plan unlocks exactly Week 1 of the HTN track with later weeks locked.
- **Next / owner steps:** confirm the ₦5,000 video-visit price (update `video_visit_prices`); the video-visit patient WhatsApp templates (`video_consult_booked`, `video_visit_declined`) need Meta approval (SMS fallback live now — ~~sender redeploy~~ DONE, v17); ~~deploy stripe-webhook~~ **DONE same session (v9, includes the video_visit booking type)**; a real Paystack round-trip of request→pay→accept in a non-sandboxed browser (the checkout redirect can't run in the preview browser — same known gap as every prior payment pass).

### 2026-07-23 — /resources SEO gap closure + weekly drip unlock nudge (same branch/PR #113)
A review of the two most-recently-shipped items above (`/resources`, per-programme drip) found and closed 4 real gaps — 2 code-only, 2 requiring a DB write. Two migrations applied + recorded (locally renamed to match their actual remote-assigned versions, `20260723225107`/`20260723225124`, same recurring drift class as every prior entry in this log); typecheck/lint/389 tests/production build green; `get_advisors` no new findings.
- **JSON-LD on every article:** `MedicalWebPage` structured data (headline/description/datePublished/dateModified/author/publisher, plus `reviewedBy`/`lastReviewed` only when a real review exists) + OpenGraph `article` metadata + canonical URL, all in `resources/[slug]/page.tsx`. Browser-verified the emitted JSON-LD parses and matches the DB row exactly.
- **E-E-A-T review credit:** `marketing_resources.reviewed_by_name`/`reviewed_at` (nullable, same discipline as `health_education_content` — a library-level quality credit, not the per-patient `ReviewedByDoctor` case-review component), settable in the admin editor, gates the byline ("Medically reviewed by X on date" vs the honest default "By the TarragonHealth editorial team") and the JSON-LD `reviewedBy` block. Browser-verified both states render correctly and the admin round-trips.
- **Sitemap `lastModified` fixed:** was `new Date()` on every render for every article (crawlers learn to distrust that). Now reads each row's real `updated_at`. Browser-verified: touching one article's review field changed only that article's `<lastmod>`, every other article kept its real original timestamp.
- **Weekly unlock nudge for the education drip:** the drip engine unlocked content passively with no notification. New `private.queue_health_education_unlock_nudges()` (`health-education-unlock-nudge-daily` cron, 07:15) detects content that unlocks on a patient's OWN track clock (condition-specific or general) and enqueues one aggregated notification — bookkept via a new `health_education_unlock_notifications` (patient, track, week) table so a daily cron re-run never double-nudges.

### 2026-07-23/24 — Nudge moved to a real in-app channel; NotificationBell built (same branch/PR #113, founder correction)
Founder correction: the education-unlock nudge should be **in-app only, not WhatsApp** — it's an engagement nudge, not a reminder/alert/confirmation, and WhatsApp/SMS stays reserved for those per the Non-Negotiable Business Rules. Investigating turned up a genuine platform gap: `notification_channel` has had an `in_app` value with RLS already written for exactly this ("recipient sees own, may mark read" — 20260705211409) since the very first migration, but **no UI anywhere had ever displayed one** — flagged as a deferred "Next" item on the 2026-07-21 app-shell entry and never picked up until now.
- **`private.queue_health_education_unlock_nudges()` now inserts `channel='in_app'`** (follow-up migration `20260724000000`, CREATE OR REPLACE over the function from the entry above) instead of `whatsapp`. Confirmed `send-pending-notifications` only ever queries `channel IN (whatsapp, sms, email)`, so an in_app row is never picked up by that function at all — no edge-function redeploy or Meta template needed for this notification, ever. Removed the now-dead `health_education_unlock` WhatsApp/SMS template from the sender source (was unreachable dead code the moment the channel changed).
- **New `NotificationBell`** (`apps/web/src/components/shell/notification-bell.tsx`, wired into `AppShell`'s topbar for every role) — polls `useInAppNotifications` (`lib/queries/notifications.ts`, explicit `recipient_id` filter even though RLS also permits org-staff read, since this is a personal-inbox surface) every 60s, shows an unread-count badge, a dropdown with per-template rendering (currently just `health_education_unlock`; anything unmapped still shows a generic line + dashboard link rather than disappearing), "mark all read", and click-to-open marks read + navigates to `/patient#prevention` where the health-education card lives.
- **Verified live, non-rolled-back** (a fresh disposable patient account created through the real `/signup` flow + email-confirmed via SQL, given a real hypertension care plan, real dev server on :3112): the real nudge function inserted a genuine `channel=in_app` row; the bell rendered the badge ("Notifications, 1 unread"), the dropdown showed the exact aggregated text ("2 new lessons ready — starting with…"), clicking it flipped the DB row to `status='read'` and navigated to `/patient#prevention`. Test fixture (account, care plan, consents, notification) fully deleted afterward.
- **Admin editor "easily editable" pass:** added a Write/Preview toggle to the `/admin/settings/resources` article editor (`resources-manager.tsx`) that renders the parsed sections in the exact structure the public page uses (title/category/byline/sections/disclaimer) so a non-engineer can see precisely what will publish before hitting Save — pure client-side render of the same `parseSectionsFromText` the save path already used, no new data path. typecheck/lint (incl. the React purity rule — first draft called `Date.now()` mid-render, fixed) all clean; not independently browser-driven this pass (the sandboxed browser's signup form intermittently didn't submit — a tooling flake, no network request even fired — unrelated to this change, which is a pure presentational addition over already-validated logic).
- **Next / owner steps:** the /resources reviewed-by field is genuinely empty for all 8 live articles until the Medical Director actually reads one and an admin fills it in via the (now-previewable) editor.

### 2026-07-23 — seed.sql entitlement reconciliation (branch `claude/charming-hellman-fd6e4b`, explicit ask)
Audited every comprehensive-tier plan row in `supabase/seed/seed.sql` against the live `subscription_plans.features` arrays (same drift class as PR #77's seed reconciliation + the PR #84/#85 entitlement audit). Root cause: the base plan rows are seed-only inserts, but the entitlement migrations (`20260717123000` annual_review, `20260717141000` lifestyle_coaching, `20260717151000`+`20260719213449` health_education, `20260723010040` async_doctor_visit — that last one from another open PR, already applied to the live DB) run before seed on `supabase db reset`, so their UPDATEs no-op and a fresh env got weaker Complete/Family/ParentCare plans than production. Fix: **inlined the full live feature sets** into all 24 `complete%`/`family%`/`parentcare%` insert arrays (all currencies/intervals) and removed the now-redundant post-insert `health_education` array_append block. `diaspora_premium%` deliberately carries no `async_doctor_visit` (matches live); `essential%`/`free` already matched. Prices untouched. **Verified:** scripted diff of every parsed seed array vs the live DB — all 32 plan rows match exactly, zero mismatches.

### 2026-07-23 — Prevention-first repositioning for healthy users (branch `claude/prevention-healthy-users-visibility-0g3ua1`, explicit ask, built on auto)
Founder thesis: healthy people should see the platform as theirs (vaccination guidance, yearly labs, cancer screening, education), with chronic programmes as what follows a finding — a comprehensive 3-layer audit (marketing ~75/25 chronic-weighted; healthy patients hit paywalls on exactly the prevention actions; no prevention SKU existed) then a full build of every gap found. Two migrations applied via `apply_migration` (recorded: `20260723150205`/`20260723150222`); typecheck/lint(0 errors)/389 web + 47 shared tests/production build all green; `get_advisors` security shows zero findings on any touched table.
- **Fixed the dead ₦25,000 prevention-screening add-on:** its `prevention_coordination` feature key was read by NOTHING (a paying customer unlocked nothing). Now wired: screening-calendar booking + the Labs & bookings dashboard section gate on `lab_coordination OR prevention_coordination`; new `UpgradePrompt` copy for the key. Lab order list/results stay visible to Free users below the prompt (they can hold real AHC orders).
- **Annual Health Check now self-purchasable** (was structurally unbuyable: 4-test bundle could never pass the single-test self-service rule, and no checkout existed). `panel_bundles.self_bookable` (data-driven, admin-controlled — ONLY annual_health_check flagged) + a narrow branch in `private.enforce_lab_order_origin` allowing a patient_initiated order for a self-bookable bundle with NO schedule link. **This is deliberately NOT the deferred wellness-testing catalogue** — ad hoc single tests still require a due screening or clinician order (verified via rolled-back live SQL: AHC allowed, hypertension_panel still 23514). New `AnnualHealthCheckBooking` component (FacilitySelector + RegionGate + PayForLabOrderButton, one open order at a time) on the dashboard prevention section, the Health Check journey page, and the new hub.
- **`/patient/prevention` hub** — one destination composing the existing prevention surfaces (Health Check journey link, AHC booking, screening calendar, programmes, risk assessment, vaccinations, annual review + education behind their existing gates); "Prevention" + "Health Check" added to patient sidebar nav.
- **Dual-state dashboard overview:** no active care plan → prevention-first stat tiles (screenings due / next screening / vaccines due / latest BP) + a nudge to build the screening calendar; active care plan → unchanged chronic tiles. `getPatientPreventionStats` in `summary.ts`. AnnualReviewCard moved from Care to Prevention section (same `annual_review` gate). Onboarding intake step now sells the payoff ("two minutes builds your screening calendar; skipping leaves it empty").
- **Tarragon Prevent plan tier** (the missing healthy-person SKU between Free and Essential): 6 rows (NGN/GBP/USD × monthly/yearly), features `[tracking, reminders, education, prevention_coordination, health_education]` — deliberately no chronic/clinician_review (doctor involvement = the plan-independent abnormal-result pipeline). **PRICING IS PLACEHOLDER (₦3,500/mo, ₦35,000/yr; £7/£70; $9/$90) — founder to confirm**; all rows `is_active=false` until synced to Paystack/Stripe via /admin/settings/subscriptions (inactive-until-synced convention). seed.sql reconciled.
- **Marketing rebalance:** homepage gains a "Two ways in, one record" dual-door section (healthy → /prevention, condition → /chronic-care) right after proof stats, healthy sentence in hero body + problem panel, Prevention card in the explore grid; ContinuityPath visual now starts at a "Screening" node (5 moments); PREVENTION_CALLOUT + /prevention product page rewritten to the positive stay-well story (no fear pitch, honest "most years you'll get confirmation all is well"); Prevent tier cards added to NGN+GBP pricing tables; PlanFinder healthy path now recommends Prevent (was a Free dead-end); 2 new pricing FAQs + a homepage "I'm healthy — is Tarragon for me?" FAQ; footer Programmes gains Preventive Health. Fixed a pre-existing lint error in contact/page.tsx (unescaped apostrophe) in passing.
- **NOT browser-verified** (no dev-server/test-credential run this session — composition over DB-verified primitives + green build). **Drift note:** 9 migrations from other sessions (2026-07-23 care-outreach/async-consults/education-drip/video-visit-requests/marketing-resources) exist on remote with no local files on this branch — left alone, types hand-patched with only `panel_bundles.self_bookable` to avoid dragging their in-flight schema in.
- **Second slice (same day, "continue on auto"):** `/annual-health-check` marketing landing page built (hero + 6-item included grid + 4-step how-it-works + honest-expectations block cross-linking Prevent and the Annual Doctor Review; footer Programmes link, sitemap 0.7, homepage prevention-callout secondary CTA — route auto-covered by the `isMarketingPath` allowlist since it derives from `MARKETING_ROUTES`). Fixed the Health Check journey's 3 DEAD stage anchors (`/patient#risk-assessment`/`#screenings`/`#vaccinations` never existed as ids) — now point at real `scroll-mt-24` anchor divs on the prevention hub. Screening-calendar empty state rewritten (old copy said "your care team will schedule…", hiding that the patient's own 2-minute risk assessment is what builds the calendar). 6 new jest tests pin `recommendPlan`'s routing (healthy → Prevent NGN+diaspora, conditions → Essential/Complete, parents → ParentCare, families by load). Verified the onboarding plan-selector's `_yearly(?=_|$)` grouping regex handles all 6 prevent codes (no change needed). typecheck/lint(0 errors)/395 web tests/production build (incl. the new static route) all green.
- **Third slice (same day, competitor-informed, founder-directed "do 1–5 + 7, Tier 3 WHO-essential only"):** built the retention loop every leading prevention platform (Function/Neko/Thriva/Vitality/Apollo — researched live this session) converges on. Migration `20260723164727_who_essential_health_check_tiers` (applied + recorded; trigger/cron verified via rolled-back live SQL: debrief alert fires exactly once on the resulted transition, rebook function runs clean):
  - **Health Check tier ladder** (Apollo/1mg pattern): `health_check_basic` ₦15,000 (HbA1c+lipids, WHO PEN) / annual_health_check ₦65,000 / `health_check_comprehensive` ₦75,000 (adds HIV+Hep B) — **package prices PLACEHOLDER, founder to confirm**. Booking card generalised to a tier picker.
  - **WHO-essential confidential screenings now self-bookable** (founder-directed narrow pull-forward, NOT the general wellness catalogue, which stays guardrailed): `single_cervical_smear` (WHO 90-70-90), `single_hiv`, `single_hep_b` — the privacy-sensitive tests where self-service matters; sex-filtered in UI (cervical hidden for men); **PSA deliberately NOT standalone** (WHO doesn't recommend population PSA) — packages only.
  - **Doctor debrief on every self-booked check**: `handle_health_check_resulted` BEFORE UPDATE trigger raises a `clinician_review` alert (level 2) when a self-booked order hits `resulted` — including all-clear results ("confirmation all is well" is the product); abnormal results still flow the untouched Cat 2→1 pipeline.
  - **Retest cadence**: `queue_health_check_rebook_reminders` daily cron (`health-check-rebook-daily`, 06:35) — 11-months-after-last-resulted rebook reminder, 60-day dedupe, suppressed while a newer check exists (template `health_check_rebook_due` needs Meta approval; the stale send-pending-notifications deploy caveat applies) + an in-card "your last check was {month} — time for this year's" nudge.
  - **Results trends card** ("your numbers moved"): latest-vs-previous per `lab_analyte_readings` code with neutral deltas (no per-analyte verdicts — interpretation stays the doctor's), on the prevention hub + labs section (visible below the paywall prompt too, since Free users can hold real check results).
  - **Health Score v2**: `vaccination` compliance component (weight 10, only scored when something is due — mirrors screening compliance's never-penalise-missing-data rule), `model_version` bumped to `health_score_rule_based_v2`; card label + tip added; test fixtures updated (+1 test).
  - **Health Check "Smart Report"** PDF route (`/api/patient/health-check/report`) — thin title-override wrapper over HealthPassportDocument (QuarterlyReportDocument precedent), download link on the journey page once reviewed.
  - Marketing: `/annual-health-check` gains the 3-package ladder + WHO framing + confidential-screenings note ("from ₦15,000" hero); TYPICAL_PRICES lists all three packages.
  - typecheck/lint(0 errors)/396 web + 47 shared tests/production build all green. seed.sql reconciled.
- **Next / founder steps:** confirm Prevent placeholder pricing + the two new package prices (₦15,000/₦75,000), then sync the 6 plan rows (NGN → Paystack, GBP/USD → Stripe) to activate; browser click-through of check purchase → Paystack → payment_confirmed, the debrief alert appearing on the clinician dashboard, and the Prevent-tier booking gate once a preview env is free. Deliberately skipped per founder: rewards/points layer (6), home collection ops (8).

### 2026-07-24 — Competitive-survey gap closure: child immunization, women's health bridge, AI intake pre-visit summary, voice reminders, wearables slice (branch `claude/pathway-gap-closure`, isolated worktree, explicit ask)
Closed 5 gaps from a prior competitive-teardown survey. **Reconciliation first:** discovered a same-day parallel session (`claude/health-wallet-rewards-referral-wip`, uncommitted worktree) had already applied `child_immunisation_nphcda` and `voice_reminders_and_language` directly to the shared remote DB with no local files on this branch — pulled both migration files in verbatim (byte-identical, git-committed there) rather than rebuild. Also caught and reverted a real mistake this session made before noticing: an early migration added a redundant `profiles.voice_reminders_enabled` column + a duplicate voice-insert in `queue_medication_checkin_reminders`, which would have double-sent a voice notification once combined with the other session's `remap_notification_channel` trigger — reverted both before any app code touched it (`revert_voice_notification_duplication`). Five new/updated migrations applied via `apply_migration` (recorded): `booking_requests_profile_access_manage`, `reproductive_health_profile`, `wearable_connection_tokens`, plus the two pulled-in files. `pnpm typecheck`/`lint`(0 errors, 2 pre-existing warnings)/`test` (408 web) + full production build all green; `get_advisors` shows no new findings on any touched table.
- **1. Child immunization card:** the other session's NPHCDA catalogue insert (BCG/OPV/penta/PCV/rota/IPV/measles/yellow-fever/MenA/HPV-girls) used a new `{age_schedule_weeks, max_age_years, sex}` `recommended_age` shape that was never actually implemented in `computeVaccinationStatuses` — despite that session's own changelog claiming otherwise (verified: no reference anywhere in committed code). Implemented the shape for real (DOB-anchored dose series, age-ceiling, sex filter, +6 unit tests) and threaded `dateOfBirth`/`sex` through `VaccinationProfile`/`generateVaccinationScheduleBestEffort`. Separately, the other session's RLS extension (a `profile_access` 'manage' grantee can log/view a child's doses) still had no way to actually **create** a child's profile in the first place (`profiles.id` is a hard FK to `auth.users`, and `family_plan_members` only links to *existing* accounts via phone lookup) — built `addChildDependentAction` (server-only `auth.admin.createUser`, no password, a synthetic non-deliverable email, then sets DOB/sex + a `family_plan_members` row + an auto-granted `profile_access` 'manage' row). New "whose vaccinations?" subject selector (`vaccination-for-family.tsx`) composes the existing patientId-parameterised `VaccinationRegistry`/`VaccinationBooking`/`LogVaccinationForm` unchanged, just pointed at the child's id. Found `booking_requests` RLS was the one table in this flow still gated to `profile_id = auth.uid()` only (vaccination_records/schedules had already been extended) — extended it the same way. Catalogue-wide UI filter added so 11 new childhood-only entries don't clutter every adult's card (`not_applicable` hidden from the list, not just from due-soon). Verified live via rolled-back SQL (parent can select/insert booking_requests for a managed child; a stranger with no grant cannot, both directions).
- **5. Women's health bridge:** two independent halves. (a) Cervical/breast screening was **already** auto-scheduled generically for every eligible female patient via the existing age/sex screening engine (not gated behind Women's Health enrolment at all) — the real gap was the Women's Health *programme card* showing nothing cervical-specific. Fixed by composing the existing `screen_types`/`screening_schedules` data into the enrolled-programme view (`WomensHealthScreeningStatus`) — no new engine, pure read-composition. (b) Built the genuinely-missing half: `reproductive_health_profiles` (self-reported life stage + last-period-date + average-cycle-length, patient-owned RLS + `profile_access` 'manage' support) + a pure `computeCycleNudges` engine (+7 tests) — next-period estimate honestly labelled "not a prediction", pregnancy → antenatal-booking nudge, perimenopause/menopause → care-team-conversation nudge. Never a diagnosis, never fed into risk/escalation scoring, same discipline as `mental_health_screens`/`lifestyle_assessments`.
- **6. AI intake pre-visit summary:** pure UI/data-assembly bridge, no schema. New `PreVisitSummary` composes the *existing* `prevention_risk_scores`/`mental_health_screens`/active-`care_plans` hooks (no new data, no new scoring) into one compact card at the top of `/clinician/patients/[patientId]` — every Worklist alert row already links straight to that page, so the "lands on the Tier 1 worklist" bridge is satisfied by placement, not a new page.
- **7. Voice-call reminders:** Termii's Voice Call API is the same `/api/sms/send` endpoint as SMS with `channel: 'voice'` (confirmed via Termii's docs — the earlier assumption that Termii Voice was OTP-only turned out wrong; it accepts arbitrary text-to-speech). Implemented `sendTermiiVoiceCall` + dispatcher wiring in `send-pending-notifications`' *source* (not deployed — joins the existing stale-deploy queue flagged throughout this file) and a patient-facing reminder-channel toggle (`ReminderPreferenceForm`) on top of the other session's already-built `profiles.preferred_reminder_channel` + transparent remap trigger.
- **9. Mobile stores + wearables (buildable slice only, per explicit founder-equivalent decision — store creds/wearable API creds are owner prerequisites this session cannot obtain):** built the patient-facing "Connect a wearable" card (shows "Not yet available" rather than a dead click-through for any of the 4 providers with no real developer app registered — configured-provider check stays server-side since the env vars are server-only), the OAuth authorize-redirect + callback routes (real per-provider token-exchange endpoints, HMAC-signed short-lived `state` for CSRF, service-role-only token storage — added `wearable_connections.access_token/refresh_token/token_expires_at`, missing until now), and a webhook ingestion route. **Honest caveat:** Fitbit/WHOOP's real webhooks are notify-only (they push an owner ID and expect an authenticated follow-up GET, not inline data) — the webhook route resolves the connection and stores any *inline* reading data a provider's webhook config does carry, but the notify-then-fetch half needs a real registered webhook to develop against, so it's flagged as a scaffold, not asserted as finished. Store submission (`eas build`/`eas submit`) remains untouched, per the same reasoning.
- **Not browser-verified** this session (composition over DB-verified primitives + green production build + rolled-back SQL for the two RLS-sensitive changes). **Next / owner steps:** browser click-through once a preview env is free (add a child → log a dose → see the card; set a life stage → see a nudge; toggle voice reminders); register real developer apps with Oura/WHOOP/Garmin/Fitbit + Apple/Google store accounts (owner-only, unchanged blocker); the `send-pending-notifications` catch-up redeploy (now even longer overdue — voice/language/several other queued templates all wait on it) is one deploy that unblocks multiple features at once; reconcile with the parallel `claude/health-wallet-rewards-referral-wip` session when it lands (its wallet/referral/testimonials layer is untouched by this branch).

## Definition of Done
- TypeScript: compiles, ESLint passes, tests pass, migrations committed
- Python: mypy passes, pytest passes, all Pydantic schemas typed
- Both: feature branch (never commit to main), `.env.example` updated for any new vars, works fully via app/web — WhatsApp/SMS notifications are additive, never required

## What Claude Must Never Do
- Never commit directly to `main`
- Never hardcode credentials
- Never bypass Supabase RLS, "just for this query"
- Never give the ML service direct database access
- Never skip Zod validation (TS) or Pydantic schemas (Python)
- Never design a patient-facing feature that requires a WhatsApp send to succeed, or that only works via WhatsApp — app/web is the interface for every core action; WhatsApp/SMS is notifications plus human doctor↔patient support chat, not a transactional interface
- Never build a WhatsApp-initiated signup, onboarding, or account-creation flow, and never build automation (bots/intent parsing) that turns an inbound WhatsApp message into a platform action — signup and core actions are app/web only; inbound WhatsApp only ever routes to a human clinician inbox
- Never deprioritise or silently swallow an abnormal screening result event
- Never invent a standalone sub-brand name for an internal product (see `docs/BRAND_GUIDE.md` §7)
- Never render a UI element claiming a doctor reviewed a specific case without a corresponding `reviewed_by`/`reviewed_at` record — the "Reviewed by Dr. X" pattern must be a single shared component that is null-gated, never a hardcoded string (see `docs/CLINICAL_TRUST_MODEL_SPEC.md` §2, §9)
- Never grant a Care Coordinator account write access to medications, escalation resolution, or protocol signing — app-layer gate only, see Clinical Tier Ladder above
- Never infer or default a `doctor_tier` in code — an unset tier means the record needs an admin to assign one; same null-gating principle as `reviewed_by`/`reviewed_at`
- Never rebuild the Annual Health Review as a parallel review record that re-does the condition reviews — it is an orchestration layer that **adopts + rolls** the patient's existing `medication_reviews` (and reuses `patient_risk_scores`/`care_plans`/Zoom `video_consultations`); condition-specific reviews must stay intact and running on their own cadence (see 2026-07-17 Current Sprint entry)
- Never build functional code (not just schema scaffolding) for the full referral-matching pipeline, patient-initiated wellness testing, or Employer/HMO risk dashboards without an explicit ask — see Clinical Tier Ladder above. (Home sample collection and medication delivery logistics were the same kind of guardrail until pulled forward on explicit ask 2026-07-16 — see Current Sprint.)
- **Superseded 2026-07-15 — Tarragon now directly employs its own doctors; a named doctor is the default face of the day-to-day patient relationship** (see `docs/CLINICAL_TRUST_MODEL_SPEC.md` §1, §9, updated same date). Escalation-doctor review (a second, more senior doctor, distinct from the patient's own day-to-day doctor) is still earned per-case and must never be claimed without a real `reviewed_by`/`reviewed_at` record — that specific attribution rule still applies.

## Where to Look
- System architecture, topology, RLS model, event pipelines, infra → `docs/ARCHITECTURE.md`
- Business model, pricing, full DB schema, 7-sprint plan, clinical protocols, launch gates → `docs/FEATURE_SPEC.md`
- Brand voice, tagline system, colour/type tokens, dashboard copy patterns → `docs/BRAND_GUIDE.md`
- Public marketing site — sitemap, page copy, design tokens, hostname routing, DoD → `docs/MARKETING_SITE_SPEC.md`
- Logo assets → `/brand/Tarragon_Health_Logo_Mark.png`, `/brand/Tarragon_Health_Logo_Lockup.png` (marketing deploy copies → `apps/web/public/brand/`)
- Competitive-intelligence feature roadmap (Health Score, wearables, symptom tracking, fee-at-risk contracts, phased Now/Phase 2/Phase 3) → `docs/FULL_SPECIFICATION_V4.md` — additive feature layer only; per its own §11 guardrail it informs the roadmap and never overrides "Current Sprint" above
- Clinician/doctor role architecture, per-touchpoint attribution rules, escalation→doctor review flow, `clinical_staff`/`care_team_assignment`/`protocol_versions` schema, MDCN/NMCN compliance → `docs/CLINICAL_TRUST_MODEL_SPEC.md` — authoritative on conflicts touching clinician attribution or escalation branding; its Stage/§ cross-references map to the original `docs/source/` planning docs, not `FEATURE_SPEC.md`'s Sprint numbers — see the reconciliation note at the top of the file
- 5-tier doctor ladder, Care Coordinator role, doctor-tier staffing/indemnity rules, phased Phase 1/2/3 roadmap (specialist-matching engine, wellness testing, Employer/HMO dashboards, home sample collection, medication delivery) → `docs/Tarragon_Health_Master_Operating_Plan_v4.md` — authoritative on the clinical staffing model where it conflicts with `CLINICAL_TRUST_MODEL_SPEC.md`'s older flat-role language
