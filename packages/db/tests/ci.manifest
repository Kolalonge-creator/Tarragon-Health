# Proof scripts run by scripts/run-db-proofs.sh, and therefore by the
# "Supabase migration replay" CI job, against a freshly reset local stack.
#
# A file here must: be idempotent, build its own fixtures, wrap itself in
# BEGIN/ROLLBACK, and depend on nothing but what the migrations and
# supabase/seed/seed.sql create. It must fail loudly (raise exception) rather
# than only recording a FAIL row, though the runner catches both.
#
# Everything else lives in ci.excluded with a reason. Nothing may be in
# neither: the runner fails if a .sql file is unlisted, because a proof that
# runs nowhere is the exact problem this manifest was added to fix.
care_access_request_identity_and_pending_read_window.sql
care_access_request_phone_proof.sql
doctor_time_entitlement_grantable_by_purchasable_product.sql
force_safe_patient_order_insert_defaults.sql
refund_ledger_reversal.sql
service_purchases_insert_only_via_intent_rpc.sql
symptom_red_flag_handler_pages_a_clinician.sql
critical_notification_dead_letter_without_pathway.sql
finance_posting_failures_queue.sql
payment_activation_amount_and_reference.sql

# --- Added 2026-09-05 -------------------------------------------------------
# Nineteen more, each confirmed to build every fixture it needs. Where a script
# borrowed something the live project happens to hold -- a @tarragon.test QA
# account, a hardcoded patient uuid, "the first patient in public.profiles",
# "the first spare profile with no clinical_staff row" -- that party is now
# minted here instead. The organisation is resolved from public.organisations
# (migration 20260706084837 seeds the direct-consumer org) rather than from an
# existing patient, and any entitlement is resolved by FEATURE
# (`'vitals_red_flag_doctor_escalation' = any(features)`) rather than by a
# product code, since the packs that used to carry it are is_active = false
# after the pay-per-service pivot and a by-code fixture would entitle nobody
# and pass vacuously.
acting_for_someone.sql
english_only_no_voice.sql
fertility_assessment_requests_staff_gate.sql
get_since_last_visit_summary.sql
glucose_emergency_backstop.sql
integration_gateway.sql
medication_logs_acting_for.sql
medication_logs_append_only.sql
medication_missed_reason_routing.sql
patient_reminder_groups_rls.sql
pregnancy_antenatal_postnatal_rls.sql
red_flag_handlers_notify_phoneless_clinicians.sql
reminder_time_window.sql
reproductive_health_profiles_rls_regression_fix.sql
reproductive_health_profiles_write_side_category_scoping.sql
spo2_red_flag_engine.sql
temperature_red_flag_engine.sql
tier_authority_monotonicity.sql
vitals_red_flag_notification_wiring.sql
vitals_red_flag_plan_gate.sql
revenue_recognition_orphan_source_guard.sql
migration_replay_leaves_no_orphaned_finance_rows.sql
population_health_engine.sql

# --- Added 2026-09-10 -------------------------------------------------------
# The B2C revenue reset. Proves the four rules in that change that carry real
# money or real clinical risk, each in BOTH directions -- asserting only that
# something is refused proves nothing when the mechanism is refusing everyone,
# which is how this platform once shipped a doctor-escalation gate that
# resolved false for every patient with its tests green.
b2c_revenue_reset_invariants.sql

# --- Added 2026-09-10 (second pass) -----------------------------------------
# specialist_providers had a SELECT policy of `using (true)`, so every logged-in
# patient could read commission rates and partner contact details. Proves the
# table is closed to patients, that the directory view they read instead still
# works AND still carries the licence number (deliberately patient-visible), and
# that the admin console is not broken by the lockdown.
specialist_providers_commercial_columns_hidden.sql

# --- Added 2026-09-10 (third pass) ------------------------------------------
# 20260910011851 shipped the Supervised Weight Management product and its full
# clinician-side machinery, but nothing ever created a weight_management_enrolments
# row when a purchase activated -- a paying patient saw the empty state forever.
# Proves activation creates the row with the right shape, a duplicate purchase
# while one is already live is a no-op rather than a failed payment, and an
# unrelated product's activation does not touch this table at all.
weight_management_enrolment_on_purchase.sql

# --- Added 2026-09-14 --------------------------------------------------------
# private.auto_assign_escalation() and the reassignment-authority pair had
# zero test coverage -- the migration's own closing assertion block only
# proved the objects exist and don't error on a dummy uuid. Mints its own
# fresh org/doctors/patients (never borrows the shared seeded org or the QA
# roster) so it runs correctly against both a clean `supabase db reset` and
# the live project.
escalation_auto_assignment_and_reassignment_authority.sql

# --- Added 2026-09-16 ---------------------------------------------------------
# private.auto_assign_escalation() and private.auto_match_internal_specialist()
# only checked clinical_staff.active/doctor_tier -- neither ever consulted
# provider_time_off or provider_availability_rules (both built 2026-08-28 for
# patient-facing appointment slots), so a doctor genuinely on leave with the
# lightest caseload could still be auto-routed a brand-new emergency case.
# These two proofs cover the fix (migration
# 20260916005747_escalation_auto_assignment_respects_availability.sql): on
# leave excludes outright (including dropping alert-owner continuity), merely
# blocked/outside-declared-hours only deprioritises, and zero availability
# rules configured is never penalised (fail open). Each mints its own fresh
# org/doctors/patients.
escalation_auto_assignment_respects_availability.sql
specialist_referral_auto_match_respects_availability.sql

# --- Added 2026-09-14 (second pass) ------------------------------------------
# The Platform Analytics & Audit Console (apps/web .../analytics/) has ~55
# SECURITY DEFINER RPCs individually gated by private.is_analyst(), but only
# one had ever been exercised by any test, and only on the positive path (an
# analyst session gets an answer) -- nothing had ever proven a patient,
# clinician, or anonymous caller is actually refused. Discovers its target
# set live from pg_proc rather than a hardcoded list, so it keeps covering
# the console as it grows. Two independent proofs per function: a structural
# check that its is_analyst()/is_admin() guard precedes any data access in
# the live function body, and a behavioural probe of each function under
# simulated sessions. Found and fixed a real gap in itself while being
# written (a naive "did it raise 42501" check flagged an already-safe
# silent-empty-return function as a false leak) before landing on the
# two-layer design here. public_service_coverage() is the one function with
# no gate at all -- confirmed intentional (state-level service-availability
# flags only, no PHI) -- and its lack of a gate is what proves the
# structural check actually discriminates (sabotage control) rather than
# passing vacuously.
analytics_console_rpc_authorization_gate.sql

# --- Added 2026-09-15 (main-dev -> main release reconciliation) ------------
# Found unregistered while merging the Diagnostic Safety Pathway engine
# (originally PR #317) into main-dev's current manifest-completeness check --
# the proof itself already existed and covers four real migrations
# (diagnostic_episode_core/closure_criteria/referral_linkage/
# repeat_test_recalls), it just never got added to this list.
diagnostic_episode_engine.sql

# --- Added 2026-09-17 --------------------------------------------------
# Continuous clinical note: guarantees a draft clinical_encounter_notes row
# the moment an escalation/async consult/video consultation concludes,
# rather than depending on a clinician remembering to open "new note".
# Proves auto-drafting + idempotency + that a manual insert can never
# self-claim auto_generated=true (server-derived, no GUC leak).
auto_generated_continuous_clinical_note.sql

# --- Added 2026-09-17 --------------------------------------------------
# Platform Credit: a patient session cannot write platform_credit_balances/
# _ledger_entries/_topup_intents directly (only private.platform_credit_apply,
# a SECURITY DEFINER RPC, can), a patient cannot read another patient's
# balance/ledger, a spend consumes the promotional (admin-granted) bucket
# before the paid (real-money) bucket, an overspend is refused without
# partial application, and a sabotage run (restoring the old grant+policy
# shape) proves the RLS checks actually discriminate.
platform_credit_ledger.sql

# --- Added 2026-09-18 --------------------------------------------------
# Standing regression coverage for two of the four bug classes found in the
# 2026-09-18 finance-console and notification-deliverability audits (PR #680,
# #681), added so each stays closed by CI rather than only by memory/CLAUDE.md
# prose. private.remap_notification_channel() no longer honours a dead
# whatsapp/sms notification_channel_preference as a direct lock-in (both
# channels are 100% failure live) -- proves sms/whatsapp preferences fall
# through to the same push-or-fallback ladder no-preference already used,
# email is still honoured, and a sabotage run (reinstating the old lock-in)
# reopens the hole. The payment-activation fee-tolerance bug (PR #531) already
# has standing coverage in payment_activation_amount_and_reference.sql above
# (section 8); the false-consent onlyTypes bug (PR #661) is TypeScript-level
# application logic, not DB logic, and is covered instead by
# apps/web/src/app/onboarding/consent-onlytypes-scope.test.ts (Jest).
notification_channel_preference_dead_channel_no_lockin.sql

# private.finance_post_platform_credit_ledger_entry()'s 'spend' branch used to
# credit revenue account 4100 (immediate) unconditionally, with no check for
# whether the purchased service has a bounded access window -- the equivalent
# card-payment path correctly defers to 2000 + a revenue_recognition_schedules
# row when service_products.access_duration_days is set. Proves a bounded-
# access spend defers to 2000 with a matching recognition schedule, a
# genuinely unbounded (one-off) product still posts straight to 4100 with no
# schedule created, and a sabotage run (reverting to unconditional 4100)
# reopens the hole on the bounded case.
platform_credit_spend_revenue_deferral.sql

# The three DSAR request tables (data_export_requests, data_deletion_requests,
# data_correction_requests) shipped with RLS/attribution triggers allowing an
# admin (org staff, for correction requests) to review a patient's request,
# but no UI ever called that path -- a 2026-09-18 audit found real requests
# stuck pending in production with nowhere to go. Proves the admin queue
# added alongside this test can actually open the gate (review through to a
# terminal status, with reviewed_by/completed_by/fulfilled_by stamped
# server-side) on all three tables, that the requester themselves stays
# blocked from self-approving, and that a terminal (fulfilled) request locks
# against further edits.
dsar_admin_review_workflow.sql

# Standing coverage for the new admin/support "view as" shadow-view feature
# (20260922175144_support_view_as.sql, PR building the read-only support
# debugging tool the 2026-09-18 admin/ops maturity audit found missing).
# Proves: a caller without the support.view_as permission cannot start a
# session; a session cannot target a back-office/admin account or the caller
# themselves; a granted caller CAN start one and the server — not the client —
# derives the ~30-minute expires_at window; while active, the viewer can READ
# the subject's profile but a WRITE attempt is genuinely blocked (read-only
# proven, not assumed); an unrelated staff account with no session of its own
# still cannot read the subject; starting a session writes a real audit_log
# row carrying the reason and a real in-app notification to the subject;
# ending the session immediately revokes the read grant; ended_by cannot be
# set independently of a genuine ending transition; an ended session cannot
# be un-ended or have its window silently extended; the update-guard's
# generic jsonb-diff immutability check still blocks a column the allowlist
# it replaced named; and a caller holding only the Customer support
# administrator preset (no direct grant) can genuinely start a session.
support_view_as_sessions.sql

# private.finance_reverse_entry read finance_journal_entries with a plain
# SELECT and only later set is_reversed = true, so two concurrent reversal
# attempts on the same entry (finance_reverse_journal's synchronous path, or
# finance_approve_request's journal_reversal branch) could both read
# is_reversed = false before either committed -- a real race, though NOT the
# double-financial-effect one it first looked like: a pre-existing unique
# index on (source, source_ref) already makes a genuine second contra
# posting for the same entry impossible, so what the race actually produced
# was a raw, unhandled duplicate-key error (or a misleading false-success)
# instead of a clean business error. The FOR UPDATE fix is still correct --
# see the migration's own header for the full, corrected account, and this
# proof's SABOTAGE 3 for how it verifies that corrected claim rather than the
# original wrong one. finance_approve_request also called finance_reverse_entry
# with no prior check that the target entry was reversible at all, so a
# second pending request approved after the first already reversed the entry
# (or a request whose target entry no longer exists) raised and rolled back
# the WHOLE approval transaction, leaving that request stuck at
# status='pending' forever -- this half is unaffected by the correction
# above and is a real, severe bug. A .sh proof, not .sql, because the first
# finding specifically needs two real overlapping transactions -- see its
# own header for why a single-session BEGIN/ROLLBACK script can't prove a
# lock actually blocks a second session. Proves exactly one reversal
# succeeds under concurrent direct calls (check 1) and under concurrent
# approvals of two duplicate pending requests -- the loser cleanly rejected,
# never stuck pending (check 2) -- and two sabotage runs (reverting the FOR
# UPDATE lock; reverting only the approve-time pre-check) each reopen their
# respective hole. Also fixed in the same migration+PR: the CHECK constraint
# that had silently rejected every journal_reversal request since
# 20260917235414 shipped (drop constraint if exists + a closing DO
# assertion), and the Approvals UI/Zod schemas, which would have thrown on
# the whole approvals query the first time a real journal_reversal row
# existed (apps/web/src/lib/finance/schemas.ts only accepted
# request_type in manual_journal/period_lock).
finance_reversal_concurrent_lock.sh

# Two successive /code-review high passes on migration 20260918100746
# (adding clinical_encounter_notes.clinical_encounter_id), both before it
# ever reached a PR, found 4 real bugs: (1) the sync trigger's row()
# construct had an untyped string literal that raised "cannot compare
# dissimilar column types text and unknown" on every real insert/update with
# a link column set; (2) two AFTER UPDATE triggers on async_consults fire in
# undocumented alphabetical order, so an auto-drafted note could be created
# before its clinical_encounters row existed and never get linked; (3)
# clinical_encounter_id could be set directly by any clinical-tier staff
# member via a plain UPDATE despite its own "never client-supplied" comment
# -- fixed in 20260918101643; (4) the fix for (2) itself introduced a new
# regression, caught by the SECOND review pass on that fix: its fix-up
# UPDATE could abort an entirely unrelated, session-less source-table write
# (e.g. a Zoom webhook completing a video consultation) by tripping
# clinical_encounter_notes_enforce_attribution's UPDATE branch, which has no
# trusted-system escape hatch -- fixed in 20260918103547 by making the
# fix-up best-effort (exception-wrapped, never blocks the write it
# piggybacks on). Proves all four fixes end-to-end via real
# async-consult-answered and video-consultation-completed flows, with a
# sabotage control for bug (1).
clinical_encounter_note_link_bugs.sql

# cancel_care_voucher (20260830103626) located the GL entry behind each
# payment it cancels with a PLAIN select, unlocked -- composed with
# 20260922181900's fix to finance_reverse_entry (its own SELECT ... FOR
# UPDATE, and a RAISE when a concurrent caller already reversed the row),
# a genuinely concurrent, independent reversal of the SAME payment entry
# (an unrelated finance officer action, or an approved journal_reversal
# request against it -- NOT a second cancel_care_voucher call on the same
# voucher, which this function's own top-level care_vouchers FOR UPDATE
# already fully serialises) could abort the WHOLE cancellation transaction
# with an unhandled 'entry already reversed' -- the voucher left uncancelled
# and its refund never queued, over something that isn't actually a reason
# cancellation should fail. Same .sh-not-.sql reasoning as
# finance_reversal_concurrent_lock.sh above (a single session can't hold a
# lock against itself to prove a second one blocks on it). Fix: lock +
# recheck, adding FOR UPDATE to both of this function's target entry
# lookups -- under Postgres's documented SELECT ... FOR UPDATE semantics, a
# lookup that blocks on a row someone else is reversing re-evaluates its
# WHERE clause against the newly-committed row once unblocked, so an
# already-reversed entry is simply excluded from the result rather than
# handed to finance_reverse_entry to raise on. Proves the fixed race
# completes cleanly (voucher cancelled, refund queued, challenger genuinely
# blocked >=1s) and a sabotage phase that temporarily redeploys the exact
# pre-fix body reopens the unhandled-abort failure mode.
care_voucher_cancellation_concurrent_reversal.sh

# NGO-funded cohort module (dormant, platform_modules key ngo_funded_cohort).
# Proves dormancy, org-type/product/cap validation, cross-org invitation-read
# isolation, and the claim flow producing a correctly-shaped care_voucher --
# including the real leak this proof caught before the module ever shipped
# (claim_funding_programme_invitation originally attributed the voucher's
# purchaser_profile_id to the inviting ngo_admin, which handed them a second,
# unrelated RLS door into the row via care_vouchers_select's own "purchaser_
# profile_id = auth.uid()" clause -- fixed in 20260922184722 by attributing
# to the programme's own creator instead). Ends with a same-organisation
# sabotage control for private.is_org_staff()'s ngo_admin exclusion, the one
# targeted enough to actually falsify it (a cross-org attempt is blocked by
# ordinary tenant isolation regardless of the exclusion and would sabotage
# nothing real -- see the file's own header for why).
ngo_funded_cohort.sql

# 20260922183343 added an explicit ::care_access_category cast to 16 RLS
# policies whose can_read_clinical(patient_id, '<literal>') call was left
# bare by earlier migrations, closing the ambiguous-overload hole a future
# CREATE POLICY re-resolving that same bare text would hit now that
# can_read_clinical has 3 live overloads (a real incident on a separate,
# unmerged admin/support "view as" branch, which is where this was found).
# Proves all 16 policies carry the cast, and sabotages by creating a scratch
# policy with the pre-fix bare text against a real table to confirm it still
# fails to even CREATE (42725) today.
can_read_clinical_bare_literal_policy_cast.sql

# --- Added 2026-09-22 — launch-scope audit reconciliation --------------------
# Three standing proofs for the launch-scope audit reconciliation work
# (see docs/LAUNCH_SCOPE_AND_PLATFORM_REBUILD_AUDIT_2026-09-21.md):
#   platform_credit_topups_kill_switch.sql — the new platform_credit_topups
#     module gates record_platform_credit_topup_intent (refuses while off,
#     opens once a superadmin switches it on via set_platform_module, closes
#     again on toggle-back) while spend and balance/ledger reads are always
#     unaffected, proven with a real funded balance throughout.
#   continuous_monitoring_90d_single_tier.sql — the retired 3/6/12-month
#     tiers are inactive but an EXISTING active purchase against one still
#     grants access (deactivating a product must never retroactively revoke
#     it); the new 90-day tier opens/closes the escalation gate and expires
#     exactly 90 days after a real record_service_purchase_intent purchase;
#     buying a retired code is refused outright.
#   preventive_health_check_review_sku.sql — the SKU ships is_active = false
#     (a real purchase attempt is refused, not just documented as blocked)
#     pending Clinical Director sign-off on prevention_intake; the
#     service_purchases trigger that opens/stamps a patient's Health Check
#     on activation is scoped to only this product's feature (an unrelated
#     purchase never touches annual_health_checks) and works even for a
#     patient who never started the free Health Check journey.
platform_credit_topups_kill_switch.sql
continuous_monitoring_90d_single_tier.sql
preventive_health_check_review_sku.sql

# /code-review high on the above continuous_monitoring_90d_single_tier.sql
# migration, run before its PR was opened, found a real bug in it: the new
# product's features array was copied from continuous_monitoring_cover.sql's
# original committed INSERT text (vitals_red_flag_doctor_escalation only),
# never checking that the LIVE continuous_monitoring_3m/6m rows had since
# been expanded to seven features by a later migration. Fixed in
# 20260922193500_fix_continuous_monitoring_90d_missing_doctor_time_features.sql,
# corrected again by 20260922225731_restore_annual_review_reachability_on_
# continuous_monitoring_90d.sql (that fix's own "not the 12-month-only
# annual_review bonus" reasoning turned out to make annual_review
# unreachable platform-wide — see that migration's header). The dedicated
# regression test for this, continuous_monitoring_90d_doctor_time_features.sql,
# needs a real chronic-programme enrolment fixture that only the live
# project has activated (supabase/seed/seed.sql never activates one) — see
# its entry in ci.excluded for why, and confirmation it passes live. CI
# coverage for "annual_review must stay reachable" instead comes from
# doctor_time_entitlement_grantable_by_purchasable_product.sql (line 14
# above), which builds its own fixtures generically and already covers this.

# --- Added 2026-09-22 -------------------------------------------------------
# CMO governed-config INSERT dual-gate: 5 tables were admin-INSERT-only while
# their sign_* RPCs were Clinical-Director-only, leaving nobody who could
# ever insert AND sign a new draft. See the migration's own header.
cmo_governed_config_insert_dual_gate.sql
cmo_clinical_rules_insert_dual_gate.sql

# --- Added 2026-09-23 -------------------------------------------------------
# Doctor-to-doctor curbside consult messaging: participant-only RLS (proved
# as `authenticated`, not `postgres`, so the SELECT policy is genuinely
# exercised), doctor-tier gating (Care Coordinator excluded on both sides),
# same-org-only, forge-proof authorship, closed-thread terminality, and the
# bidirectional in_app notification. See the migration's own header.
curbside_consults.sql

# --- Added 2026-09-23 -------------------------------------------------------
# public.resolve_patient_service_access(): a founder-commissioned launch-scope
# audit's read model aggregating service_purchases into one patient-facing
# service status (monitoring_active/service_active/self_tracking), backing
# the patient dashboard's ServiceStatusCard. Deliberately does not yet cover a
# "review in progress" state -- that depends on annual_health_checks columns
# owned by a still-unmerged sibling branch, see the migration's own header.
# Proves each status derives from real entitlement data (never a hardcoded
# flag), that monitoring wins priority over an unrelated active purchase when
# both are true, that self/caregiver/org-staff access all work, that an
# unrelated caller is refused, and sabotages the org-staff branch by calling
# as staff of a DIFFERENT organisation to confirm it discriminates by org
# rather than admitting any staff account. Run as `authenticated`, not
# `postgres`, so the function's EXECUTE grant is genuinely exercised.
resolve_patient_service_access.sql

# --- Added 2026-09-22 (doctor income analytics) ------------------------------
# See supabase/migrations/20260922190157_doctor_paid_work_and_income_analytics.sql
# and its 20260922192954 follow-up fix (cancelled-appointment carve-out,
# O(doctors x rows) by_product fix, shared clinical_staff lookup, mixed-
# currency flag, redeemed_entity_type CHECK constraint). Covers the
# 'verified_document' and 'appointment' redemption paths end-to-end (the
# latter the one path joining profiles directly, not clinical_staff), the
# analyst gate (open/closed, with a sabotage grant/revoke), totals
# reconciliation, and the cancelled-appointment exclusion, each verified
# live including a wrong-actor-table-join sabotage run.
doctor_income_attribution_and_analyst_gate.sql

# --- Added 2026-09-23 -------------------------------------------------------
# public.get_funding_programme_stats(): the ngo_funded_cohort module's own
# platform_modules.description names "aggregate small-cell-suppressed
# programme reporting" as an intended capability that was never built (PR
# #713 shipped the create/invite/revoke/claim RPCs with zero reporting on
# top). Proves an ngo_admin of the programme's own org gets correct
# invited/expired/revoked/claimed counts, a same-org non-ngo_admin is
# refused, and sabotages by calling as ngo_admin of a DIFFERENT ngo
# organisation to confirm the authorisation check discriminates by the
# programme's own org rather than admitting any ngo_admin.
funding_programme_stats.sql

# sponsored_service_reservations: the launch-scope audit's claim-based
# diaspora flow (pay for a named service against a bare phone number + first
# name, no profile created up front). Proves the full lifecycle -- create as
# pending_payment, a matching charge.success payment activates it to
# invited with a real invite_token, a claim from the WRONG phone is refused
# (the load-bearing anti-impersonation check), the correct phone succeeds
# and creates a real active care_vouchers row -- plus two sabotage runs: the
# same reservation cannot be claimed twice, and a payment of the wrong
# amount does not activate a fresh reservation. Run as `authenticated`, not
# `postgres`, for both patient-facing RPCs.
sponsored_service_reservations.sql

# --- Added 2026-09-23 -------------------------------------------------------
# region_service_available's normalized-state fallback
# (20260923212037_region_service_available_normalized_state_fallback.sql, plus its
# same-day 20260923213439_fix_region_service_available_review_findings.sql
# follow-up): a casing/whitespace/"...State"-suffix variant of profiles.state --
# the exact shape the free-text location field has always allowed -- now
# resolves the region gate the same as the canonical spelling, for both
# location models (regions text[] and state text) and all five gated
# services, without the master switch (service_regions.is_active) or an
# unrelated/unrecognized location ever being bypassed, and with
# service_regions_normalized_state_key_unique proved to reject a
# normalized-key collision outright (the /code-review high follow-up's fix
# for the resolution CTE's otherwise-nondeterministic tie-break). Sabotage
# reverts the function to its pre-fix exact-match-only body and confirms the
# normalized case would then fail.
region_service_available_normalized_state_fallback.sql

# --- Added 2026-09-24 -------------------------------------------------------
# insulin_logs/foot_self_checks/sick_day_logs never got the 2026-08-01
# "acting for someone you support" treatment vitals_readings/symptoms
# already have (20260924071557_diabetes_self_monitoring_acting_supporter.sql).
# Found during a patient-dashboard audit: a supporter (e.g. a parent managing
# a diabetic child's account) could not log any of the three at all, and the
# app-layer code silently misattributed the write to the supporter's own
# patient_id instead -- including a real foot-problem report, whose trigger
# then raised a same-day urgent clinician_alerts row against the wrong,
# non-diabetic patient. Proves a 'manage' supporter can add all three
# (server-stamped logged_by_profile_id, never client-supplied), can never
# revise or delete one (including confirming no DELETE grant exists to
# authenticated at all), a stranger with no grant is refused, the foot-check
# alert lands on the child and never the parent, and sabotages by dropping
# the three *_insert_acting_supporter policies to confirm the insert that
# just succeeded would then fail. Extended same-day to also prove
# 20260924073141_diabetes_self_monitoring_acting_supporter_followups.sql
# (the /code-review ultra findings on that first migration): a supporter can
# read back an entry they just wrote (the missing *_select_own_entry
# policies), and setPatientReportedDiabetesType's separate RPC-level
# misattribution (a parent's diabetes-type pick for their child landing on
# the parent's own patient_diabetes_profile row instead) is fixed and a
# stranger with no grant is refused.
diabetes_self_monitoring_acting_supporter.sql

# --- Added 2026-09-24 (second pass) ------------------------------------------
# private.finance_post_voucher_redeemed had two bugs, found while auditing
# whether promo-code/care-voucher-covered service purchases report accurately
# on the finance dashboard (20260924211113_fix_voucher_redemption_finance_
# posting_amount_and_deferral.sql): it always posted the voucher's full
# face_value_kobo as revenue instead of the amount actually applied to the
# order (redeem_care_voucher's own least(face_value_kobo, payable)), and for a
# service_purchase it always posted straight to 4100 immediate revenue with no
# check for service_products.access_duration_days -- the same bounded-duration
# pack paid by card or platform credit correctly defers to 2000 with a
# recognition schedule. Proves a reward voucher worth more than a bounded-
# duration order's price posts only the covered amount, deferred to 2000, with
# a correctly-sized 4020 recognition schedule; a control proves the same for an
# unbounded order (covered amount, straight to 4100, no schedule); sabotage
# reverts the trigger to the pre-fix unconditional-face-value/unconditional-
# 4100 body and confirms both holes reopen on a fresh redemption.
voucher_redemption_finance_posting_amount_and_deferral.sql

# --- Added 2026-09-24 (third pass) -------------------------------------------
# public.finance_revenue_by_funding_source() (20260924211136_finance_revenue_
# by_funding_source.sql) is a new read-side dashboard RPC, not a bug fix, but
# gets the same standing-regression treatment given the financial-correctness
# stakes: a reward-voucher-funded bounded-duration purchase's recognised
# revenue must land entirely in promotional_minor, a platform-credit-paid_
# balance-funded one entirely in cash_minor, a plain card payment/voucher
# redemption classified correctly at the point-of-sale leg, and nothing in
# unclassified_minor. Sabotage collapses the function back to summing every
# revenue-account credit as cash (the pre-fix gap: no funding-source
# distinction at all) and confirms the split disappears, proving the checks
# discriminate rather than passing vacuously.
finance_revenue_by_funding_source.sql

# --- Added 2026-09-24 (fourth pass) -------------------------------------------
# /code-review high on the previous two migrations, before they were ever
# merged, found two real bugs the fix pass above introduced: private.
# finance_post_voucher_redeemed read the redeeming order's voucher_covered_kobo,
# which is CUMULATIVE across every voucher ever applied to that order, so a
# second voucher redeemed directly against an already-partially-covered order
# (redeem_care_voucher has no "one discount per order" guard of its own -- that
# lives one layer up, in redeem_promo_code) double-counted the first voucher's
# amount; and finance_revenue_by_funding_source computed one blended cash/promo
# ratio per service_purchase and applied it to every revenue_recognition_
# schedules row tied to that purchase, even though a single purchase can
# accumulate multiple, separately-funded schedules (a partial voucher plus a
# later card/platform-credit remainder) -- correct over the purchase's full
# lifetime, wrong for any single reporting period queried before all of a
# purchase's schedules have posted revenue. Fixed at the root
# (20260924213634_fix_voucher_redemption_cross_voucher_double_count_and_revrec_
# rounding.sql) by persisting the exact per-voucher covered amount and the
# exact per-schedule promotional split at the moment each is created, instead
# of re-deriving either from state shared with other redemptions/schedules.
# Proves a second voucher against an already-partially-covered order posts
# only its own amount; a purchase with two separately-funded schedules reports
# a mid-period recognition of only one as fully attributed, not blended with
# the other's lifetime share; the original single-schedule fix still holds;
# and two independent sabotage steps (reverting each trigger to its pre-fix
# shape) reopen the corresponding hole.
voucher_redemption_cross_voucher_double_count_and_revrec_rounding.sql

# account_lockouts_anon_execute_and_search_path.sql: 20260924215642 backfilled
# a missing migration record for public.is_account_locked(_by_phone) (found
# anon-executable with no legitimate caller, the anon-execute-via-PUBLIC
# gotcha recurring again) and revoked anon EXECUTE. Its first version also
# redefined the shared private.set_updated_at() trigger (~280 tables) as a
# side effect of copying in unrelated functions, silently dropping its
# search_path pin -- caught by /code-review high before merge, fixed same day
# in 20260924221235. Proves both: anon lacks EXECUTE on the two functions
# (authenticated retains it), and private.set_updated_at() keeps search_path
# pinned -- each with a sabotage step reproducing the exact regression to
# confirm the check actually discriminates.
account_lockouts_anon_execute_and_search_path.sql
