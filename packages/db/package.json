{
  "name": "@tarragon/db",
  "version": "0.1.0",
  "private": true,
  "description": "SQL-only package: the rolled-back-transaction RLS and invariant suite in tests/, run against the linked Supabase project. Contains no TypeScript - generated database types live in @tarragon/shared (packages/shared/src/database.types.ts), which is the single copy every app imports."
}
